# PF-XX 工作包

## PF 编号

`PF-XX`

## 状态

`待派遣 | 开发中 | 待编译 | 已完成 | 阻塞`

## 设计就绪度（派遣前必填）

`待细化 | 待前置核验 | 已就绪`

“待派遣”是分派状态，不表示可执行。只有批准范围全部已就绪才能实际派遣；缺项必须列编号、责任、输入/退出条件和精确回写章节。编码任务不能承担未完成的表/字段/接口/页面布局设计。

## 负责人

`Claude Code | Harness`

## 目标

用可验证的一句话描述交付结果。

## 实施方案入口

- `docs/implementation/<file>.md`

## 派遣依据快照

- 实施文档版本/精确章节、细化规格版本/章节、代码基线或工作树快照。
- D01～D08就绪检查结论（见实施目录STANDARD-派遣前详细设计与页面验收.md）。
- 数据字典/API样例/PageId线框图/验收案例入口；无SQL或无页面需说明不适用。
- 实际目标环境、隔离测试数据、共享文件唯一写入者和先后顺序。
- 技术核验/PoC与生产范围分开：PoC回写前不得启动未冻结的生产部分。

## 内部任务序列

- `TASK-XXX` 起按实施方案依赖顺序连续执行；这些编号不是 Codex 派遣、验收或提交门。

## PF 工作线与 Worktree

`PF-XX / <固定 worktree 路径；连续任务复用>`

## 分支

`<固定工作线分支，例如 work/pf-02；不为本任务新建分支>`

## 允许修改范围

- 精确目录或文件。

## 禁止修改范围

- 主工作树、其他任务范围、`AGENTS.md`、`CLAUDE.md`、`DSH.md`、`docs/status/CURRENT.md`。

## 页面与协作要求

- 先按已绘线框图实现；黄金页、yefeng结构、主从选择/局部loading/重复提交/菜单发现性遵守共同规则。
- 不干扰VS/VS Code/既有服务；核对实际UnifiedHost/Gateway和数据库拓扑，真实测试数据隔离。
- 总控拥有设计/任务/共享文件排序；执行者内部连续实现、自测、修复，整包稳定交付；设计变化先回总控更新依据。

## 完成条件

- 实施方案中本 PF 当前剩余内部任务全部完成并通过执行者内部验证。

## 交付规则

- 内部任务连续推进，不逐项交回 Codex。
- 整个 PF 完成后保留未提交工作树并返回简短结果。
- Codex 只执行一次最终 Release 编译，成功后提交和集成。

## 结果回写位置

`docs/evidence/PF-XX.md`

## Codex 提交信息

`feat(scope): complete PF-XX scope`

# PF-04 File / Notification / Audit 工作包

## PF 编号

PF-04

## 状态与授权

开发中。2026-09-06 用户授权启动 Core 001～009 的完整功能开发和当前会话独立验收；TASK-PF04-010 Audit Advanced 不在本轮。

## 负责人

- 功能开发：`01a076d2-a8d3-7163-8721-8cae51393d92`，gpt-5.6-luna max（极高）。
- 独立验收：`01a076d3-3abf-7921-a367-9b70749d0780`，gpt-5.6-sol high（高）。
- 总控：`01a0759d-fc1c-7452-8a78-1031774dbb45`。

## 目标

在现有 SystemData 服务边界内交付 File、Notification、Audit Core 的后端、前端、权限、导航、迁移、可靠性和可复现验收，不创建新微服务，不实现 Advanced。

## 实施方案入口

- `docs/implementation/07-Industrial Platform File Notification Audit开发实施方案.md` V1.1，第 1.3、12～17 章。
- `docs/blueprint/05-Industrial Platform平台基础功能与独立模块设计.md`。
- `docs/blueprint/30-Industrial Platform日志审计与可观测性平台设计.md`。
- `docs/blueprint/32-Industrial Platform Service Host与内部模块边界.md`。

## 内部任务序列

TASK-PF04-001～009 按实施方案依赖连续执行；编号不是独立派遣、审批、提交或验收门。TASK-PF04-010 保持待细化。

## PF 工作线与工作目录

- 执行目录：`D:/Code/Industrial Platform/IndustrialPlatform`。
- 分支：`develop`。
- PF-02/PF-03 已提交；2026-09-06 用户明确改定 PF-04 直接使用主工作树，不保留专用工作树或功能分支。
- 开发与独立验收顺序交接，不并行修改文件。除 PF-04 已迁回的未提交改动外，不覆盖、清理、暂存或提交其他本地内容。

## 页面与交互规则

先完整读取 `$industrial-platform-management-page` 及其 `references/table-selection.md`、`references/local-development-acceptance.md`。以用户管理为黄金样板，统一查询区、`AppDataTable`、操作列“更多”、业务弹窗/抽屉；行选择仅在业务需要时使用稳定业务键。根据业务采用整表、主从或树表，yefeng.club 仅作为信息架构与舒适度参考，不机械复制业务功能或视觉。

主从页保持左侧主项稳定挂载，右侧负责空态、加载和错误。复杂主从或三个数据集先写紧凑 ASCII 线框。禁止页面 CSS/DOM 补丁伪造共享组件能力，禁止声明未经实现和验证的跨页选择。

## 允许修改范围

- SystemData 服务内 File、Notification、Audit 的 Domain/Application/Infrastructure/Api/Contracts、迁移、种子、托管作业及测试。
- UnifiedHost、Gateway、Identity 权限目录和现有审计生产者的最小公开接线；不得改变原领域行为或信任边界。
- PF-04 前端 API、页面、组件、路由、导航、权限、语言包和测试。
- 共享管理组件仅限真实需要且有集中回归的最小兼容扩展。
- `docs/evidence/PF-04.md` 及实施方案中由实际实现产生的精确证据回写；总控拥有的蓝图、CURRENT、总 TODO 和本工作包只提交差异建议，不由执行任务改写。

## 禁止修改范围

- 其他 PF 工作线及其功能/evidence；`AGENTS.md`、`CLAUDE.md`、`DSH.md`、`docs/status/CURRENT.md`。
- 新增 File/Notification/Audit 微服务、第二套数据库编排/消息总线、PF-07 调度平台、聊天、MES 业务、Audit Advanced。
- 停止或重启 VS、VS Code、已有服务；修改私密配置；对普通开发数据库做破坏性或造数式 E2E。
- commit、push、merge、reset、清理其他任务内容。

## 完成条件

- Core 001～009 的实施方案验收矩阵 F1～F7、N1～N4、A1～A5、U1 有可复现分层证据。
- 菜单链路 route → API bootstrap → 权限目录/策略 → 版本化导航种子/upsert → 既有数据库接收新种子 → 动态菜单 → 中英文 → 真实页面可进入。
- 页面符合黄金页和更新后的表格/主从规则，正常、空态、加载、错误、权限、取消/重试、分页筛选和业务动作均验证。
- 后端 fresh Release build 后再运行 Release tests；前端相关单测、类型检查/构建通过；真实浏览器完成验收并恢复视口。
- 开发完成内部验证并向独立验收交接；独立验收给出明确结论和缺陷证据。

## 本地执行与交付

先读取 `src/开发注意事项-数据库拓扑与本地配置.md`，识别 UnifiedHost 或独立拓扑，复用现有环境；缺失依赖按任务授权直接启动并记录 PID/URL。写验证只使用临时数据库或隔离租户。

开发任务在主工作树保留未提交修改，并一次性交接关键文件、迁移/种子、拓扑、URL、命令/退出码、浏览器证据、限制和验收步骤。验收任务在稳定交接前仅做只读预检，交接后独立复核，不修改生产源码。最终提交与集成由总控执行。

## 结果回写位置

`docs/evidence/PF-04.md`

## Codex 提交信息

`feat(system-data): complete PF-04 core`

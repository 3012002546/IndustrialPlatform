# PF-05-R1 定向整改与隔离补验

Shared启动链回归关闭（2026-09-09）：独立验收 evidence §17.7 为 `PASS WITH TOPOLOGY LIMITATION`，13文件冻结aggregate `d156b9db36bc0ea9bfb56fb45c23996dd59898d757c3c55a54fcee234f16a3cf`。主控核对SystemData/Collaboration检查路径已先检测账本物理列，Inspect保持只读，缺列交Apply处理。fresh Release 0警告/0错误，定向38通过，全后端1770通过/0失败/6环境门控跳过；其中3个PG用例随后显式启用，在真实任务专属临时库3通过/0失败/0跳过。覆盖旧账本与业务数据、固定applied_on、只读Inspect、首次升级、二次ApplyCount=0、真实drift阻断及Shared/search_path隔离。三个临时库已由夹具清理，普通调试库未DDL，用户Shared模式42703故障按实测范围关闭。PerService目前只有resolver合同，真实四库整链仍须在R3补验；Redis/RabbitMQ/页面/容量故障门禁继续保留，不能据本次通过关闭整包。

启动链回归重新打开（2026-09-09）：用户 PostgreSQL 正常调试再次报 `42703 column checksum does not exist`，位置 `SystemDataServiceInitializer.InspectAsync`，早于迁移Runner补列。此前定向PASS只覆盖Runner，不能证明旧库完整启动，验收 evidence §17.4 已标记 REOPENED / NOT ACCEPTED。当前优先修复全部模块初始化检查/执行的兼容边界：Inspect只读检测旧表/缺列并报告需要升级，Apply负责DDL和受控回填；Collaboration Inspect当前建表/补列/回填也应移入执行阶段。未知数据库、权限、连接异常不得吞并放行。完成标准升级为实际旧版三列账本与业务数据的 Inspect→Plan/Apply→Verify→二次启动，覆盖缺表、缺列、NULL、真实描述/非空checksum/物理drift、后续合法迁移及时间/数据保留；SQLite快回归之外必须有隔离真实PostgreSQL及项目支持的Shared/PerService/search_path证据。主控实际探针确认配置为Shared；默认沙箱PG TCP访问Socket10013，经系统批准同一目标探针成功，故不能把沙箱限制当真实依赖离线。仅使用任务专属隔离库，普通调试库/IDE/现有服务保持不动；无隔离建库权限须报告真实缺口。原两任务优先闭环本回归，模型配置不变。

调试回归处理（2026-09-09）：用户正常启动 UnifiedHost 时，Identity 在 `ID-004-01` 报 migration checksum drift。根因是新增 nullable `checksum` 列后，对历史三列账本产生的 `NULL` 无条件执行校验比较。Identity 与 SystemData 已改为：仅在历史描述完全一致时回填 `SHA-256(id|description)`，保留 `applied_on` 和业务数据；描述漂移、已有非空错误 checksum、物理结构漂移继续失败关闭。独立验收 evidence §17 判定定向修复 PASS，冻结 aggregate `a036da2f0f3d23967e3cae3d6ed7e64be8a33c8883ebb8cc4cd73ebb28a94f8c`；fresh Release 0警告/0错误，后端1765通过/0失败/3外部跳过。主控随后在当前工作树复跑 Identity/SystemData `SchemaMigrationRunnerTests`，分别10/10通过。未连接、改写或重启用户普通调试库/服务；该修复不替代R3剩余验收。

最新阶段进度（2026-09-09）：独立验收 evidence §16 确认 `R2 = PASS WITH RUNTIME CONTINGENCIES`，145/145 文件hash匹配，aggregate `93a40eea05ace769744bca3445a351d2145c209d616462bb4b1be70bb2f13a5d`；fresh Release 0警告/0错误，后端1758通过/0失败/3外部跳过。SQLite下合同、并发、多Case File和三模块迁移验证已通过；真实PostgreSQL/Redis/RabbitMQ、多进程、页面和资源故障门禁转R3，整包仍未准入。本机127.0.0.1默认端口关闭仅说明本机探针结果，R3必须根据实际配置核对本地/远程目标和现有UnifiedHost，不推断全部依赖不可用。

阶段进度（2026-09-09）：独立验收 evidence §15 确认 `R1 = PASS`，25/25 文件hash与冻结 `PF05-R1-dirty-d3f3fc492c2ee9902d9a33f2230e533732ac6f66415a2161037cd6e4eea1d125` 一致；fresh Release 0警告/0错误，后端1725通过/0失败/3外部跳过，Embedded15/15，19个目标前端文件格式通过。完整格式检查仍有132个非目标基线失败。开发已获验收通知进入R2；R2/R3及整包生产准入尚未通过，原范围与设置不变。

2026-09-09 主控决定。主工作树 `D:/Code/Industrial Platform/IndustrialPlatform`，`develop`；接续 `PF-05.md` 与实施08原范围，不创建新任务/分支、不修改任何模型配置、不暂存/提交/推送。

## 输入、结论和责任

输入为 `docs/evidence/PF-05-acceptance.md` §14，冻结 `PF05-dirty-f86415d-472e41094ed37412`；121 文件 SHA-256 清单摘要 `472e41094ed3741253baae3f6de7c8c086abc0e5a7e8087dce138370d74c80ae`。独立通过 fresh Release 0警告/0错误、后端1713通过/0失败/3外部跳过、Collaboration25通过、前端125文件/927测试及类型/Lint/build；A02/A06失败，其余未验/部分通过按原矩阵保留。

主控分类：A06 是现有冻结设计未落实的实现缺陷；A02 是变更文件格式缺陷加历史基线差异；A03～A24 是缺少专项合同和真实环境执行证据，当前没有证据证明环境必然不可用；A25 是交付记录陈旧。没有新需求待用户决定，也未证实模型/运行能力故障。两轮已有实质修复和新增证据，不将“同问题两轮无新证据停止往返”解释为任意两轮后停止。采用以下明确顺序和可检查退出条件，避免再次笼统返修。

开发 `01a07f9e-355b-75f1-ba20-5d41f49f07ca` 唯一修改生产代码/所属测试/开发证据。验收 `01a07f9d-2f78-7c21-9f2e-09144882ac11` 稳定交接后独立验证，只写验收证据。主控 `01a07e50-cf88-7252-ac12-bd5fb54998b8` 只处理有证据阻塞及最终结论。双方通信省略 model/thinking。无共享输出目录并行构建或代码写入竞态。

## R1：先关闭可定位失败

1. 按 `PF05-前置契约与合规执行补充.md` §2.3 完整实现 Embedded 参考装配：宿主服务端认证取得真实主体，签发独立 typ/issuer/audience/kid、60秒断言；接收方验证签名、Origin、浏览器绑定、nonce、tenant映射和持久jti；禁止浏览器裸传 ExternalSubject 决定身份。按既定三元组生成 ext 身份，不自动创建平台用户。
2. challenge 由 Collaboration 自有持久表原子消费，两个实例共享验证；数据库故障失败关闭。样例会话必须可验证、绑定主体/权限/版本，不能只是随机 cookie 或返回未被后续鉴权消费的 token。续期重新从已认证宿主获取断言，停用失效，换人清理旧 Store/草稿/事件 epoch；选择既定同站 BFF/反向代理或安全顶层跳转完成 cookie 约束，不实施两套无业务必要方案、不提供无绑定降级。参考目录不宣称客户 MES 已集成。
3. 先补可复现的负例：浏览器伪造 subject、错 issuer/audience/tenant、篡改/过期、跨浏览器绑定、两实例重复消费、持久存储不可用、续期停用、旧主体迟到事件。成功路径必须走实际签发/交换/鉴权代码；不只测 helper 或 mock 返回值。
4. 对当前全部前端变更文件定向 Prettier 检查并修复，包含验收列出的10个失败文件；禁止全仓格式化。对剩余完整 format:check 失败保存文件清单，并用同一格式器/配置与 HEAD 原内容对比区分既有问题和新增问题；不通过修改格式规则/ignore消除失败，完整命令仍非零时保留事实，不声称全仓门禁通过。
5. 更新开发 evidence 为实际命令、计数、新 dirty manifest 与未验项，保留历史。首次小交付先让验收定向确认 A06 实现和变更文件格式；若同缺陷再次不通过，给出最小失败测试、原因与修复差异，不重复整包口头完成声明。

## R2：合同与一致性验证

按 A03～A18 补齐有意义的缺失行为测试并修复暴露的问题：两实例 nonce；SystemData internal HTTP 正反例；20并发建会话/50并发发送及事务失败无空洞；历史410/隐藏/墓碑竞态；真实 Hub/Redis/背板；多Case文件引用；导出worker接管/期限；持久预算；迁移首次/重复/drift 与 Shared/PerService/SQLite。测试不能以固定数量为完成标准；矩阵每项必须给出对应可运行行为证据或精确未验原因。安全及跨模块变化需要 fresh Release build→test --no-build，复验覆盖受影响总回归，不为每个小编辑机械重跑全套。

## R3：隔离服务与页面补验

先检查仓库数据库拓扑/本地开发文档、当前进程/端口、依赖配置是否具备，隐藏或脱敏凭据；保留用户 IDE 和既有服务。复用安全且不会写入正常调试数据的入口。需要写入时使用任务自有临时库或明确隔离租户；仅对该隔离目标执行仓库既有初始化/迁移，记录目标、归属、PID、端口、日志与清理计划，不迁移普通调试库、不改远程Docker生命周期。权限限制走系统审批，不换任务/工具绕过；连接、账号、依赖不可用须记录实际探针失败及安全可行的下一步，不以“未启动”直接判永久阻塞。

安全实现和合同先通过再运行涉及敏感业务的真实链路。开发提供可重复隔离夹具和 Collaboration Playwright 场景；验收执行实际 UnifiedHost/Gateway、Identity/File/Audit、Embedded 的成功/拒绝链路，PC/PDA/Mobile及管理页、菜单权限、双语/主题/响应式/键盘、2C4G和故障恢复矩阵。复用用户管理黄金样板与 management-page Skill/references；不把 Playwright 文件存在、mock、跳过或旧回归当真实通过。外部资源限制导致无法验证2C4G时记录未验，不伪造资源约束。

## 稳定交接与退出条件

每阶段实际完成后开发冻结相关源码和共享构建，提供包含 tracked/untracked 的源码内容hash清单、新交付标识、可运行测试/夹具及证据；双方直接定向验收和修复，阶段通过后继续下一阶段，无需主控再次派遣。主控不持续轮询，不接收普通进度流。

最终验收仍按 A00～A25 汇总；A02历史格式债务需单独列明，不能掩盖本轮失败；无全局ignore访问权限则仅声明审计实际可见范围，不清除用户配置。任何真实门禁缺证据时保持 PARTIAL/NOT PRODUCTION-ACCEPTED。同一问题两轮确实无新证据时提交最小复现/已排除假设/权限或环境事实，停止该项重复尝试并推进独立可执行项，不自行换模型或创建替代任务。

# PF-06 聊天内屏幕共享与独立语音执行工作包

## 当前有效范围（V5，2026-09-15）

用户重新明确四阶段顺序：1）总结通用协作教训；2）验证已通过页面的代码基线，再清理有证据的冗余；3）平台通用单点登录；4）独立部署接入。**当前主控只完成 1、2；3、4 由用户分别开启新主控，禁止恢复下方旧独立入口/认证返修指令。** 原开发及原验收任务已停止。

- 入口契约与新主控代码索引：[平台单页入口与独立部署交接](../../implementation/details/平台单页入口与独立部署-后续主控交接.md)。平台 `mode=single&token=...` / `mode=single&ticket=...` 与独立 `mode=standalone` 分开；独立 Collaboration 另有 `account` 标识用户。通用单点登录不是 PF06 专属功能。
- 通用协作经验已更新到[统一约定](../../agents/开发协作与页面统一约定.md)，不把业务参数写入通用规范。
- [本次基线验证与清理](../../evidence/PF06-基线验证与冗余清理-20260915.md)：先撤除错误入口尝试并恢复 `ae22f6a`；随后按用户授权完成 6 个生产文件的限定冗余清理，净减少 100 行。新鲜 Release 全方案构建 0 警告/0 错误，后端 Collaboration 157 项、前端定向 189 项及类型检查通过。媒体链路、实时清理顺序、路由和数据库拓扑未改；详细差异与证据见第 6 节。当前改动未提交或推送。
- 平台 PC 先前用户确认通过；历史现场结果保留其原范围，本次未重做真实双端媒体测试。独立部署的列表超时、未知在线、媒体不可达与新入口合同仍未交付，不能因单元测试通过而关闭这些问题。待后续独立部署主控先查实际依赖。
- Native、SQL Server、云部署/TURN及质量稳定性等后置事项不纳入当前收口，不自动成为新通用单点登录任务的实施范围。
- 收束确认：用户反馈清理后 PC、Mobile 实测均无问题；提交前范围、残留引用、忽略文件和差异卫生检查通过。当前第 1、2 阶段收束，17 个文件待用户提交，未暂存、未提交；不恢复第 3、4 阶段开发。

## 以下为历史工作与验证记录（不作为恢复执行授权）

版本：V4.1（独立协作接入与部署标准化收口）；2026-09-15。派遣状态：原独立验收已确认约定范围内PASS，开发及验收收口，不追加生产修改或重复测试。既有平台聊天、语音、屏幕共享及HTTPS手机联调按用户现场确认保留；本轮检查了公共组件隔离，未发现原平台受损证据，不宣称所有部署环境已经验证。完整边界见[独立协作实施合同](../../implementation/details/PF06-独立协作接入与部署实施合同.md)。

## 当前结论与剩余项（2026-09-15）

**用户实机启动回归，当前交付状态重新打开（2026-09-15）：** UnifiedHost 报 REF-INITIALIZATION-DRIFT；EmbeddedHost 报缺少显式 IsStandalone。主控比对发现 ReferenceData.LocalTargetIdentity 无条件加入 schema 改变旧平台账本标识，且本机独立私有配置缺失时配置投影静默跳过。原开发仅修复历史标识兼容与独立配置入口，禁止修改真实迁移账本、清库、关闭校验或影响媒体逻辑。下面最终PASS保留为此前测试范围的历史结论，不能覆盖本次旧数据库/实际启动回归；待定向修复和核验后更新。

最终独立验收：[约定范围内PASS](../../evidence/PF06-standalone-collaboration-final-acceptance-20260914.md)。冻结manifest核验MISSING=0/MISMATCH=0；最终组合测试证明续期跨旧expiry后真实BindMedia、SignalMedia、MediaReady、KeepAliveMedia成功，撤销后下一次调用拒绝且不刷新lease。真实pagehide事件与三进程不同CWD共享SQLite四模块初始化/重复启动验证通过，原有outbox消息保留。最后补证仅修正测试夹具；原平台已通过矩阵未重复执行。

剩余部署验证：真实MES接口联调、PostgreSQL advisory lock、Redis/RabbitMQ、真实双机/跨网/TURN及生产部署。Native、SQL Server和完整裁剪前端包不属于本次完成声明。用户接线入口见[宿主README](../../../src/backend/src/Hosts/IndustrialPlatform.Collaboration.EmbeddedHost/README.md)及[前端部署说明](../../../src/frontend/DEPLOYMENT.md)。后文首轮返修描述仅作历史记录，由本段最终结论覆盖，不再据此启动修复。

当前授权工作：账户URL接入、MES当前用户和人员接口、离线留言/在线媒体邀请、同源多页面身份与注销隔离、多端接听；独立统一私有配置、共库同Schema、独立启动增量初始化。原开发 `01a09510-275e-7a82-99c2-07222783234c` 唯一写源码，稳定后交原验收 `01a0950f-7ccc-71d2-9965-7efe2e7ee66e`。禁止覆盖模型设置或并写；不影响平台云配置，不实施SQLServer/Native，也不把本轮说成已有完整裁剪前端生产包。交付必须列出真实文件、类/方法、字段和中文MES接线示例。

V4首轮稳定交付已进入原验收，当前不能交付用户：主控一致性核对发现独立页面仍是应用级连接、Shared正式环境与Schema/并发初始化尚无完整落实、MES权限接线说明仍偏离合同。原验收一次汇总返修，主控不并写源码。开发报告的后端4/4来自11:43旧Release测试程序集，已明确排除为本轮源码通过证据；最终源码必须先正常fresh编译，再运行受影响测试。多路径AccessDenied尚未证明为IDE文件锁，按实际sandbox审批/文件占用证据处理，不停用户进程、不禁构建功能绕过。

原验收首轮结论：不通过，已一次退回开发七项缺口，主控确认继续同合同返修。包括20秒续期更新expiresAt触发Hub重建/清聊天及过期快照；三类媒体仅查人员Active而非Presence；独立页面离开仍常驻连接且持久媒体未结束；正式account未确认、MES仍被要求实现细分权限、无效HTTP示例；Production共享拓扑/真实PG库名/统一Schema未落实；跨进程初始化无统一锁；最终源码无fresh编译。返修后按缺口增量复核，不重复已关闭平台媒体矩阵，不把功能缺失改写为环境未测。

下列记录保留历史排查与当时证据；其“未实测/待用户确认”字样由用户后续已确认的基线覆盖。独立部署新行为另按V4合同验收，不能因为旧平台已通过而省略本轮关键差异验证。

用户最新确认“上述测试验证没有问题”：本轮HTTPS手机联调及停止观看后残留MEDIA_NOT_ACCEPTED提示按用户现场确认收口，不再重复派发验证。该确认不扩大为Android本机屏幕采集、所有网络/TURN或独立宿主前端现场通过。接下来用户询问独立部署操作；现有宿主/FixedDemo可运行，协作专用裁剪前端尚未实现，操作与正式发布缺口同步记录在[前端发布说明](../../../src/frontend/DEPLOYMENT.md)，本轮未擅自部署或启动独立前端架构开发。

停止观看后MEDIA_NOT_ACCEPTED缺陷已完成代码修复及增量独立验收，无返工项。延迟测试确认：共享终态等待停止回报时，旧signal/Ready回包仍能通过原全局上下文检查；语音共存时旧screen bind成功还可写回。修复按活动能力与session/call ID归属过滤回包、终止时清理匹配Screen错误，并在bind写回前核对当前能力组合，保留有效语音及真实当前错误。开发30/30定向测试、app/node类型及store ESLint通过；验收未复跑测试，核对稳定文件hash与成功/失败两条旧绑定路径，代码层PASS。见[增量独立验收](../../evidence/PF06-late-media-result-acceptance-20260914.json)（SHA256：52FE894899C4F6BB8371C5A4EB4715FD1AA44DD03679094FDC76268ABF8D9D3E）。手机HTTPS真实场景仍未复测，整体PARTIAL仅保留现场确认，不重新排查HTTPS/TURN或扩展Capacitor，不把截图推断为完整停止证明。

HTTPS追加交付：已生成10.13.49.141/localhost专用开发证书、配置.env.lan.local，增加dev:lan:https与前端VS Code HTTPS调试项；临时端口验证TLS、页面、HTTP/WSS代理及私钥403通过，node配置类型检查通过，未改业务或Capacitor。当前Windows用户CA信任安装被自动审批拒绝，需要用户明确授权，手机也尚未安装信任，因此不能声称手机可信HTTPS已可用。见[HTTPS证据](../../evidence/PF06-lan-https-20260914.md)及[操作说明](../../agents/局域网调试入口.md)。不重跑既有媒体测试，设备信任安装后再实机验证。

本轮HTTP兼容稳定交接：协作4处请求/协商编号复用createCorrelationId，安全上下文保留randomUUID，缺失时用getRandomValues生成UUID v4并保留原32hex契约；移除共用helper原Math.random后备。采集入口增加MEDIA_SECURE_CONTEXT_REQUIRED中英提示，麦克风与屏幕API独立判断。开发定向4文件66项通过，app/node类型、生产文件ESLint及diff check通过；未重启调试、未改Hub生命周期或PC协商结构。见[开发证据](../../evidence/PF06-lan-media-uuid-20260914.md)。独立验收补齐唯一测试缺口后确认代码层PASS：无randomUUID时Connected→Hub和Disconnected→REST两条发送路径均调用原发送实现；补证仅测试文件，聊天组件31/31，测试文件ESLint及diff check通过。原验收与主控未重复命令；见[最终增量验收](../../evidence/PF06-lan-media-uuid-acceptance-20260914.json)（SHA256：15DB024A5C6D9D6A3452BB1F7F56C1ADE913EB0B60EB438EF6A554CB581B340E）。本轮实现和代码验收收口，不追加代码返工；手机现场消息发送及可信HTTPS下语音/观看仍待实际验证，因此整体PARTIAL。Android Chrome共享本机屏幕能力限制见[LAN说明](../../agents/局域网调试入口.md)。

### 追加：旧目录清理、前端发布边界和移动端反馈

用户本轮要求：移除旧src/samples避免误导；明确前端平台集成与独立部署如何打包；华为手机自带浏览器通过/mobile登录时聊天/语音/共享不可达，截图报MEDIA_SIGNAL_FAILED，并要求PDA/Mobile头部紧凑化，不影响现有PC及聊天/媒体行为。

- 旧src/samples经确认只剩bin/obj/.vs及空目录，已按授权移除；正式宿主和程序集保留。历史evidence路径不追溯修改。
- 前端采用共享业务代码、平台与独立能力两类产物。当前embedded认证不等于已经独立裁剪打包；当前事实和后续入口装配边界见[src/frontend/DEPLOYMENT.md](../../../src/frontend/DEPLOYMENT.md)。本轮先解决移动故障，不混入独立前端架构重构。
- 原开发负责本轮前端兼容修复；主控负责LAN说明及状态文档。最新证据：聊天构造请求前直接调用仅安全上下文支持的crypto.randomUUID，普通LAN HTTP会在网络调用前失败；媒体采集在缺mediaDevices时统一报MEDIA_UNSUPPORTED，未区分HTTP限制与缺少特定API。修复仅涉及协作请求标识及捕获前置判断，不再改Hub重连、媒体协商或生命周期。手机语音需要可信HTTPS，Android Chrome本机屏幕捕获缺API与观看远端画面分别处理。
- PDA/Mobile顶部形态为“会话入口｜联系人/状态｜更多操作”；语音、共享我的屏幕、请求对方共享、会话详情、隐藏会话进入不占消息布局空间的点击浮层，保留原handler/权限/状态/结束控制，支持点击外部与Escape关闭。PC仍保留原操作；不修改已通过的媒体协商核心。
- 仅对本次连接原因和移动菜单/直接受影响PC路径做定向验证；固定演示、原握手和已过媒体矩阵不重跑。手机现场效果尚未确认，旧通过结论仅覆盖原已测版本与设备，不用本次新故障否定原结果，也不提前宣称手机修复完成。

前一轮稳定交接：PDA/Mobile头部和More浮层已实现；开发报告布局相关34/34通过，app/node类型检查均exit 0，Hub/authGateway定向18/18通过。嵌入适配导致token可选后的两处类型契约已补明确存在性检查。排查中提出的全命令5秒等待/隐式重连改动及其测试已撤回，因其不能证明华为端连接失败根因且会扩大媒体生命周期影响。原调用语义保持；该轮只交原验收做布局/类型增量检查。当时缺少访问地址；现以本节新增Chrome截图和HTTP兼容断点继续处理，不能用布局通过关闭通信问题。

移动布局/类型增量独立验收通过，见[验收证据](../../evidence/PF06-mobile-header-menu-acceptance-20260914.json)；原五动作handler、权限、用户手势、结束控制及PC内联均保留。主控已完成唯一文件Prettier格式化及check（exit 0），关闭非功能格式提示，见[格式收口](../../evidence/PF06-mobile-format-close-20260914.md)。未停止用户进程或复跑业务测试。当前通信修复单独交付；手机可信HTTPS下的语音/观看现场效果尚未验证，不能改为已完成。

### 追加范围：局域网调试与MES独立嵌入（2026-09-14）

用户明确要求新增两项工作，覆盖此前外部MES后置边界：本机调试可被同LAN电脑访问；`src/backend/src/Hosts/IndustrialPlatform.Collaboration.EmbeddedHost`承载同一套聊天与媒体，能独立部署接入现有MES。原samples宿主按用户要求迁入正式Hosts目录，提供同名专用解决方案，后续能力遵循[宿主规范](../../../src/backend/src/Hosts/README.md)。MES技术栈为.NET Framework 4.5.2及.NET6，保留当前登录和获取用户接口；以独立进程/HTTP适配集成，不把现代.NET程序集直接引用旧MES，不复制业务。真实接线由用户后续完善，现场接入状态不得写完成。

数据库本轮仅PostgreSQL与SQLite；用户明确SQL Server在下一阶段，当前保留现有配置/provider/迁移扩展边界，不新增SQL Server实现或支持声明。

- 主控负责LAN前端启动模式、同源HTTP/SignalR代理、HTTPS证书配置入口及文档；后端继续原VS调试与唯一云配置，不自动重启、改防火墙或信任库。LAN访问说明见[局域网调试入口](../../agents/局域网调试入口.md)。代码/隔离代理验证与第二电脑实际访问、受信HTTPS媒体效果分别记录。
- 原开发已完成EmbeddedHost缺口评估，继续完成下述仓库适配合同；同一生产文件只有一个写入者，不动LAN配置。MES现场集成不能以静态用户/会话映射样例代替。
- 原PF06功能已通过结论保留；本段追加范围尚在处理，不重新打开旧媒体缺陷或把用户明确后置项派回开发。PF06A已有独立主控，本任务不修改其工作包和工程。

#### MES仓库适配执行合同

用户最新收敛：本轮先提供可理解、可替换的适配入口和示例，真实MES接口由用户后续完善，不再要求本轮提供接口资料或把现场接线列为返工阻断。关键代码使用易懂中文注释，解释入口用途、替换位置、输入来源、字段映射、返回要求与失败行为；配套最短接线说明，避免只有空接口却不知道如何接入。

用户进一步明确：演示可使用固定A/B/C用户，默认当前用户A，A能搜索B/C并发起聊天，SQLite承载聊天业务数据。提供明确的演示启动入口；真实身份接口尚未提供，不继续扩展真实认证验收。原平台聊天、屏幕共享、语音已实测通过，必须保持；演示配置和身份替身限于独立宿主的显式演示模式，不改变UnifiedHost或共享媒体业务。验证只保留新鲜构建、A搜索B/C并建立聊天/发送/读取的最小链路，以及受影响的原入口隔离检查，不重复已通过的媒体矩阵。

已交付：正式宿主目录内提供同名csproj/slnx与中文接线README，总solution及测试项目引用已迁移。FixedDemo固定当前A并提供B/C目录，仅在显式模式映射演示会话端点，数据使用SQLite；独立真实HTTP探针已完成搜索B/C、建会话、发送及读回1条消息，见[固定用户演示证据](../../evidence/PF06-fixed-demo-20260914.json)。主控在最后源码交付后执行总solution Release构建，exit 0、0警告0错误。原嵌入入口验收见[接口验收证据](../../evidence/PF06-embedded-mes-entry-20260914.json)；最后会话修复20/20记录按当时路径保留。当前演示证据是API/SQLite，不扩大为浏览器、真实MES、跨网或媒体现场复测；原平台媒体核心未因目录和演示调整修改。

最终独立验收PASS：核对固定运行证据及脚本hash，16/16静态隔离检查通过；复用已有API/SQLite与主控Release结果，没有再次运行探针、构建或旧媒体矩阵。见[最终增量验收](../../evidence/PF06-fixed-demo-acceptance-20260914.json)。当前开发与验收停止执行，不再围绕后置真实身份接口追加测试。保留用户已经实测通过的聊天、语音、屏幕共享基线；本轮未提交或部署。

接线后的用户可见结果：用户继续登录现有MES，经受信握手进入独立部署的协作前端；页面恢复HttpOnly会话并显示该用户有权使用的聊天/媒体入口。对方来自MES用户目录。会话失效或撤权后停止受保护访问，不能复用上一用户的身份。媒体继续沿用当前已经通过的邀请和连接实现。此处是适配合同，不表示当前已连接真实MES。

实现边界：复用协作宿主的challenge/assertion/exchange、浏览器绑定和session端点；显式配置运行依赖，为MES当前登录、用户目录、权限提供服务端可替换适配入口。目录标识、租户、session、安全版本须贯通REST、Hub及媒体权限，不能仅在前端展示用户名称。真实接入模式未配置时失败关闭；固定用户仅作为明确演示数据，不充当真实MES身份。不得虚构用户Token、无条件授予权限或在真实接入模式从浏览器传入的subject直接建立可信身份。

前端增加明确embedded认证模式与会话恢复/过期退出流程，使用宿主Cookie及权限结果，不伪造平台access/refresh token绕过守卫；普通http/mock登录保留。复用现有页面和媒体组件，不新增会议页面或重写WebRTC。部署模板说明实际包含的依赖、前端资源、API/Hub/握手路径和HTTPS/嵌入策略；独立进程不等于旧MES必须升级运行时。

开发唯一写入范围为sample、嵌入所需前端auth/API/路由和必要Collaboration注册接口、对应定向测试与sample说明。主控LAN代码已稳定，仅可在保留LAN行为前提下扩展runtimeConfig；禁止修改vite配置、LAN文档及PF06A文件。原验收在稳定交接后核验新增会话/权限边界与嵌入入口一次，不重复已过媒体矩阵或环境检查。

完成分层：仓库参考部署/适配接口及嵌入入口必须有实际代码和定向验证，关键替换点有中文注释与接线示例；真实MES当前用户、目录字段、凭据与部署地址由用户后续接线，不能报现场部署完成，也不因上游未提供反复扩建或重测。当前数据库只接既有PostgreSQL/SQLite，保留provider与迁移扩展点，SQL Server不在本轮实现或验证。

用户最新反馈“测试均没有问题”，据此关闭本次浮窗缩放后拖动尺寸重置、Edge发起Chrome接听残留错误提示两项真实UI待验。保留此前用户确认的双向发起、语音与屏幕共享共存及实际画面/声音可用结论。当前没有已知未修复生产缺陷，不重新派发功能开发或复跑这些路径。以下历史按时间保留，旧黑屏、残留错误、全部真实媒体MISSING等描述不再代表当前状态。

用户最新收尾安排：媒体停止已实测一端断开后另一端也断开，本轮按该现场行为通过，不重复取证；不虚构分轨计数、具体断连类型和测量时限。网络/质量移至后续云Docker部署；异常移至Electron/Capacitor；终端完成后验证PC/PDA/Mobile。这是明确延期安排，不能再作为当前开发返工阻断。

| 剩余事项 | 对应TASK/场景 | 尚缺的收尾结果 |
| --- | --- | --- |
| 媒体停止 | 008/009/011；停止场景 | 用户确认一端断开、另一端随之断开：现场行为PASS，本轮关闭。原规格分轨/时限等测量未凭空补填，后续需要对应支持声明时随目标场景记录 |
| 目标网络及TURN | 007/011；A09 | 用户明确延期：后续项目云Docker部署后验证跨设备direct、TURN、受限TCP/TLS及RelayOnly；当前不探测或改变云环境 |
| 画质、语音质量与稳定性 | 001/008/009/011；A08/V09 | 用户明确延期：同一次云部署验收集中做Screego同条件对照、30分钟组合与弱网；不新建独立PoC |
| 多端、权限及异常并发 | 003/004/005/010/011；异常场景 | 用户明确延期：Electron/Capacitor就绪后验证多端抢接/忙线、权限、迟到操作、故障恢复；已有代码回归保留 |
| 三端支持 | 006/011；A10 | 用户明确延期：终端完成后验证PC/PDA/Mobile实际能力及聊天/观看交互，不提前宣称原生容器支持 |
| 数据库与最终集成 | 002/011；A11及仓库流程 | 保留实际部署拓扑、新旧库升级/UTC支持边界及最终交付门禁；现用云PG修复和已过回归不重做。未收到本轮提交推送指令，保留WIP；本轮已完成协作复盘与状态文档收束 |

本次只维护文档和协作复盘，未运行测试、改生产代码、探测环境或启动新任务。TASK-011状态为本轮功能验收通过、用户指定后续专项待验；保留原验收项目作为部署/终端阶段输入，不宣称全网络/全端通过，不把明确延期反复派回开发。复盘见[减少联调返工](../../agents/PF06开发注意事项-减少联调返工.md)，通用经验已回写唯一协作约定。

2026-09-14文档职责纠正：通用协作约定已移出聊天/媒体专属行为，分别归位PF05/PF06业务规格；通用文件仅保留协作、页面共性与工程预防方法。下一阶段按路线为PF-06A，PF-06延期项不再阻挡启动；当前可进入001受控核验，设备/发布G06A-1未闭合前不将相关生产整包判为就绪。具体见[下一阶段工作包判断](../pending/PF-06A.md)，本次未派遣新任务。

## 当前优先交付：截图反馈与完整媒体操作（2026-09-13）

2026-09-14合并代码验收收口PASS：浮窗resize→drag保持实际宽高；首轮发现的sendMediaReady旧source失败落入无来源fallback问题已修正，过期来源忽略、当前真实错误保留、匹配能力成功清理。稳定WIP四文件按hash记录，原验收只读断言15/15通过；开发报告浮窗1项、原错误3项、mediaReady延迟失败1项及typecheck通过，验收和主控未复跑。真实UI单次浏览器读取失败，未重试环境；原生缩放后拖动手势、Edge→Chrome接通后红提示消失均NOT RUN。保留用户已确认双向发起及语音/共享共存可用结论，不扩张质量/TURN/完整停止通过范围，PF-06整体PARTIAL。见[合并验收证据](../../evidence/PF06-resize-stale-error-20260914.json)。

本次追加已完成开发、合并验收进行中：观看浮窗原生缩放后拖动恢复初始尺寸，根因为CSS resize修改实际DOM尺寸而响应式size未同步。现于开始拖动读取实际边界矩形同步宽高及起点，显式border-box避免边框尺寸漂移；760×480 DOM几何模拟后拖动的1项定向回归与typecheck通过。真实浏览器原生缩放手势尚未复验，不能把几何模拟写成实际手势PASS。语音残留错误提示修正的3项定向自测结果保留；两份稳定交接pf06-resize-drag-handoff.md及pf06-stale-error-cleanup-handoff.md已合并交原独立验收一次增量核验，不重复媒体矩阵，不改环境或模型设置。

用户最新实测更新：语音与屏幕共享可共存，双方均可发起语音或共享，保留实际可用结论，不再重复媒体矩阵。唯一剩余反馈为Edge发起语音时Chrome已接通/显示进行中，但仍残留MEDIA_CONTEXT_STALE红条。原开发本轮仅复现并修正信令异步失败回包的context/revision/negotiation归属和恢复后的过期错误清理；不因Active全局隐藏错误、不屏蔽所有stale、不改变已可用媒体协商。仅2-3项错误时序定向用例+一次typecheck，原验收只读增量。组合的长期稳定性、质量/TURN及所有停止场景不由本次用户口头确认自动扩大为通过。

本轮最终增量代码验收PASS，覆盖首轮NEEDS_FIX：用例已实际投递新revision先到/旧Answer后到，Low取消旧negotiation、对未完成协商rollback并按最新binding继续，保留原PC及严格版本门禁；迟到旧信令进入有界队列后flush丢弃，不处理为新Answer。聊天用例已真实触发空容器scroll后放行消息DOM，非后台首载/显式重新加载到底，后台/更早历史保留阅读锚点。开发定向媒体4/4、聊天1/1及typecheck通过；原验收只读15/15，主控未复跑。见[集中验收证据](../../evidence/PF06-direction-combination-scroll-20260913.json)。用户已确认单独屏幕共享与Chrome→Edge单语音可用；Edge→Chrome、真实组合和停止连续性仍待实测，整包PARTIAL，不把fixture与静态PASS写成真实收听通过。

最新三项反馈合并交付：独立屏幕共享与Chrome→Edge语音由用户实测可用，按具体方向保留PASS；Edge→Chrome语音报MEDIA_CONTEXT_STALE，两种顺序加入第二项媒体均不能连接；每次聊天加载停在消息顶部。原开发一次集中定位发起/接听与Low/High协商角色、双端Bind/Signal版本顺序、增量媒体协商及初次消息DOM就绪后的落底。仅必要的媒体定向用例与一个加载滚动回归，稳定后一次typecheck；后台刷新/更早历史的阅读锚点保留，不改云环境或重复独立共享验收。前轮终态历史ID与KeepAlive版本回退2项代码修复保留，不能把它们视为已覆盖新增方向/组合缺陷。整包PARTIAL；具体交接由pf06-direction-combination-scroll-handoff.md记录。

最新用户实测：屏幕共享已可用，按用户确认保留PASS，不重复其实际画面验证；这不扩张为TURN、质量基准、所有停止组合均通过。语音仍失败，截图明确MEDIA_CONTEXT_STALE并显示等待本地设备授权。当前只排语音上下文版本：重点复现结束屏幕后加载终态历史再发起语音时，context事件能力ID匹配与Bind有效能力ID不一致的可能，以及迟到KeepAlive版本回退；未复现不追加补丁。原开发定点测试+一次typecheck，原验收仅只读增量，不覆盖模型或改环境。

本轮定点交付已完成：Bind业务失败/rejection按待连接能力显示具体错误并保留另一项Active；Accepted/Connecting按有效绑定与PC状态触发启动，同key单飞、有效绑定跳过、失败key抑制事件重试。Host与屏幕浮窗显示对应能力错误。原验收只读增量PASS，无生产返修项；见[交付证据](../../evidence/PF06-pre-peer-fix-20260913.json)。开发typecheck通过；测试命令未限制住文件范围，实际133文件/994项通过，超出用户要求，属于验证范围偏差；验收和主控均未复跑。真实双端画面/语音未验，整包仍PARTIAL，不把已知卡死路径修复等同于用户现场唯一根因已关闭。

用户授权本轮定点修复（2026-09-13）：按已确认的绑定失败仍Connecting及有效状态启动缺口修改，控制验证消耗。原开发唯一写入：集中处理Bind业务失败/rejection、将错误归属待连接能力并在浮窗可见、有效Accepted/Connecting状态按绑定/能力/代次条件启动，防止重复Bind事件循环；已有另一能力Active不受影响。不改ICE/授权/后端/云配置/布局，不重启调试。仅最小相关前端用例与一次typecheck，原验收只读核增量，不重复开发命令，主控不第三次测试。实际现场根因尚缺Bind错误证据，不把这些代码修正写成真实黑屏已关闭。

最新真实dump（21:25，用户提供，覆盖此前根因猜测）：Edge共享方1次getDisplayMedia请求及成功回调、仅视频轨道；Chrome观看方无采集记录；双方PeerConnections为空，无协商/ICE/RTP证据。原开发只读追踪确认Bind失败/rejection未充分反映到能力状态，以及Accepted事件触发边界是候选解释，尚不能确定现场唯一原因。空记录也可能来自构造失败或连接已移除，不能断言从未创建，更不能据此继续调TURN/码率。当前未改生产代码、未重跑测试；下一步读取原聊天页store的errorCode、hasBinding、hasPeerConnection等脱敏状态，必要时对照BindMedia返回。证据见[真实导出摘要](../../evidence/PF06-real-dump-20260913.json)。参照Screego仍是原生捕获→PeerConnection→协商/ICE→实时播放的媒体路线，不能用参照选型替代实际链路证据。

本节按用户最新三张截图纠正此前独立浏览器窗口的误解，覆盖历史窗口方案及邀请成功结论。当前共享方浏览器已显示捕获，观看方仍黑屏0:00；实际传输未通过。既有列表紧邻排列与终态清理的验证保留，不机械重做。总体PARTIAL。

本轮最新反馈（Edge与Chrome分别登录不同账号）：标签页选择列表空白；观看浮窗仍为0:00转圈且带视频进度条；语音是否实际生效不明确。此前浮窗代码验收不代表这三项通过，用户当前可登录，不沿用自动化登录超时解释其媒体故障。新增要求：原生getDisplayMedia显式允许当前标签页，保持屏幕/窗口选项；观看视频移除controls，作为实时流展示；Accepted/Connecting/错误不得一概显示进行中，只有实际播放及对应媒体就绪才显示成功状态。

2026-09-13主控已用一次不联网的定向时序用例复现代码断点：media.context.changed(revision2)先于本端Bind(revision1)完成时事件被丢弃，随后Offer仍为revision1，被拒MEDIA_CONTEXT_STALE。该证据是协议回归，不是用户当前黑屏唯一根因或真实媒体通过。原开发同批修复绑定期间事件与信令处理、单次绑定重入和当前三项UI问题；清理须按context/代次，有界且不放宽服务端授权/版本校验。稳定后一次受影响回归/build，原验收只核增量；当前整包仍PARTIAL。

最新验证收口：原验证最终代码/模拟时序PASS；提前revision、提前Offer、能力改变补Bind、旧生命周期不阻挡新Bind共4/4，静态14/14。两组返修（错误状态隔离、Bind生命周期/组合）已关闭，开发最终前端构建exit0，不重复旧回归。浏览器会话接口不可用，真实标签页选择、双向声音/实时画面与分项停止仍MISSING；上述历史FAIL已被本结论覆盖，整包PARTIAL。见PF06-media-connection-20260913.json。

### 固定交互与边界

最新媒体路径定位（2026-09-13）：用户新截图显示发送端已捕获、双方仍连接中/等待远端画面。详细规格§1.3要求默认All，但修复前Options/Binding DTO默认RelayOnly；唯一私有调试配置未覆盖策略，且没有ICE地址及TURN签名配置。该组合无法提供relay候选，影响屏幕和语音。主控已在唯一私有配置明确All，原开发已对齐代码默认值、补显式RelayOnly缺配置的媒体级错误，以及All+TURN短期凭据；原验收提出的DTO策略大小写规范化已修复。最终Release与provider/Bind定向7/7通过，原验收只读增量PASS（未重复开发测试），见PF06-ice-policy-20260913.json。显式RelayOnly不得回退直连；不新增公共STUN或改云Docker。IOptions已有进程是否加载须单列，磁盘修改不等于用户VS实例已生效。

独立原生传输探针仅使用隔离Edge→Chrome、合成画布和直接SDP交换，不经过应用登录/Hub：RelayOnly+空ICE得到0候选；All+空ICE得到候选，但该自动化探针未建连、未收帧，不能作为All已修复真实媒体的证据。本轮不继续重复环境/登录探测。真实动态画面、双向收听、分项停止与TURN仍未通过；下一步以加载修复后的用户现有实例实测为准。

- 仅观看共享画面采用页内悬浮窗：参考用户提供的控制台浮窗，标题栏可拖动，边角可调整尺寸，支持全屏及退出全屏恢复位置/尺寸；适配视口，画面保持比例。聊天在后方仍可操作，不开新浏览器页面，不弹出语音控制窗口。
- 悬浮窗只负责视频展示与观看控制，底层仍由唯一媒体store管理。收起/隐藏展示不结束语音或重建连接，保留重新打开观看入口；“退出观看”明确结束本次屏幕能力，保留语音。全屏退出只还原窗口，不停止媒体。
- 语音呼叫、接听、静音、挂断与共享邀请/停止留在聊天及既有常驻条。音频由唯一全局媒体Host播放，不能因视频窗挂载/隐藏而重复播放或中断。
- 点击“共享我的屏幕”即在该用户手势中调用原生getDisplayMedia，让共享方选择整个屏幕/桌面、应用窗口或浏览器标签页（具体可选来源由浏览器决定），随后发邀请；接收方接受前仅持有本地捕获，不向其发送。对方接受后自动接入已选轨道，不再要求点第二次“选择共享内容”。
- “请求对方共享”由被请求方点击同意时打开原生选择器；完成选择及本次授权后再开始共享。不能后台自动选择来源，不能绕过对方同意。不采集系统声音或摄像头。
- 取消选择、邀请失败/拒绝/撤回/超时必须释放本轮预采集轨道；代次检查防止迟到媒体复活，新增失败不结束已有另一项能力。
- 本地捕获、媒体连接中、对端实际播放必须区分。不能因Accepted或Hub成功就显示共享已生效；黑屏故障按sender实际轨道→协商/ICE→收发帧计数→video播放定位首个断点，避免仅修文字和窗口。
- 左侧列表紧邻排列及终态隐藏保持。结束/拒绝/取消/过期DTO可留历史，但不显示Ended活动条与结束按钮；一项结束仅移除该项，两项结束无残留。

### 双方操作必须闭合

| 场景 | A端动作与可见结果 | B端动作与可见结果 | 判定依据 |
| --- | --- | --- | --- |
| A共享自己的屏幕 | 点击即原生选源，选择后邀请并等待；B接受后无需再次选源 | 接受后在可拖动、缩放、全屏的页内浮窗看到A所选动态画面 | 本地有效轨道、sender接入、远端解码/播放与增长帧计数，非仅Hub成功 |
| A请求B共享 | 发请求并等待；B同意选源后浮窗观看 | 点击同意即选本机来源，完成后共享 | 方向正确，所选源变化在A可见 |
| 语音与共享组合 | 语音始终在聊天控制，加入共享不新开页面 | 同样保持独立语音控制和唯一音频播放 | 双向音频及屏幕均实际传输；挂断语音保留屏幕，停止屏幕保留语音 |
| 浮窗操作 | 拖动、边角缩放、全屏/退出全屏、收起再打开 | 各自控制本端观看展示 | 不拉伸画面、不重建PC、不影响语音或后台聊天；退出观看明确只结束屏幕 |
| 取消与结束 | 取消选源/邀请失败释放预采集；分项结束释放对应sender/轨道/播放 | 对端同项停止，另一项保留 | 先证明实际媒体增长，再证明对应媒体停止；窗口消失/黑屏不能独立作为证据 |

### 一次交付与验收责任

1. 原开发仍为唯一生产代码写入者，主控维护本节；原验收在稳定交付前不并行跑浏览器/测试、不改源码。续派不覆盖model/thinking、不新建分支/任务。
2. 先一次性列出布局、终态显示、用户激活/采集、绑定/协商、远端播放各已证实断点，按依赖连续修复。允许最小相关调整，不新增状态框架、媒体引擎、缓存或鉴权旁路。
3. 开发从真实页面按钮起步验证一条固定双端路径：现有测试身份登录→语音接听/授权/播放→加入屏幕/选择来源/播放→分别结束→另一方向请求共享→结束并刷新；布局及终态在同次页面检查。只在失败断点取脱敏错误，不预填Accepted/Binding、伪造Hub事件或直接调用store绕过UI来冒充真实业务通过。
4. 固定既有云Docker调试配置、既有测试账号/测试对端；不得创建账号、改密码/角色或邀请普通用户。不要把凭据写入脚本/证据；主控持有用户凭据时由主控执行需要该凭据的一次受控页面步骤，不让开发反复搜索其他会话或仓库凭据。用户VS/现有Host/Vite不重启、不另起占用5041的实例；确有缺失才按协议启动，必须登记清理责任。
5. 只对变化的媒体UI/store/窗口边界做一次相关类型检查、回归和页面复验；纯布局不造镜像单测，后端未变不跑后端全套。新增失败有证据再修，已通过不机械重跑。独立验收集中核对稳定增量及开发证据；主控不第三次复跑。
6. 合成音视频可验证传输、独立结束与统计，但必须标记合成；真实屏幕选择器、麦克风/播放、Screego质量、TURN及双机停止证据各自区分。若工具不能操作原生选择器，集中报告这个具体人工步骤，不用反复启动浏览器/重新登录去替代，也不把媒体已完成写成PASS。
7. 交付逐行对应上表：实现位置、已通过步骤、证据、未测/阻塞。未实际看到画面/验证音频不得返回“语音/屏幕可用”。主控可报告代码完成但真实验收未完成，不能把子任务“代码测试PASS”改写为用户功能通过。

最新执行结果（2026-09-13，覆盖下方历史调试结论）：使用用户提供的既有测试账号与唯一云 Docker 配置，已修复媒体开关未开启、失败提示不可见，以及 Pending/Ringing 无 mediaContext 时“结束全部”未撤回服务端邀请的问题。加载冻结补丁后，三个真实页面入口连续完成邀请→撤回：InviteVoiceCall/EndVoiceCall、两组 InviteScreenShare/EndScreenShare 均 ok=true，页面无错误；未创建账号、改密或赋权。独立定点审查 PASS，本轮停止重复验证。证据见 [三入口运行记录](../../evidence/PF06-three-entry-20260913.json)。本次仅证明邀请/撤回，不代表接听后的声音、画面、真实媒体停止、TURN 或质量已通过；整体继续 PARTIAL。测试浏览器已关闭，本地 Host/Vite 保留。

当前优先事项（用户最新纠偏）：用户反馈语音通话、共享我的屏幕、请求对方共享均不可用；本轮先定位并修复三个入口，不先展开整包验收。用户确认默认连接现有云Docker、已有测试身份且已停止IDE调试；凭据仅用于授权登录，不记录于文档/脚本。原开发只确认一次本地实例并启动必要缺失服务，使用唯一Development.local.json云配置与固定测试身份，禁止重复环境探测、替换SQLite/Redis、新建账号、改密或默认授管理员。先检查实际Hub返回、媒体开关及失败提示，再定点修复；调试开关可在唯一Development配置落实，既定认证/授权/RelayOnly不降低。项目Playwright结果先保存脱敏文件再输出摘要。开发最短链自测完成后交原验收一次集中核验，无新证据不重跑/返修，全部续派不覆盖模型强度。下方为此前阶段记录，不能以历史“未复现产品缺陷”否定本次用户报告。

当前调试验收：项目Playwright独立Edge已成功启动，开发取得一次真实登录（约3.7秒）、精确聊天链接导航及login/auth/me/negotiate/conversations等200证据，暂未复现产品登录或路由缺陷。此前Codex扩展故障不再阻塞该路线。主控13:18正式只读确认用户5041/5173监听及health=200；未重启调试或改配置。云库PF06-001/002及语音表索引已落地。两名本轮临时测试账号已于13:33经正式API完成撤权/可恢复删除，并由独立验收核对收尾PASS。普通文本双端、成功邀请、媒体运行时开关、TURN与真实停止/质量仍MISSING。最后一次只读探针没有持久化结果、退出码不可恢复，不计通过，也不重复运行。下一轮先落实第二最小权限测试身份及实际媒体条件，不再默认授管理员角色、自动改密或反复创建账号。

本轮续派（2026-09-13）：用户明确同意使用项目Playwright启动独立Edge（msedge）测试实例，复用现有5173/5041服务，并由原开发与独立验收任务继续执行。保留任务当前模型/强度，通信不传model/thinking，不新建任务/分支，不再排障Codex扩展。开发先核实最小启动与真实登录/聊天/邀请链，使用隔离测试账号与会话；仅修可复现缺陷，稳定增量及hash交原验收。验收准备阶段不并跑测试/写evidence，收到交付后只核新增证据和受影响路径。已通过代码门禁与索引修复不重复执行；两轮无新证据则分类报告阻塞。合成媒体结果单列，不替代真实双机、TURN、停止及质量证据；保留用户调试、云服务及全部WIP。

云库启动修复（2026-09-13，已关闭）：65/66字节索引名被PostgreSQL截到63字节，旧Guard按完整名误判并回滚PF06-002。已按PG原生name类型规范化，保留SQLite精确匹配及真正缺失拒绝；DDL/迁移ID/种子/账本未改。开发定向2/2，主控最终Release 0警告/错误；PG隔离schema实际002、重复执行、多字节名称、真正缺索引均通过并回滚。随后用户正常调试启动已补跑业务库002，表与索引只读确认存在，不需清库。本项不等于完整PostgreSQL升级矩阵或真实媒体验收通过。

历史隔离SQLite结果：登录200，聊天/权限/SignalR曾因安全存储返回503；当时临时进程已停止。该历史依赖阻塞不代表当前用户云环境不可达，后续真实登录与聊天接口200证据以上方当前状态为准。55项内容hash对应当时媒体代码交付，不覆盖随后新增的公共Guard修复。旧Debug页面观察不替代Release门禁；详细时间线保留于evidence，不重复已有测试。

剩余实测条件：第二个同租户最小聊天/媒体权限测试身份、明确的测试素材与接收终端、双机入口及证书、运行时媒体开关和TURN路径尚未落实。早期实际媒体派发曾因素材/接收端/可信边界不明被自动审批拒绝，不能扩大为禁止已授权的独立Playwright页面检查；也不能用该页面检查代替真实媒体授权与证据。两台PC、TURN/Screego对照、分轨停止及30分钟质量仍需实际验证。

最新收口依据：[PF06 evidence](../../evidence/PF-06.md)“独立验收：三项集成修复集中复核”。Bind锁内校验、High首Offer守卫、ICE分项失败语义均PASS，此前EndAll结论保留。内容边界55项文件hash全部匹配，独立定向测试后端1/1、前端8/8通过；主控最终Release编译退出码0、0警告/错误，未重复全套测试。当前代码返修闭合，不再将已关闭问题重复送验。真实双机/TURN/Screego/逐MID与SSRC停止证明/30分钟质量仍MISSING，下一步只补真实环境与媒体证据，不能宣布PF06总体完成。

历史第二轮复核及范围约束（后续关闭结论以上述最新收口为准）：[PF06 evidence](../../evidence/PF-06.md)第二轮返修复核。该次门禁通过但功能结论仍FAIL；已确认修复结果保留，剩余四项为媒体Context有界串行控制、EndAll单事务原子性、已有PeerConnection加入第二能力时更新槽方向与协商、ICE主动10秒超时及状态迁移。等待超时不等于队列有界，会话锁不等于数据库事务；须用行为测试验证。原开发继续定点修复，冻结新快照并记录HEAD/WIP清单与hash后交验，验收按缺陷及影响复验，不并行修改源码/共用测试产物。统一约定没有“两轮返修自动停止”的上限，只有同一问题两轮没有新证据时由主控诊断原因。真实双机/TURN/分轨停止/质量缺证另列MISSING，不能以门禁通过代替。HTTP分布式后台投递仍属后续支持，实际使用Http依赖组合时PF06不启用/不声明支持，不静默改全局配置或影响普通聊天；不新增任务/改模型。

## 目标与唯一输入

在已有一对一聊天中完成屏幕双向邀请与独立语音：分别接受/授权/开始/结束，可同时运行。同一对选定终端复用一个WebRTC连接；结束voice保留screen，停止screen保留voice，静音不等于挂断。文字聊天继续工作。屏幕默认无系统声音，语音仅麦克风无摄像头。

按以下顺序读取，不需要从历史ChatGPT会话重新提炼设计：

1. 自身仓库入口及[执行者协议](../../agents/EXECUTOR.md)，核对工作线和职责；读取[开发协作与页面统一约定](../../agents/开发协作与页面统一约定.md)，沿用已确认的Skill、黄金页面、聊天布局、协作和配置边界。
2. 本包全部；[实施09 V3.0](../../implementation/09-Industrial%20Platform%20RemoteAssistance开发实施方案.md)§1～12固定范围与依赖。
3. [PF06执行规格V3.0](../../implementation/details/PF06-数据接口与页面规格.md)§1真实入口/配置、§2最终字段、§3事务/状态、§4全部15个Hub方法、§5共用媒体、§6页面、§7迁移/权限/审计。
4. 实施09 §13对应九字段TASK，规格§8测试步骤和§9任务映射、§10门禁。
5. [共同派遣标准](../../implementation/STANDARD-派遣前详细设计与页面验收.md)，按聊天增量落实，不新增管理表或独立房间页面。
6. [PF06开发注意事项：减少联调返工](../../agents/PF06开发注意事项-减少联调返工.md)：PF05实际教训、真实链前移、端到端契约、UTC/版本、影响范围复验和稳定交付。直接套入本包已有步骤与证据，不新增审批层或逐TASK派遣。

V3是唯一执行版本；旧单屏信令协议、单表后再补语音设计、旧§6语音附录均已替换。**语音字段、忙线、共享连接与分项停止的设计不再留给执行模型自行决定。**

## 本次执行授权与登记

用户2026-09-12明确开始实施09全部工作，并指定直接使用当前工作树/当前分支，不新建分支。本次授权优先于默认独立worktree规定，不改变其他PF。当前主控为01a08a28-6cbb-7ac1-9dc8-ccd6ed5acfcb。用户指出执行偏移后，主控纠正此前“001/009真实环境全部通过才允许写生产代码”的过度限制。既定数据/协议/页面设计无待决策项，开发现在从002开始连续完成聊天内业务集成；001/009已有样本作为参考，未具备的真实媒体/网络对照随实现补验，限制最终验收/发布声明，不阻塞可实现的代码。实际发现架构矛盾再定点报告，不以未测推定路线失败。

| 项 | 当前值/填写要求 |
| --- | --- |
| 开发执行任务/模型 | 01a09510-275e-7a82-99c2-07222783234c（local）；gpt-5.6-luna max（极高），唯一生产源码写入者 |
| 独立验收任务/模型 | 01a0950f-7ccc-71d2-9965-7efe2e7ee66e（local）；gpt-5.6-sol high（高），稳定交付后只读验收，不并写源码 |
| 允许通信对象 | 主控01a08a28-6cbb-7ac1-9dc8-ccd6ed5acfcb；开发01a09510-275e-7a82-99c2-07222783234c；验收01a0950f-7ccc-71d2-9965-7efe2e7ee66e；常规交接直接完成，不联系无关任务，已有任务续派不传model/thinking |
| 工作树绝对路径/分支/基线 | D:\Code\Industrial Platform\IndustrialPlatform；develop；0a77597f94e38873b67565e0b477e120e72a57f8；启动核对tracked/untracked无改动。本次后续主控登记文档不计开发WIP；不开分支/worktree |
| 唯一源码写入者 | 本次PF06开发任务；主控仅维护文档/最终集成，验收不改生产源码，稳定交验期间暂停开发写入 |
| 批准运行范围 | 本地源码实现、自测、浏览器与合成媒体PoC、隔离测试数据库/账号、必要缺失本地服务；先复用并核实实际实例。不重启IDE调试、不变更远端Docker/防火墙或普通数据，不擅自部署；工具强制审批照规则处理 |
| 实验终端 | 两台实际PC、OS、Edge/Chrome具体版本、显示尺寸/缩放；移动端另列 |
| HTTPS与聊天 | 批准测试URL、受信证书、两个同租户可聊天账号；第三账号/第二端用于越权和忙线测试 |
| 测试数据库 | PostgreSQL与SQLite独立测试库/临时文件位置，Shared/PerService预期目标；不能迁移普通调试数据 |
| 配置与实际实例 | 私有值仅src/backend/appsettings.Development.local.json，同目录example只作模板；填实际进程/端口/加载版本与脱敏有效值、允许的隔离测试入口，不复制多Host配置或擅自重启调试 |
| TURN/网络 | 实际服务器/版本、secret引用位置、UDP/TCP/TLS与RelayOnly可测路径；不写秘密值 |
| Screego基准 | 固定未修改版本/来源、同浏览器/设备/素材/路径；不集成生产身份 |
| PoC结论 | 同机三槽/共用连接自动化样本部分完成，真实G06-1/3仍MISSING；不再作为禁止002开发的条件，不代表正式Hub/双机通过 |
| 实际契约核对 | G06-2在派遣时复核规格§1.2的现有签名/版本漂移；当前已完成只读识别，未作运行验证 |

缺少TURN只阻塞该路径实际验证，缺少真实设备不得用fake media替代验收。执行者把准确缺项和可继续的工作报主控；主控负责环境与范围决策，执行者不静默更换产品路线或把MISSING写成PASS。

## 连续执行与交接

当前顺序：**002 → 003 → 004 → 005 → 006 → 007 → 008 → 010 → 011**；001/009早期探索已有部分结果，缺少的真实验证贯穿集成补齐。008形成屏幕开发自测基线；环境缺项不阻塞010语音实现，011仍不得在必需真实证据缺失时宣称全功能PASS。停止扩建独立PoC，直接复用正式CollaborationHub、媒体控制器和现有聊天。

| 阶段 | 执行模型应完成 | 交接检查 |
| --- | --- | --- |
| 001＋009 | 填环境清单，Screego屏幕对照、原生双向语音、两种组合顺序、碰撞/拒绝/分项停止、30分钟样本 | 实际版本/路径/误差/结果齐全；真实缺项记入最终验收，不阻塞已定业务开发、不再扩建同机演示 |
| 002 | 两领域表＋语音slot、完整字段/枚举/约束、幂等事务、两provider追加迁移、重启终结 | 不先只建屏幕字段；slot跨Caller/Callee竞争有效；旧PF05迁移不改 |
| 003＋004 | 屏幕用例及最终公共Hub契约、可信端点、同一Context/PC、初始三槽与Perfect Negotiation、逐能力清理 | 无SignalScreenShare旧兼容层；无广播媒体；迟到捕获和旧信令不能复活 |
| 005 | 三权限及seed、双方当前身份评估、PF06 Outbox分支/稳定审计事实 | 不拿A的HttpContext冒充B，不把原Outbox当现成审计；真实适配前开关关闭 |
| 006＋007 | 唯一App媒体host、聊天邀请/观看与常驻条、配置/短期TURN凭据/网络验证 | 收起video不销毁audio/PC；不stop聊天Hub；无秘密入库/日志 |
| 008 | 开发完成A01～A11屏幕稳定自验，不额外派独立验收 | 只报告屏幕验收，不能宣布PF06整体完成 |
| 010 | 独立语音呼叫/忙线/多端、授权/静音/挂断/播放恢复，复用已有最终连接 | V01～V07；两个加入顺序与独立结束；不新增会议服务 |
| 011 | 稳定快照独立验收A/V完整矩阵、真实质量/停止、权限/并发/重启/审计故障 | 范围内缺陷修复后定点复验；最终支持声明以真实证据为准 |

每个TASK详细步骤、输入、允许范围、测试编号、交付和提交策略见实施09 §13，执行者不得将这些卡改写成空泛TODO。内部连续执行，不逐卡派遣、审批或提交。005真实适配后开发即跑屏幕最小链，006验实际页面入口，010接通即验语音与两种组合；011才在稳定整包上首次独立验收，验收者不改生产源码。详细落实见规格§6.2、7.3、8.4。

## 文件边界与已知易错点

后端允许：现有Collaboration各层RemoteAssistance子目录、新增DTO/实体/用例/存储；现有CollaborationHub/DI/服务级迁移/初始化、公开Identity适配、现有Outbox/Audit适配增量；所属测试。

跨模块明确允许的必要增量：Identity PermissionCatalog、BootstrapSeedCatalog及相关测试；通过现有公开IPermissionEvaluator/IAuditService接入。不能改Identity/SystemData/Messaging的私有仓储或直接查其他模块表。当前seed读取为1.5.0，实施前复核并选择未占用的升级号，不覆盖其他PF增量。

前端允许：App.vue唯一认证媒体host、api/collaborationHub.ts、新media store/composable、现有CollaborationChat/QuickDrawer、新ScreenSharePanel/VoiceCallBar、locale、相关单元/组件/E2E测试及批准配置。若权限目录由生成流程维护，按现有流程更新生成文件，不能手改伪造源头。

禁止默认扩展：新程序集/信令服务/会议首页/房间管理、JoinTicket/ParticipantGrant/EngineLease、多引擎、第三人、同时双屏、远控、摄像头/系统声音、录音录像、外部MES/原生包。不要为此范围添加通用媒体平台框架。

特别按规格执行：

- 初次仅Low端add三个transceiver，High映射远端槽，避免双方各建一套重复m-line。
- Accept后立即Bind并确认，授权弹窗等待仍计入Setup60秒；任何await后核验generation。
- End不靠expectedVersion拦住清理；EndAll按具体旧ID幂等，不能误结束新通话。
- 语音slot两行与Call/Outbox在同一事务；失败先rollback再查冲突，不能仅用进程锁保证跨角色忙线。
- 状态事件可到用户组，SDP/ICE只能到本次选定ConnectionId；认证sid/ver/exp按双方检查。
- 系统Audit actor为空且原事件时刻不变；中央审计失败不等待它才能停媒体。
- 通用媒体连接失败会影响两种能力；只新增设备/授权失败应保留原能力，不虚构完全网络隔离。

主控维护蓝图/TODO/CURRENT/本工作包；执行者只更新PF06证据和所属代码，不修改全局状态。保留全部既有WIP，不reset/clean/stash覆盖；不干扰用户IDE/调试服务、安装组件、改防火墙或处理真实数据，除非派遣登记已有相应授权。

## 验收和证据格式

停止必须先有真实活动源及增长计数，再触发结束。正常≤5s，静默失联为最后有效确认请求发出＋30s且最多5s调度容差，截止后再观察10s。按音频/视频MID/SSRC分别观测；页面黑屏、关闭页面、WSS断开、票据或TURN凭据到期、PC总字节均不能独立判定通过。旧媒体止住的同时，保留能力必须连续。

源码变更后从仓库根fresh Release build，迭代按影响定向测试，最终开发交付运行solution Release --no-build门禁；前端typecheck/lint/test:unit/build及必要媒体E2E，完整命令在规格§8.4。构建锁/环境失败单列，释放本次资源后重新build，不能用旧二进制宣称新代码通过。

唯一阶段证据为未来实际创建的 `docs/evidence/PF-06.md`，包含：

```text
状态：开发自测 / 待独立验收 / 验收结论（不能混写）
交付标识：工作树、分支、HEAD、既有WIP、本轮变更manifest/hash及计算方法、稳定交接时间
环境：终端/浏览器/数据库/网络路径/Screego版本、非秘密配置
TASK进度：001..011各自状态与证据链接
测试矩阵：A01..A11、V01..V12的操作、期望、实际、PASS/FAIL/MISSING
命令：实际命令、退出码、失败/跳过数；真实设备人工步骤另列
停止/质量：时间基准、测量误差、分轨计数、持续观察、对照指标
剩余风险/缺项：受影响能力×设备×网络，不能泛化成全端PASS
范围外发现：精确文件/接口、复现证据、建议，交主控决定
```

详细合成统计可放 `docs/evidence/pf06/<delivery-id>/` 并链接；不提交真实屏幕/声音、SDP/ICE/IP、原始抓包或构建缓存。无实测时不创建充满PASS的占位报告。

## 历史执行指令（已停止，不再派发）

以下为本次整包指令；创建任务记录与通信对象在上方登记。

> 你执行PF-06工作包。先读docs/agents/开发协作与页面统一约定.md最新版本，完整读取并遵守$industrial-platform-task-collaboration；核对本任务工具、写入范围与实际权限，常规已授权操作直接执行，强制审批不绕过。读取本包指定的V3.0实施与执行规格，在登记的唯一工作线内从002继续003→004→005→006→007→008→010→011，001/009未验项随正式集成补齐。先核对真实输入与现有WIP；技术验证不达标时报告准确证据，不擅自改路线/指标。按最终三表、15个Hub方法、一个MediaContext/PeerConnection、两个独立业务状态实现，保持原聊天。逐卡完成所列步骤与测试，真实停止和质量不能由mock或关页面代验。正常内部步骤不重复请求确认；出现跨PF文件冲突、缺环境或设计与真实行为冲突才交主控定点处理。完成后保留未提交修改，更新唯一PF06 evidence，交稳定快照给登记的独立验收任务，双方直接汇总返修与影响范围复验，不让用户传话；已有任务通信不传model/thinking，主控只处理真实阻塞与最终结论。不要暂存、提交、推送、变更全局状态或自行部署。

历史说明：当时恢复实际业务实现，覆盖更早的“不进入生产002”。该派发现已停止；当前授权仅以本文顶部 V5 范围为准。

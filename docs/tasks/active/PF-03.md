# PF-03 ReferenceData 完整实施工作包

## PF 编号

PF-03

## 状态与授权

已派遣，待执行任务完成环境预检后进入开发中。2026-09-04 用户已明确授权完成 PF-03 全部内容，期间不询问用户，自行实现、验证并注意边界。此授权替代 V2.7 中“尚未整体授权”的状态占位，不代表业务已经实现或依赖已经验收。

## 负责人

PF-03 专用 Codex 执行任务，模型 gpt-5.6-sol ultra（用户指定）；负责完整实现、修复、自验证和最终交付，不只负责编码。用户要求的 1.5× 倍速无法通过 create_thread 单独设置；主机当前默认 service_tier=priority，未将其等同于已验证的倍速。

## 目标

完成实施 06 V2.7 全部七模块、共享治理、七个 PC 页面及适用自动化/真实链路验收；内部任务连续推进，不逐步交回总控。

## 实施方案入口

- docs/implementation/06-Industrial Platform ReferenceData Service开发实施方案.md（V2.7）
- docs/blueprint/32-Industrial Platform Service Host与内部模块边界.md
- docs/blueprint/33-Industrial Platform SystemData数据库编排与环境引导.md
- docs/agents/EXECUTOR.md

先读第 1～7、23～28 节，再按当前切片读领域章节及对应 API、页面、验收条目，不重建设计或另开规划审批。若旧角色入口只指 PF-02，本工作包才是本执行任务的 PF 范围；不得误执行 PF-02。

## 内部任务序列

001 薄基础 → 002 Dictionary → 003 Parameter → 004 DynamicProperty → 005 UnitOfMeasure → 006 Metadata → 007 CodingRule → 008 StateMachine → 009 共享缓存/Outbox/可观测性 → 010 契约/E2E/联合验收。

每一模块按后端、API、页面、测试纵向完成后继续；这些编号不是独立派遣、审批、提交或验收门。七模块不得为推进速度退回仅骨架、假数据或只有配置页。

## PF 工作线与 Worktree

- 主仓库（只读上下文）：D:/Code/Industrial Platform/IndustrialPlatform
- 专用执行目录：D:/Code/Industrial Platform/IndustrialPlatform-worktrees/pf-03
- 分支：work/pf-03-reference-data
- 已创建基线：17e4821aa487a451364cde7df73b6804822999c3
- 所有代码、安装、构建、测试、日志和 evidence 写入均在专用工作树或任务自有临时目录。新任务在应用中归属父项目不代表可在主仓库编码；显式设置命令 workdir。
- 不再创建工作树/分支，不自行合并/rebase/cherry-pick/pull 主工作线。基线已包含 V2.7；CLAUDE.md、DSH.md 的主工作区本地修改不复制或覆盖。
- PF-02 仍有独立收尾事项；本次用户已明确启动 PF-03，允许在隔离工作树推进，不把 PF-02 状态文本当成重复请示理由。相关实际契约/环境若未就绪，记录具体证据，继续不受影响部分，不能伪造联合验收通过。

## 必须使用的技能与页面边界

先完整读取 $industrial-platform-task-collaboration 与用户指定的 $industrial-platform-management-page；遵守仓库 AGENTS.md。模型以用户本次明确指定的 gpt-5.6-sol ultra 为准，不套用协作技能的默认 luna 模型，不另建用户任务。

页面以当前 IdentityUsersPage.vue 为黄金样板；先核对 AppQueryPanel、AppDataTable.vue/ts、AppFormDrawer、OrganizationsAdminPage 与相应测试/语言资源。统一查询、表格、操作列“更多”、结构化业务弹窗和主题/权限/中英文；不机械复制用户管理业务功能。先判断是否需要行选择或主从布局，普通列表不增加无用选择；需要选择/主从时必须读取技能 references/table-selection.md，并以当前共享组件真实契约为准，禁止假想 props 或页面 CSS/DOM 补丁。

保留业务操作与表格工具的职责分区，保留居中弹窗/右抽屉偏好；纯确认仍用确认框。状态机首版节点表/转换表，不引入画布或通用工作流编辑器。

## 允许修改范围

仅在专用工作树内：
- src/backend/src/Services/ReferenceData/** 与 tests/ReferenceData/**，含新 Contracts 项目、迁移/种子、模块领域/用例/API/契约、缓存、服务级 Outbox、初始化/readiness。
- src/backend/IndustrialPlatform.slnx，现有 Gateway/UnifiedHost 的 ReferenceData 引用、路由、依赖注入和配置注册最小增量。
- Identity 的 ReferenceData 权限目录、种子和必要 policy/registry 接线及对应测试；不改登录、用户/角色或 SystemData 业务语义。
- src/frontend/src/api/referenceData/**、pages/pc/referenceData/**、ReferenceData 专属组件/局部状态与测试；现有 router/routes.ts、navigation、permissions、locales/localization 的必要注册。
- 2026-09-05 总控核对后补充前端入口窄例外：src/frontend/src/app/createIndustrialApp.ts 仅增加 ReferenceData API factory/registry 的 import，并在现有 HTTP 分支使用同一 client 调用 registerReferenceDataApi(createReferenceDataApi(client))；须先实现对应 ReferenceData API 模块，不另建 HttpClient，不改认证刷新、Mock 分支、客户端配置或其他服务行为。src/frontend/src/router/routeNames.ts 仅新增与 routes.ts 七页注册一致的 ReferenceData 命名路由常量，不改既有名称或注册过滤逻辑。完成后运行 typecheck、API 注册/路由/权限相关回归，并在真实页面就绪后验证路由冒烟；不得把只有注册声明当成页面或 API 完成。
- 共享管理组件仅允许新页面真实需要且无替代的最小兼容扩展，并增加集中回归测试；不得改既有页面行为或复制第二套基础组件。
- tests/IntegrationTests/** 内 ReferenceData 契约/真实基础设施/入口测试，相关架构与权限一致性测试、前端组件/单元/E2E 与任务自有测试配置。
- 2026-09-04 总控核对后补充测试窄例外：仅允许修改 tests/SystemData/IndustrialPlatform.SystemData.Tests/Application_DatabaseOperationRunnerTests.cs 中 RunOnceAsync_ServiceOwnedReferenceData_UsesRealInitializerAndReadiness 及其必要测试 helper。该例须用 DevelopmentTopology with { SharedSqliteFile = dbPath }，让 Runner、registration、plan/apply 和 readiness 使用同一隔离临时目标；CreateOperationService 可增加可选 topology 参数，默认仍为 DevelopmentTopology，不影响其他测试。禁止修改共享 DevelopmentTopology 常量、SystemData 生产代码、放宽 ReferenceData 实际目标校验或降低成功/readiness 断言；测试通过情况回写 PF-03 evidence，随后连续执行。
- 本工作树的实施 06 执行记录/必要实际契约回写、docs/evidence/PF-03.md 及 README。不得借回写扩大设计边界。

## 禁止修改范围

- 主工作树代码/配置、其他工作线及其工作包/evidence；AGENTS.md、CLAUDE.md、DSH.md、全局 CURRENT.md。
- PF-02 业务功能、Identity 非上述必要接线部分、MasterData/OperationalData/WorkOrder 等生产实现，PF-04 Audit/File/Notification。
- 七套 Schema/迁移账本/Outbox/Inbox/连接；没有真实入站消费者不建 Inbox。
- 基础 Entity 生命周期重构、业务实例 EAV、通用 SetStatus、业务状态/库存事务接管、物料专属换算、脚本/公式/审批/低代码运行引擎。
- 不重启/停止 VS、VS Code、用户现有服务或远端 Docker；不自动部署、迁移/清理用户数据库，不复制或输出私有连接串/密钥。

## 验证、持续执行与交付

先检查实际工具、写权限、网络/审批策略、项目依赖和基线。依赖安装、缺失的任务自有本地服务启动、自测直接执行；使用任务自有端口和可验证隔离数据库/Schema/测试租户，先复用现有环境的只读能力。真实用户库只读，写验证只能使用可证明隔离且获权限的目标。

源码变化后必须 fresh Release build，再运行 Release tests；不得用旧 --no-build 结果替代新编译。后端最终命令：
- dotnet build src/backend/IndustrialPlatform.slnx --configuration Release
- dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build

前端按 package.json 执行 lint/typecheck/unit/build/e2e；覆盖七页、两种查询模式、中英文、权限/冲突、响应式、键盘、更多菜单、弹窗偏好及需要的选择/主从行为。依据 V2.7 验证 PostgreSQL/Redis/RabbitMQ、Shared/PerService、Gateway/UnifiedHost、降级、固定版本历史、单位换算及状态机业务失败不推进的测试夹具。用真实页面截图验证布局，不以 Mock 或测试早退宣称外部链路通过。

自行完成修复并继续下一步；不在中间问用户是否继续/是否测试。完成整个 PF 才正常交付，不能以单个模块完成结束整个工作。真实阻塞一次性附证据与可行方案发总控，继续可做部分；安全审批走实际系统机制，不绕过。

整个 PF 使用 docs/evidence/PF-03.md。交付包含状态、主要范围、命令/退出码/通过失败跳过数、真实与模拟证据区分、截图/报告和剩余风险。默认不暂存、不提交、不推送、不合并，保留工作树交总控最终 Release 编译和集成；不要因到达内部任务末尾自行提交。

## 允许通信的任务

总控：01a05d86-26f0-7be2-890a-e74974cd6c31（PF-03 ReferenceData 设计优化）。仅向总控发送必要交接/真实阻塞；不要联系或唤醒 PF-02、提交任务或创建新的用户任务。可在本执行任务内部用有边界的只读/测试子智能体辅助，不并行修改共享文件。

先完整读取并遵守 $industrial-platform-task-collaboration。期间不要询问我：本轮已定范围内的指定任务通信、自测验收及必要的缺失本地服务启动直接执行，不再重复征求同意；先复用现有环境，不干扰 VS、VS Code 和已有服务。系统强制审批依实际权限处理，不绕过；真正阻塞集中交总控，只汇报交接、结论和需处理的问题。

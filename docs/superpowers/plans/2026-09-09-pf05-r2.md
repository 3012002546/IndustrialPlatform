# PF05 R2 Consistency and Runtime Verification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the R2 A03–A18 behavior and consistency gaps with executable tests and minimal production fixes, while recording exact verified and unverified matrix entries.

**Architecture:** Reuse the existing Collaboration application, SQL repository, Identity/SystemData internal HTTP adapters, SignalR Hub, export worker, and schema migration runners. Add only missing behavior ports or state checks where a failing test identifies a real gap; keep each subsystem's persistence and service boundary explicit. Use isolated SQLite/in-memory HTTP fixtures for deterministic unit behavior, and separately probe real Redis/PostgreSQL/RabbitMQ availability without claiming skipped environments as passed.

**Tech Stack:** .NET 10, ASP.NET Core, xUnit, SqlSugar, SQLite, SignalR, Redis backplane adapters, `HttpMessageHandler` test doubles, existing migration runners, Vue/TypeScript only when an R2 regression reaches the frontend contract.

**Spec:** `docs/tasks/active/PF-05-R1.md` R2 section; `docs/implementation/details/PF05-前置契约与合规执行补充.md` §§2–6; `docs/implementation/08-Industrial Platform Collaboration开发实施方案.md` §§9–18.

## Global Constraints

- Continue in the existing `D:\Code\Industrial Platform\IndustrialPlatform\IndustrialPlatform` `develop` working tree; do not create a task, branch, worktree, or replacement implementation.
- R2 only; do not start R3 browser/page/2C4G acceptance work.
- Do not modify `docs/evidence/PF-05-acceptance.md`, controller status, or independent-acceptance evidence.
- Do not stage, commit, push, reset, or discard existing user changes; preserve unrelated dirty work.
- Every new behavior must have a failing test, a minimal implementation or a documented precise environment limitation, and a passing targeted test before full release verification.
- Concurrent operations must be tested against shared isolated persistence and must prove no sequence/epoch/approval/claim hole, not merely count returned tasks.
- External Redis/PostgreSQL/RabbitMQ and multi-process behavior must be recorded as passed only after a real probe; skipped tests remain skipped with their exact reason.
- After any backend source change, run `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release`, then `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` before reporting completion.
- Update only `docs/evidence/PF-05.md` for development evidence, append a new R2 section, and include a complete tracked/untracked source manifest plus exact test counts and remaining matrix entries.

---

### Task 1: Freeze the R2 matrix and current baseline

**Files:**
- Read: `docs/tasks/active/PF-05-R1.md`
- Read: `docs/implementation/details/PF05-前置契约与合规执行补充.md`
- Read: `docs/implementation/08-Industrial Platform Collaboration开发实施方案.md`
- Inspect: `tests/Collaboration/`, `tests/Identity/`, `tests/SystemData/`, `tests/IntegrationTests/`, `src/backend/src/Services/Collaboration/`
- Create: `docs/superpowers/plans/2026-09-09-pf05-r2.md`

**Interfaces:**
- Consumes: current R1 dirty working tree, existing test projects, R2 A03–A18 requirements.
- Produces: a verified matrix mapping each A03–A18 item to existing tests, missing tests, implementation files, and exact runtime prerequisites.

- [x] **Step 1: Read the R2 requirements and list each A03–A18 behavior.**

Record the behavior names and required evidence before editing code: two-instance nonce; SystemData internal HTTP positive/negative; 20 concurrent session creation; 50 concurrent message sends with no sequence holes after transaction failure; 410/history, hide, tombstone races; real Hub/Redis/backplane; multi-Case file hold/reference release; export worker claim/takeover/deadline; persistent budget across instances; Identity/SystemData/Collaboration migration first-run/idempotency/drift/Shared/PerService/SQLite.

- [x] **Step 2: Run the existing targeted test inventory.**

Run:

```powershell
dotnet test tests/Collaboration/IndustrialPlatform.Collaboration.Tests/IndustrialPlatform.Collaboration.Tests.csproj --configuration Release --no-restore --list-tests
dotnet test tests/Identity/IndustrialPlatform.Identity.Tests/IndustrialPlatform.Identity.Tests.csproj --configuration Release --no-restore --list-tests
dotnet test tests/SystemData/IndustrialPlatform.SystemData.Tests/IndustrialPlatform.SystemData.Tests.csproj --configuration Release --no-restore --list-tests
```

Expected: test names identify which matrix rows already have executable coverage; do not infer missing runtime coverage from file names alone.

- [x] **Step 3: Record the baseline without changing code.**

Run the existing Release build and full backend test, capture per-project totals, skipped external tests, current processes/ports relevant to Redis/PostgreSQL/RabbitMQ, and the current `git status --short --untracked-files=all` manifest. Use this baseline to distinguish a new failure from the R1 dirty snapshot.

---

### Task 2: Persistent nonce and SystemData internal HTTP contract matrix

**Files:**
- Inspect/Modify: `src/backend/src/Services/Identity/IndustrialPlatform.Identity.Api/Controllers/CollaborationDirectoryController.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/HttpIdentityDirectory.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/HttpSystemDataAuditPort.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/HttpSystemDataFilePort.cs`
- Inspect/Modify: `src/backend/src/Services/Identity/IndustrialPlatform.Identity.Infrastructure/Security/TrustedServiceCallNonceStore.cs`
- Inspect/Modify: `src/backend/src/Services/SystemData/IndustrialPlatform.SystemData.Infrastructure/Security/SystemDataTrustedServiceCallNonceStore.cs`
- Test: `tests/Identity/IndustrialPlatform.Identity.Tests/Api_CollaborationDirectoryEndpointTests.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Infrastructure_HttpIdentityDirectoryTests.cs`
- Test: `tests/SystemData/IndustrialPlatform.SystemData.Tests/Infrastructure_HttpIdentityDirectoryClientAuthorizationTests.cs`

**Interfaces:**
- Consumes: fixed internal paths, RS256 service-call validator, per-service nonce stores, actor/tenant/session/security-version claims.
- Produces: positive and negative HTTP tests proving path/action/method/body-hash/audience/nonce replay/tenant/actor rejection and stable 401/403/409/503 errors.

- [x] **Step 1: Write failing positive and negative nonce tests for two independent store instances.**

Use two store objects over the same isolated SQLite/PostgreSQL test target. Assert the first valid nonce is accepted, the second use returns the contract replay error, different body hash/action/request binding is rejected, and a failed downstream operation does not consume a nonce that was never accepted.

- [x] **Step 2: Run the tests and confirm the missing behavior.**

Run the Identity directory, Collaboration HTTP adapter, and SystemData internal HTTP test filters. Expected: any missing row fails with its exact response code/body; existing passing rows stay green.

- [x] **Step 3: Implement the smallest boundary fix.**

Keep the receiver responsible for nonce persistence and verification. Do not accept a caller-selected action/path or browser Bearer as a service credential. Preserve the fixed internal routes and existing `TrustedServiceCallValidator` contract.

- [x] **Step 4: Run the focused matrix.**

Run the three project filters again and record passed/failed/skipped counts and exact unverified real-service prerequisites.

---

### Task 3: Concurrent session/message sequence and transaction failure behavior

**Files:**
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Application/CollaborationService.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/SqlCollaborationRepository.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/CollaborationTables.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Infrastructure_SqlMessageSequenceConcurrencyTests.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Application_ComplianceCommandExecutionTests.cs`

**Interfaces:**
- Consumes: conversation/member authorization, database transaction ports, message sequence uniqueness and existing `SendMessage` application contract.
- Produces: executable tests proving 20 concurrent session creations and 50 concurrent sends do not produce duplicates or committed sequence holes; injected transaction failure leaves no phantom message.

- [x] **Step 1: Write failing 20-session and 50-send tests.**

Run 20 creates against the same conversation/tenant and 50 sends against one conversation using separate repository instances where the provider supports it. Assert unique IDs, one committed sequence per successful message, no duplicate sequence, and the next successful send follows the highest committed sequence. Inject a transaction failure after sequence allocation and assert the failed operation leaves no visible row and does not advance the committed sequence.

- [x] **Step 2: Run the concurrency tests and identify provider limits.**

Expected: deterministic pass for supported SQLite semantics or an exact provider limitation; do not call an in-memory list a transaction test.

- [x] **Step 3: Fix the transaction/sequence root cause if a test fails.**

Sequence allocation, message insert, attachment link, and audit/outbox writes must share the same transaction boundary. On rollback, the sequence allocation must roll back or use a documented gapless committed-sequence mechanism already accepted by the contract.

- [x] **Step 4: Re-run the 20/50 matrix and record committed row and sequence counts.**

Include successful operations, rejected operations, committed row count, sequence min/max, duplicate count, and sequence-hole count.

---

### Task 4: History 410, hide, tombstone, and concurrent visibility races

**Files:**
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Application/CollaborationService.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Domain/CollaborationDomain.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/SqlCollaborationRepository.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Infrastructure_SqlMessageSequenceConcurrencyTests.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/MessageTextRulesTests.cs`

**Interfaces:**
- Consumes: cursor/page contracts, message visibility state, hidden/restored/retracted/tombstone semantics, optimistic version checks.
- Produces: executable tests showing history after the retained boundary returns 410, hide/recover/retract operations are idempotent and version-safe, and concurrent readers/writers never expose a retracted or tombstoned body.

- [x] **Step 1: Add failing tests for each race.**

Cover: cursor older than the retained history boundary returns stable 410; two concurrent hides produce one state transition and one safe retry; hide versus send/read never returns hidden content; retract/tombstone versus read returns the allowed tombstone projection only; restore after a tombstone cannot resurrect unauthorized body text.

- [x] **Step 2: Run the focused history/race tests.**

Expected: failures identify whether the issue is repository atomicity, service authorization, or projection filtering.

- [x] **Step 3: Apply the smallest state/version fix.**

Use the existing optimistic version and transaction contracts; do not add a second visibility state machine or change public error names without a matching frozen contract.

- [x] **Step 4: Re-run and record state transition counts and projections.**

---

### Task 5: SignalR Hub and Redis backplane runtime boundary

**Files:**
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Api/Hubs/CollaborationHub.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/PresenceRegistry.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/DependencyInjection.cs`
- Inspect/Modify: `src/backend/src/Hosts/IndustrialPlatform.UnifiedHost/Program.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/PresenceLeaseProjectionTests.cs`
- Test: `tests/IntegrationTests/IndustrialPlatform.IntegrationTests/SystemData_ControlPlaneDependenciesE2ETests.cs`

**Interfaces:**
- Consumes: SignalR hub method contracts, tenant/member authorization, Redis presence lease and backplane configuration.
- Produces: in-process Hub authorization/negative tests plus a real Redis/backplane probe when the repository-documented dependency is available; otherwise an exact environment-blocked row.

- [x] **Step 1: Write failing Hub tests.**

Exercise connection/user context, wrong tenant, non-member, expired/revoked session, message send/read authorization, and presence lease renewal/expiry. Assert no unauthorized group join or cross-tenant event.

- [x] **Step 2: Inspect runtime configuration and probe Redis without changing service lifecycle.**

Read repository-documented connection settings, inspect current process/port state, and run the existing integration filter. Do not stop or restart user services or mutate a normal debug database.

- [x] **Step 3: Fix only confirmed Hub/backplane defects.**

Keep authorization at invocation time and presence leases bounded; do not treat client group membership as authorization.

- [x] **Step 4: Re-run in-process tests and the real probe if available.**

Record Redis host/port reachability, test count, and if unavailable the exact connection/permission failure.

---

### Task 6: Multi-Case File references and safe release

**Files:**
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/SystemDataFilePort.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/HttpSystemDataFilePort.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Application/CollaborationService.cs`
- Inspect/Modify: `src/backend/src/Services/SystemData/IndustrialPlatform.SystemData.Application/Files/FileService.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Application_ComplianceCommandExecutionTests.cs`
- Test: `tests/SystemData/IndustrialPlatform.SystemData.Tests/Application_FileServiceTests.cs`

**Interfaces:**
- Consumes: File reference/hold ports, current tenant/member actor, File scan/deletion state, CaseNId and idempotency contract.
- Produces: tests proving two Cases can retain one attachment independently, releasing Case A does not release Case B, duplicate registration/release is idempotent, and download rechecks current File/attachment/member state.

- [x] **Step 1: Add failing multi-Case tests.**

Register the same FileNId for Case A and Case B, release A, assert B still blocks deletion, release B, then assert cleanup can proceed only after current scan/deletion/reference checks. Repeat registration/release with the same request ID and with a conflicting request hash.

- [x] **Step 2: Run Collaboration and SystemData file filters.**

Expected: failures identify whether Case scope is lost at the Collaboration port or SystemData reference layer.

- [x] **Step 3: Implement case-scoped idempotent references and reauthorization.**

Use unique `(tenant, case, file, request)` keys and preserve existing ordinary File references. Every content open must recheck tenant, member/message projection, retract/disposition state, and current File scan/deletion state.

- [x] **Step 4: Re-run and record both Case lifecycles and deletion result codes.**

---

### Task 7: Export worker claim, takeover, deadline, and artifact gating

**Files:**
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/CollaborationExportWorker.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/SqlCollaborationRepository.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/CollaborationTables.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Application_ComplianceCommandExecutionTests.cs`

**Interfaces:**
- Consumes: persistent export request/approval snapshot, worker lease/claim fields, File artifact scan status, audit and budget ports.
- Produces: tests for two workers claiming once, expired claim takeover, run deadline failure, approval/snapshot version invalidation, and non-Clean artifact refusal.

- [x] **Step 1: Add failing worker tests with two worker instances.**

Seed one queued request, let two workers attempt claim concurrently, assert one owner and one result. Advance the claim deadline, let a second worker take over, and assert stale owner completion is rejected. Advance run deadline and assert failure with no false Completed state.

- [x] **Step 2: Run the worker tests and capture state transitions.**

Expected: one claim, one valid completion/failure, no duplicate artifact or audit event.

- [x] **Step 3: Fix claim/deadline/version transaction boundaries.**

Claim, takeover, approval consumption, artifact status gate, and final state transition must be conditional updates on request/version/lease owner. Never write a downloadable artifact before the File gate is Clean.

- [x] **Step 4: Re-run with retries and record each worker's claim/lease/result.**

---

### Task 8: Persistent export/budget accounting across instances

**Files:**
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Application/CollaborationPorts.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/SqlCollaborationRepository.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/CollaborationTables.cs`
- Test: `tests/Collaboration/IndustrialPlatform.Collaboration.Tests/Application_ComplianceCommandExecutionTests.cs`

**Interfaces:**
- Consumes: tenant/day budget contract, persistent usage ledger, export preparation snapshot and request idempotency.
- Produces: two-instance concurrent budget tests proving atomic reservation, exact 201-item boundary behavior, retry idempotency, and no in-memory-only allowance.

- [x] **Step 1: Add failing concurrent budget tests.**

Run two repository/service instances concurrently against the same tenant/day budget. Assert exactly the allowed number of reservations succeeds, the next reservation returns the frozen budget error, the same request ID returns the original result, and a conflicting request hash is rejected.

- [x] **Step 2: Run the tests against shared persistence.**

Expected: any over-allocation or instance-local pass is a failure requiring implementation.

- [x] **Step 3: Implement atomic persistent reservation.**

Use one conditional database update/insert transaction keyed by tenant/day/request. Do not use process-local counters, locks, or cache as the source of truth.

- [x] **Step 4: Re-run and record successful/rejected reservation totals and ledger rows.**

---

### Task 9: Identity, SystemData, and Collaboration migration matrix

**Files:**
- Inspect/Modify: `src/backend/src/Services/Identity/IndustrialPlatform.Identity.Infrastructure/Persistence/Migrations/IdentitySchemaMigrations.cs`
- Inspect/Modify: `src/backend/src/Services/SystemData/IndustrialPlatform.SystemData.Infrastructure/Persistence/Migrations/SystemDataSchemaMigrations.cs`
- Inspect/Modify: `src/backend/src/Services/Collaboration/IndustrialPlatform.Collaboration.Infrastructure/Persistence/CollaborationSchemaMigrations.cs`
- Inspect/Modify: shared migration runner and module initialization files under `src/backend/src/BuildingBlocks/IndustrialPlatform.Infrastructure/` and `src/backend/src/Hosts/IndustrialPlatform.UnifiedHost/`
- Test: `tests/Identity/IndustrialPlatform.Identity.Tests/Infrastructure_IdentityMigrationTests.cs`
- Test: `tests/SystemData/IndustrialPlatform.SystemData.Tests/Infrastructure_Pf04MigrationTests.cs`
- Test: `tests/SystemData/IndustrialPlatform.SystemData.Tests/Infrastructure_SchemaMigrationRunnerTests.cs`
- Test: `tests/UnifiedHost/IndustrialPlatform.UnifiedHost.Tests/UnifiedHostInitializationOrderTests.cs`
- Test: `tests/UnifiedHost/IndustrialPlatform.UnifiedHost.Tests/UnifiedHostModuleCatalogTests.cs`

**Interfaces:**
- Consumes: Shared/PerService/SQLite migration topology, version ledger, drift detection and initialization ordering.
- Produces: first-run, repeated-run, schema drift, Shared, PerService, and SQLite behavior evidence for all three modules.

- [x] **Step 1: Add failing matrix tests for each migration mode.**

For each module, run first initialization, repeat it, alter a known column/index in an isolated target, and assert drift is reported rather than silently accepted. Repeat with Shared and PerService topology plus SQLite target adapter where supported.

- [x] **Step 2: Run migration tests and identify provider/environment gaps.**

Do not run against a user debug database. Record exact target type, database path/connection mode, migration version, and drift result.

- [x] **Step 3: Fix idempotency, drift, or ordering defects.**

Keep module ownership separate: Identity owns Identity tables, SystemData owns SystemData tables, Collaboration owns Collaboration tables. Initialization must be deterministic and must not mark a module ready before its schema/seed ledger is committed.

- [x] **Step 4: Re-run all migration mode tests and capture first/repeat/drift counts.**

---

### Task 10: R2 release verification and development evidence

**Files:**
- Modify: `docs/evidence/PF-05.md`
- Read-only inspect: `docs/evidence/PF-05-acceptance.md`
- Read-only inspect: `git status --short --untracked-files=all`

**Interfaces:**
- Consumes: all R2 targeted test results, fresh Release build/full test, real dependency probes, dirty source snapshot.
- Produces: appended R2 evidence section, exact A03–A18 matrix, complete source manifest and aggregate, and a stable handoff summary without modifying protected acceptance evidence.

- [x] **Step 1: Run the final backend gate after all source changes.**

Run:

```powershell
dotnet build src/backend/IndustrialPlatform.slnx --configuration Release
dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build
```

Record exit codes, per-project passed/failed/skipped totals, and the three external dependency rows separately.

- [x] **Step 2: Re-run only affected frontend checks if frontend files changed.**

Use the frontend package directory and the existing direct binaries when pnpm workspace resolution cannot find the package from the repository root. Do not reformat the full repository.

- [x] **Step 3: Generate the complete dirty manifest.**

Hash every changed tracked and untracked source/test/config file in the R2 scope, explicitly excluding `bin/`, `obj/`, `TestResults/`, caches, logs, and reproducible build output. Compute the aggregate from the exact LF-joined manifest text and verify it twice.

- [x] **Step 4: Append evidence and stop at R2.**

Document each A03–A18 result as `PASS`, `PARTIAL`, or `UNVERIFIED` with the command, actual number, and exact reason. Do not enter R3, do not start browser/page/2C4G acceptance, and do not claim production acceptance where a real dependency remains unverified.

---

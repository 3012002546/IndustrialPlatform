# PF06 基线验证与冗余清理

日期：2026-09-15。当前主控执行第 1、2 阶段；没有实施平台单点登录或独立部署新合同，没有恢复原开发/验收任务。

最新状态：第 1～5 节记录 14:38 的第一轮基线恢复；随后用户授权的小范围源码清理及最新测试结果见第 6 节。当前不再是源码与 HEAD 完全相同的状态。

## 1. 基线和证据边界

- 提交基线：`ae22f6a70a483c10621971110ce5f5d6b1bc60c8`；用户此前确认平台 PC 测试通过并提交代码。
- 本次进入时，生产差异仅有未提交的入口/根组件尝试及其测试。原 `docs/evidence/PF06-embedded-entry-envelope-20260915.md` 保留，它记录旧登录响应解包验证，不是独立通信完整通过证据。
- 不改平台账号、聊天/媒体、数据库/Schema、初始化账本、私有配置、Vite 或云 Docker。没有提交、推送、整树重置或全仓格式化。
- 源码与基线相同以及自动化通过不等于重新完成真实手机、双端声音、屏幕画面、TURN 或云部署验证；历史用户现场结果按原范围保留。本次没有启动/停止用户调试服务，没有新的真实双端媒体通过声明。

## 2. 验证先于清理

在清理前当前工作树执行（工作目录 `src/frontend`）：

```powershell
.\node_modules\.bin\vitest.cmd run --configLoader runner tests/unit/createIndustrialApp.spec.ts tests/unit/routerGuards.spec.ts tests/unit/collaborationHub.spec.ts tests/unit/collaborationRuntime.spec.ts tests/unit/collaborationMediaStore.spec.ts tests/unit/embeddedAuthGateway.spec.ts tests/components/CollaborationChat.spec.ts tests/components/CollaborationMediaHost.spec.ts tests/components/ScreenShareFloatingWindow.spec.ts tests/contract/collaborationApi.spec.ts
```

实际结果：2026-09-15 14:35，退出码 0；10 个文件、129 项测试通过，耗时 5.99 秒。验证覆盖平台入口/路由、聊天组件/API、实时通道、媒体状态与窗口、既有嵌入认证；使用测试环境和模拟依赖，不是现场媒体验收。

## 3. 清理判断和文件

| 对象 | 处置及理由 |
| --- | --- |
| `src/frontend/src/AppEmbedded.vue` | 删除本任务新增未提交文件；按认证模式选择专用根组件偏离新的展示参数合同 |
| `src/frontend/src/pages/embedded/EmbeddedCollaborationPage.vue` | 删除本任务新增未提交包装页；新入口设计留给后续主控，不留下无调用页面 |
| `src/frontend/src/router/embeddedRoutes.ts` | 删除本任务新增未提交路由表；曾有与通用守卫不匹配的缺口，不继续堆补丁 |
| `src/frontend/tests/unit/embeddedRoutes.spec.ts` | 删除只对应已撤除路由表的新增测试；不删除成熟业务测试 |
| `src/frontend/src/app/createIndustrialApp.ts` | 精确撤除本轮接线和附带格式变化，恢复提交内容 |
| `src/frontend/src/router/index.ts` | 精确撤除 embedded 参数/路由选择，恢复提交内容 |
| `src/frontend/tests/unit/createIndustrialApp.spec.ts` | 撤除只验证已撤除模式分支的 1 项测试，保留原有 3 项工厂测试 |
| 已提交的 EmbeddedHandshake、AuthGateway、Hub/媒体、Identity SSO/刷新 | 保留；存在实际调用和旧行为，不能凭相似名称判为冗余。新主控按职责与真实调用决定复用，不在本轮重构 |

没有删除已经通过用户测试的业务实现。这是对本任务已确认错误增量的定点撤除，不是全仓“无冗余”认证。

## 4. 清理后检查

仅重跑实际改变的工厂/路由入口测试，不第三次重复整套媒体矩阵：

```powershell
# 工作目录 src/frontend
.\node_modules\.bin\vitest.cmd run --configLoader runner tests/unit/createIndustrialApp.spec.ts tests/unit/routerGuards.spec.ts
.\node_modules\.bin\vue-tsc.cmd --noEmit --incremental false

# 工作目录 仓库根
git diff --exit-code HEAD -- src/frontend src/backend
```

结果：14:38 入口测试 2 个文件、37 项通过，退出码 0，耗时 5.75 秒；类型检查退出码 0；前后端与提交基线的差异检查退出码 0。测试数减少 1 对应上表移除的错误模式分支专属测试。已撤除的三个组件/路由名称在前端源码和测试中无引用。

没有后端源码变化，不重复编译或运行后端全套测试。类型检查使用 noEmit/incremental false，避免写入与本次检查无关的构建缓存。

## 5. 尚未解决且不得关闭的事项

- 独立部署列表超时、未知在线和媒体不可达：页面清理没有解决它们。Presence 的 Redis 依赖是已发现风险，但不是已完成的现场根因证明。
- `single` 的平台通用 token/ticket 接入、`standalone&account` 的独立交互模式尚未按新合同实施。
- 已提交复杂实现的职责合并和更广泛清理需先有调用证据及对应回归，不借本次清理提前实施第 3、4 阶段。

后续统一入口：[平台单页入口与独立部署交接](../implementation/details/平台单页入口与独立部署-后续主控交接.md)。

## 6. 用户授权的小范围清理（15:06 完成验证）

保留提交基线 `ae22f6a`，本轮改动未提交。生产源码仅 6 个文件，净减少 100 行；测试 3 个文件调整，源码和测试合计净减少 107 行。

| 文件 | 清理内容与保留边界 |
| --- | --- |
| `src/backend/src/Hosts/IndustrialPlatform.Collaboration.EmbeddedHost/EmbeddedAdapters.cs` | 删除无注册/调用的旧映射器、退出生产调用链的旧权限快照及查询方法、权限判断器无用构造参数；修正相关注释。保留当前用户目录及固定协作权限目录和判断条件 |
| `src/backend/src/Hosts/IndustrialPlatform.Collaboration.EmbeddedHost/EmbeddedHandshakeController.cs` | 移除无用目录适配器注入，将无异步操作的身份投影改为同步函数；端点、响应字段及握手服务调用不变 |
| `src/backend/src/Hosts/IndustrialPlatform.Collaboration.EmbeddedHost/FixedDemoDatabaseInitializationHostedService.cs` | 仅删除未使用的旧类名兼容包装；实际 Standalone 初始化器、执行顺序及拓扑检查不变 |
| `src/frontend/src/app/createIndustrialApp.ts` | HTTP 和 SignalR 复用同一个实时读取页级凭据的函数，不缓存凭据快照 |
| `src/frontend/src/config/runtimeConfig.ts` | account 只解析一次；演示白名单、默认 xxA、正式账户校验及平台不消费 account 的规则不变 |
| `src/frontend/vite.config.ts` | 删除已被前置异常覆盖的证书校验分支；证书要求和代理地址不变 |

未修改媒体状态 Store、实时连接清理、页面/路由、认证协议、共享服务、UnifiedHost、数据库配置/Schema/初始化账本。未删除事件 epoch 过滤器及其测试，也未合并握手/续期逻辑；这些不在本轮限定清理内。

测试调整：旧权限适配器测试替换为实际权限判断器的允许/拒绝案例以及用户目录租户隔离、自身过滤测试；握手测试仅更新控制器构造参数；前端补充 account 缺省、空值、重复、空格、非演示账户及平台入口不受影响的案例。

验证结果：

- `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release --nologo -v quiet`：最终退出码 0，0 警告、0 错误。首次构建遇到新增测试变量的 CA1859，已改用具体类型后重新完整构建通过。
- `dotnet test tests/Collaboration/IndustrialPlatform.Collaboration.Tests/IndustrialPlatform.Collaboration.Tests.csproj --configuration Release --no-build --nologo -v minimal`：157 通过、0 失败、0 跳过，15 秒；使用上述新鲜产物。
- 在 `src/frontend` 执行 `node node_modules/vue-tsc/bin/vue-tsc.js --noEmit --incremental false`：退出码 0。
- 在 `src/frontend` 执行下列定向回归：13 个文件、189 项通过，0 失败，7.90 秒。首次因沙箱不能写入 `.vite-temp` 尚未启动测试；获工具放行后使用同一条命令通过，未修改测试配置。

```powershell
node node_modules/vitest/vitest.mjs run tests/unit/runtimeConfig.spec.ts tests/unit/createIndustrialApp.spec.ts tests/unit/routerGuards.spec.ts tests/unit/embeddedAuthGateway.spec.ts tests/contract/authGateway.spec.ts tests/contract/httpClient.spec.ts tests/unit/collaborationHub.spec.ts tests/unit/collaborationRuntime.spec.ts tests/unit/collaborationMediaStore.spec.ts tests/unit/pf06-context-race-probe.spec.ts tests/components/CollaborationChat.spec.ts tests/components/CollaborationMediaHost.spec.ts tests/components/ScreenShareFloatingWindow.spec.ts
```

`git diff --check` 通过。未运行全后端测试、真实双设备媒体或云部署验证；本轮自动化回归不能作为新入口已实现或历史独立部署问题已修复的证据。用户调试进程、云服务和既有文档改动均保留，没有提交或推送。

## 7. 提交前收束

用户在上述清理后反馈：“mobile pc测试均没有问题”。记录为用户 PC、Mobile 实测通过；未提供具体用例明细，不扩展为 TURN、云部署、Native、PDA 或新单点登录入口验收。

最后检查：

- 本轮生产源码仍限定为第 6 节的 6 个文件，另有 3 个测试文件；源码和测试合计新增 70 行、删除 177 行，净减少 107 行。自动化通过后没有再次修改源码。
- 删除的旧映射器、权限快照、旧初始化器类及异步投影名称在相关源码和测试中无残留调用。
- 媒体 Store、实时连接生命周期、路由、后端共享服务及 UnifiedHost 与 HEAD 无差异。
- `git diff --check` 通过；暂存区为空。待提交合计 17 个文件：6 个生产源码、3 个测试、8 个文档（含 3 个新增文档）。
- 私有配置、本地数据库、证书、构建输出、依赖和缓存未进入本轮变更；已检查未跟踪和忽略文件，不暂存 `bin/`、`obj/`、`dist/`、测试输出或本地协作入口。
- 本次仅补记实测与提交卫生结果，没有重复功能测试，没有暂存、提交、推送或重启服务。

当前第 1、2 阶段可收束并准备提交。第 3 阶段平台 single 接入与第 4 阶段 standalone 接入仍由新主控执行，不恢复旧返修链。

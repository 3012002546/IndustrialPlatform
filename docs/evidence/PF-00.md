# PF-00 Identity 工作包验证证据

## 状态

PF-00 当前范围完成(TASK-ID-020/021/022 交付,ID-023 联合验收以本地可验证部分完成;真实 PostgreSQL/Redis 全链路为外部待验收)。按协作规则保留未提交工作树,交由 Codex 最终 Release 编译门禁后统一提交与集成。

## 主要修改范围

### TASK-ID-020 管理 API 契约(后端)
- 权限目录 +7:`identity.user-group.view/create/update/status/assign-member/assign-role`、`identity.user.reset-password`(共 30 项,种子/策略/前端权限目录同步)。
- `UserSummary` 增加 `MustChangePassword` 与角色来源投影(Direct/Group/Effective);用户列表新增 `groupNId`/`roleNId` 过滤。
- 创建用户移除 `InitialPassword`;服务端生成随机临时密码 `CreateUserResult.temporaryPassword` 只在 201 响应一次,新用户强制首次改密;登录名 `admin` 保留拒绝(ID_USER_LOGIN_NAME_RESERVED)。
- 重置密码改为服务端生成随机临时密码 `ResetPasswordResult.temporaryPassword`,独立权限,强制改密并撤销会话。
- 用户组完整管理 API(列表/详情/创建/编辑/启停/成员集/角色集/删除/恢复);错误码 ID_GROUP_NOT_FOUND/DISABLED/ROLE_INVALID。
- 写请求 Idempotency-Key 冲突检测(`identity_write_idempotency` ID-020-01;同键同内容重放幂等确认,同键不同内容 409)。
- 首次登录改密门禁:`AuthUser.MustChangePassword`、改密后清除标记。

### TASK-ID-021 前端管理闭环
- 用户管理页:组/角色过滤、角色来源展示(直接/组继承/有效)、创建无密码表单、一次性临时密码弹窗、独立重置密码权限。
- 新增用户组管理页 `/pc/identity/user-groups`:搜索/创建/编辑/启停/成员/角色/删除/恢复,双版本并发与 409 处理。
- 首次登录改密页 `/change-password` + 路由守卫(MustChangePassword 强制,仅改密+注销);登录页 HTTP 模式 bootstrap 状态诊断(ID_BOOTSTRAP_PENDING)。
- 前端 API 契约更新(createUser/resetPassword 结果、用户组端点、`httpClient.delete` 支持请求体)。

### 恢复流程闭环(前端子代理发现的后端契约缺口修复)
- 后端:`UserSummary`/`UserGroupSummaryDto`/`StoredUser`/`StoredUserGroup` 增加 `IsDeleted` 墓碑标识;`GET /user-groups` 支持 `includeDeleted` 查询。
- 前端:用户/用户组列表按 `isDeleted` 区分删除/恢复按钮;用户组页增加「包含已删除」筛选,恢复请求使用墓碑行当前双版本。

### TASK-ID-022 消除 Mock 依赖
- 真实登录 E2E(`playwright.real.config.ts`,VITE_AUTH_MODE=http)spec 已存在并更新:real-login.spec.ts(不引用 mock.admin)、identity-pages.spec.ts 增加用户组页;生产构建禁 mock 门禁已存在(runtimeConfig);Mock 仅显式 UI/单元测试模式。

### TASK-ID-023 联合验收
- 后端全链路用例经 API 测试覆盖(bootstrap→登录→用户/临时密码→用户组→审计);真实 PG/Redis 全链路 E2E 为外部待验收项(环境未运行)。

## 验证命令与结果

环境限制:SDK workload locator 缺失(`MSBuildEnableWorkloadResolver=false`)、NuGet 审计源不可达(`-p:NuGetAudit=false`)、vstest testhost 与 vitest worker 需进程句柄/spawn 权限。

后端:
- `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` → 0 警告 0 错误。
- Identity 五工程测试全绿:Domain 163/163、Application 137/137、Infrastructure 136/136、Api 56/56、Contract 38/38;SystemData Contract.Tests 16/16。

前端(`src/frontend`):
- `pnpm typecheck` → 0 错误;`pnpm lint` → 0 错误。
- `pnpm test:unit --run` → 52 文件 / 426 用例通过。
- `pnpm build` → 0 错误(仅既有 chunk 体积提示)。

## 剩余风险

- 真实 PostgreSQL/Redis 全链路 E2E(real-login/identity-pages spec)与 SystemData 全 Runner 生产门禁为外部待验收项;E2E 账号按新一次性密码流程由外部编排预置。
- Idempotency-Key 为写路径冲突检测实现(未缓存响应重放内容),生产强制携带策略待 Codex 决策。
- 前端 Mock 基线 E2E(pc/pda/mobile 等 spec)仍使用 mock.admin,属显式 Mock 模式 UI 测试(§29A.7 允许)。

## 范围外发现

- 普通用户首次改密后的登录态刷新(改密成功需重新登录)符合 §29A.4「撤销其他会话」语义,前端已按重新登录处理。

---

## 追加:平台统一运行与部署基础闭环(2026-08-16,develop 主工作树,基线 6cc5d3f)

状态:三个连续任务全部完成,保持未暂存/未提交/未推送,交回 Codex Release 门禁后提交。

- T1 admin 初始化脚本:`--initialize-admin` 新增 `--credential-output <绝对路径>`(首建写 JSON 8 字段,原子写+ACL/0600,stdout 脱敏,重复幂等);`scripts/Initialize-DevelopmentAdmin.ps1`、`deploy/application/bootstrap-admin.sh` + 各自假 dotnet/docker 测试。
- T2 UnifiedHost:`src/backend/src/Hosts/IndustrialPlatform.UnifiedHost` 组合 Identity/SystemData/ReferenceData 模块(模块入口提取,独立 Host 复用);统一认证/授权/健康检查前缀;EventBus 注册幂等化;项目图治理测试登记新项目。
- T3 本地默认 UnifiedHost+Vite;云端 `deploy/application/{Dockerfile,compose.yaml,deploy.sh,README.md}` 单应用容器。
- 验证:后端 Release 构建 0 警告 0 错误;14 测试程序集 1211/1211 通过;前端 lint/typecheck/test:unit(52 文件 430 用例)/build 全通过;PS 脚本测试 29 断言、bash 测试 30 断言 + bash -n 通过(shellcheck 本机不可用);`git diff --check` 干净。

# PF06 局域网调试增量

日期：2026-09-14。主控仅修改前端LAN入口，不改后端监听、私有云配置、证书信任库、防火墙或运行中的VS/Vite。

变更：`dev:lan` / `.env.lan`提供同源`/_backend`入口；LAN监听0.0.0.0:5173，端口占用明确失败。HTTP及WebSocket使用同一代理，服务端目标默认localhost:5041，浏览器API解析使用访问页面Origin。可配置受信HTTPS证书/私钥文件。普通dev与生产构建不启用LAN代理。

验证记录：

- runtimeConfig.spec.ts：14/14通过（明确单文件，包含新增同源地址及不合法路径用例）。最初因Vite配置改成回调而Vitest仍直接merge对象失败，已同步Vitest配置；不是业务断连问题。
- 类型检查：初次发现https显式undefined与exactOptionalPropertyTypes冲突，已改为条件属性；现有node_modules/.tmp缓存写入EPERM，使用同一vue-tsc/tsc针对app/node两个配置检查，tsBuildInfoFile仅改为系统TEMP下本次缓存，最终命令exit0、无类型错误。不安装或更新依赖。
- 隔离Vite与临时HTTP/WS接收端验证：实际HTTP路径及query去前缀正确，WebSocket upgrade及目标Hub路径正确，输出PASS。只验证代理传输，不充当真实身份/聊天/媒体证据；关闭测试服务阶段未退出，已Ctrl-C结束本次自有命令（最终进程exit1），未重跑该检查，未干扰用户实例。
- 第二台LAN电脑访问、Windows防火墙放行、证书信任及HTTPS采集未执行；按文档由实际环境提供，不能将隔离代理PASS写成双机媒体通过。

后续只需在实际LAN入口检查访问地址/受信HTTPS及所需功能，无需再次全套媒体回归。生产MES部署不得使用Vite开发服务器；独立宿主交付单列。

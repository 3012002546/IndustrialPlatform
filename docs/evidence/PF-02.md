# PF-02 SystemData 验收证据

## 状态

PF-02 已知返修项复验通过 / 26项证据状态已收束 / 整包未关闭（2026-09-04，执行 ID `PF02-20260903-功能整改`）：24项为部分，ORG-02/03为隔离通过且真实HTTP待补。第四轮关闭的具体缺陷保持关闭；014完整UI、真正200%、015真实链路及016外部门禁限制保留。017本轮矩阵与状态回写完成；013/PF-02继续active，不归档、不自动启动PF-03。

## 最终26项矩阵独立核对（2026-09-04）

- 接受开发最终矩阵的分层证据与未闭环口径，**完成证据状态收束，不判定整包真实验收通过**。矩阵：`TestResults/pf02-functional-independent-20260904/final-matrix-20260904.md`；独立核对：同目录`final-matrix-independent-review.md`。
- 独立最终fresh Release exit 0、0 warning/error；HEAD仍`65c06d5b4102ec02a1b413ca11b7bd9f43901793`。未重跑开发定向套件；只读TRX核对Identity239、组织80、ReferenceData3通过且无失败。SystemData报告75，但指定目录仅1份TRX、12通过，其余63未由该目录证实；开发已确认75为原控制台汇总且无其他原始产物，仅12列为TRX可核对，不重跑补数。
- 26个编号齐全：DB3/THEME4/INIT5/ORG4/MENU8/UI2；24项部分，ORG-02/03隔离通过/真实HTTP待补。SQLite、内存store/ledger、HTTP+fake store、Mock浏览器各按实际层级记录；真实旧库增补/SSO保存保留、主题首次写/409/ETag、管理员撤权/环境链、默认导入发布回滚runtime、真正200%和外部依赖均未被改成通过。
- 017本轮证据/状态/引用同步完成；014完整视觉、015真实链、016历史真实门禁仍保留各自缺口，013/PF-02保持active。不再次退回已关闭代码缺陷，不为未改变的隔离条件重复同一套测试；条件或有效证据变化后恢复对应验证。
- 本轮未启动服务/浏览器、未改生产/测试源码、未写用户库、未提交推送；任务回写继续省略model/thinking。不归档、不启动PF-03。

## 功能整改独立验收第四轮（2026-09-04）

- 结论：**第三轮4组返修项全部复验通过，未发现新具体缺陷；完整工作包未关闭。** HEAD前后均为 `65c06d5b4102ec02a1b413ca11b7bd9f43901793`。未修改生产/仓库测试源码，未提交推送/写用户数据库，任务回写省略model/thinking。
- 独立fresh Release exit 0、0 warning/error；新鲜构建后initializer/baseline定向7 passed、0 failed/skip。新回归调用真实initializer/seeder/SchemaMigrationRunner，架构SQLite隔离，控制面store/ledger为内存替身、Identity回执为替身；不冒充真实控制面数据库全链事务验证。开发全量1385/3skip、前端771等未重复跑，不算本轮独立数字。
- R3-01：首页独立NId `systemdata.navigation.pc-home`，旧入口/权限和自定义资源保留，旧seed增量及二次revision/commit幂等测试通过。R3-02：合法Dark/Compact/允许集合子集保持Ready且Apply不覆盖。R3-03：1440×900、1024×720树行约88.12px容纳80.12px内容，重叠消除。R3-04：新建/编辑编码只读、编辑Kind锁定、顺序中英文标签及aria-label、菜单管理命名已修复。
- 详细报告及未闭合层级：`TestResults/pf02-functional-independent-20260904/round4-review.md`；TRX在`round4-backend/`；截图在本任务visualizations的`pf02-functional-independent-20260904-r4/`。窄视口不替代真正200%，SQLite/内存组合不替代旧库全部真实迁移与HTTP矩阵；需按原26项汇总现有证据并补可安全执行缺项。
- 自有in-app tab已关闭、viewport恢复，自有4189 fixture PID43056已结束，用户服务未触碰。不再重复退回已关闭缺陷；原开发补最终矩阵交接，总控接收局部通过/整包未关闭状态。

## 功能整改独立验收第三轮（2026-09-04）

- 开发稳定交接后验收，develop / HEAD前后均为 `65c06d5b4102ec02a1b413ca11b7bd9f43901793`。未修改生产或仓库测试源码，未提交/推送/写用户数据库；回写继续省略model/thinking。
- 独立fresh Release exit 0、0 warning/error；SystemData定向32 + Identity定向67测试通过；前端5文件21测试通过。开发报告的全后端1383/3skip、全前端771等未重复运行，不充作本轮独立数字。
- R2-01/02/04/05/06/07具体缺陷关闭：管理员组启用守卫、Validate阶段执行资格检查与fail closed、新增candidate深度、登录/SSO权限输出已修；浏览器证实组织整链折叠/命中祖先保留、菜单拒写后输入与同一NId保留。
- 四组剩余：R3-01 SDM-017仍按同NId跳过旧`systemdata.navigation`，而readiness要求该资源变成pc-home，旧库增量后仍不就绪且ledger阻断重试；R3-02主题readiness强制等于出厂默认，合法改Dark/允许集合后被误判未初始化；R3-03菜单树行约25.99px、换行内容约47.13px，normal实拍相邻标签/停用按钮重叠；R3-04顺序字段缺导航页中英文标签、编辑Kind仍可修改但后端不接受、新建编码仍可手改及标题不一致。
- 报告：`TestResults/pf02-functional-independent-20260904/round3-review.md`，后端TRX：`round3-backend/`；实拍位于本任务visualizations的 `pf02-functional-independent-20260904-r3/`，其中 `04-menu-normal-settled.png` 为布局主证据。截图和DOM几何均复核，未以fixture或视口设置冒充真实写矩阵/200%。
- 两个自有in-app tab关闭且viewport恢复；自有4189 fixture PID28084已结束，用户已有服务未触碰。结论：**第三轮不通过，剩余4组集中回原开发修复**。真实DB升级、外部依赖及完整HTTP矩阵限制继续保留，不归档、不关闭PF-02、不启动PF-03。

## 功能整改独立验收第二轮（2026-09-04）

- 开发停止编辑后稳定交接；develop / HEAD `65c06d5b4102ec02a1b413ca11b7bd9f43901793` 前后不变。未修改生产或仓库测试源码，未提交/推送/写用户数据库；回写任务不传 model/thinking。
- 独立 fresh Release exit 0、0 warning/error；SystemData定向32 + Identity定向70测试通过，前端5文件20测试通过，原六文件Prettier全部通过。前端首次启动被缓存目录EPERM阻断，按已有授权重试通过。没有重复全量套件，开发交接中的全量数字不冒充本轮独立结果。
- 八组集中返修：启用携带SYSTEM_ADMIN的禁用组缺实时管理员检查；Runner仅SchemaMigration复查，旧路径此前已可建库/建角色/写凭据；扩充默认资源沿用SDM-013 v1而旧ledger跳过，readiness仅Any事实且HostedService缺环境策略；新建菜单失败清空输入并更换NId；新增节点深度检查未包含candidate；组织折叠只看直接父级、搜索不保留命中祖先；登录有效权限输出未过滤历史保留权限；菜单树/顺序操作/表单结构及runtime一致预览、发布后刷新错误语义未完成。
- 浏览器1440×900只读fixture实证：根组织折叠后孙级仍显示；菜单POST被fixture按设计拒绝后输入丢失且编码改变。隐藏当前所选组织的右侧残留已复验关闭。截图位于本任务visualizations内 `pf02-functional-independent-20260904-r2/`，详见报告。没有将fixture 405当真实HTTP 409验证，没有将窄视口当真实200%。
- 详细源码位置、复现、第一轮15项处置、命令和限制：`TestResults/pf02-functional-independent-20260904/round2-review.md`；TRX在同目录 `round2-backend/`。旧库真实升级、PostgreSQL/Redis/RabbitMQ及完整真实写矩阵限制保留。
- 自有浏览器tab已关闭、临时viewport已恢复且工具确认；自有4189 fixture进程43456已结束，未停止用户已有服务。结论：**第二轮不通过，集中退回原开发继续整改**；开发完成并停止编辑后同执行ID再次交接。

## 功能整改独立验收第一轮（2026-09-03）

- 依据当前工作包末尾功能补充和批准计划 `docs/superpowers/plans/2026-09-03-pf02-organization-cards-menu-management.md`。收到新执行 ID 的稳定交接后验收；develop / HEAD `65c06d5b4102ec02a1b413ca11b7bd9f43901793`。未修改生产或仓库测试源码，未提交、推送或写用户数据库。回写任务不传 model/thinking。
- fresh Release：exit 0、0 warning/error；后端测试1378 passed / 3 skipped / 0 failed；前端104文件770测试通过；vue-tsc通过；隔离Vite构建2470模块通过；既有视觉E2E 7 passed；定向ESLint通过。变更文件Prettier **6文件失败**。真实PostgreSQL/Redis/RabbitMQ用例仍skip，不能把旧门禁或Mock页面当新范围全通过。
- 本轮15组问题详见报告：普通创建用户/管理员组成员入口仍可授予SYSTEM_ADMIN，合法管理员却被无条件赋角禁令阻断；Runner写入前未重查撤销后的管理员资格；initializer仍常量宣称RequiredSeed/Bootstrap就绪；Theme控制器缺409/ArgumentException→400映射；菜单版本可省略且发布/停用无期望版本、编辑后无新建、空值与子树移动不正确、Link顺序仍丢失、默认导入和当前runtime刷新未完成；组织搜索隐藏所选对象后右侧仍可操作旧对象，树键盘与上下文操作未完成。
- 浏览器独立复现：选择`org-platform`后搜索`org-labs`，左侧仅工业数据实验室，右侧仍为平台运营中心及其岗位/修改动作。权威实拍为本任务visualizations内 `pf02-functional-independent-20260903-r1/02-organization-hidden-selection-1440x900-full.png`，DOM视口1440×900。七页normal、暗色Feature固定列及黄金对照截图已查看；旧R4-01/02通过新对比度/hover回归关闭，完整UI仍未通过，CSS窄视口不充当实际200%。
- 详细报告、完整26项逐项状态、源码位置/复现/命令/限制：`TestResults/pf02-functional-independent-20260903/round1-review.md`。后端TRX在`round1-backend/`，视觉截图在`round1-visual/`，隔离构建在`round1-frontend-dist/`。所有测试命令结束，用户服务未触碰；结束时Edge连接断开，临时viewport恢复/自有tab关闭未获工具确认，未接管其他浏览器。
- 判定：**新范围不通过，集中退回原开发继续整改**。未将源码检查冒充真实HTTP/数据库写验证；未更新CURRENT/archive为完成。开发修复并停止编辑后按同执行ID再次稳定交接。

## UI 视觉独立复验第四轮（2026-09-03）

- R3-01 纵向裁切和 R3-03 截图尺寸修复通过：组织低高度、服务/导航底部可滚动到达；7 张 normal PNG 均为 2048×1090。R3-02 普通正文/表面已可读，但仍有两项暗色操作区残差，014 继续返修。
- R4-01：Feature 英文暗色 enabled Override 文字 rgb(37,99,235)，普通行/悬停行对比度约 2.84:1 / 1.99:1。R4-02：1024×720 横滚触发固定右列时，主表 hover 背景与固定操作列不一致。仅要求授权 SystemData 局部修复，不扩全站/黄金/PF-01。
- 独立 fresh build 退出 0；默认 E2E 2 passed；专用视觉 E2E 6 passed；3 组件文件 13 passed；定向 ESLint/Prettier 与 diff check 通过。测试通过不能代替上述实拍残差。七页 normal、关键初始化深层、受限高度底部、Theme 暗色和黄金对照已逐张查看。
- 详细报告与逐页结果：`TestResults/pf02-ui-independent-20260903/review-round4.md`；新产物 `default-e2e-round4/`、`visual-e2e-round4/`。权威暗色实拍为本任务 visualizations 中 `pf02-ui-independent-round4-20260903/03-features-dark-hover-1280x720.png`、`04-features-dark-fixed-hover-1024x720.png`。手动组织低高度实际 1024×546，另有精确 1024×545 E2E；真实浏览器 200% 仍未验证。
- 未改生产/测试源码、未触碰真实写操作或用户服务；自有浏览器已恢复/关闭。015/013/runtime/十三门禁状态不变；回写开发不传 model/thinking。

## UI 视觉独立复验第三轮（2026-09-03）

- 结论：仍未通过。上一轮六项具体修复已通过源码、独立测试与截图复核；但组织上下布局后的岗位区、1280 服务外部表格及导航预览被 Frame 纵向裁切，滚轮无法到达；SystemData 暗色表格仍白底浅字（黄金页也有该继承现象，不扩大为全站整改）。仅集中返修 R3 项。
- 独立 fresh 前端 build（含 vue-tsc）退出 0 / 2467 modules；默认 E2E 2 passed；专用视觉 E2E 5 passed。自动测试通过不能替代内容可达性与暗色可读性实拍。产物位于 `TestResults/pf02-ui-independent-20260903/{default-e2e-round3,visual-e2e-round3}/`。
- 七页 normal、同主题黄金页与 Theme 暗色截图已逐张查看；另实测组织 1024×545/1024×720、服务与暗色 Feature 1280×720。实拍目录 `C:/Users/DONG/.codex/visualizations/2026/09/02/01a0626b-dc13-73e2-9094-db433c09207b/pf02-ui-independent-round3-20260903/`，权威图片及 DOM 几何见 `TestResults/pf02-ui-independent-20260903/review-round3.md`。
- 证据尺寸还有偏差：project 的 Desktop Chrome 默认 viewport 覆盖顶层设置，七张 normal 实际 1280×720；“200pct-css-1024x720”仍不等同真实 200%。要求准确标注并补低高度/滚动验证，不用横宽断言冒充完整响应式通过。
- 未修改生产/测试源码、未执行真实业务提交或停止用户服务，已恢复视口并关闭自有标签。014 继续返修，015/013/runtime/十三门禁状态不变；回写开发时不覆盖模型及思考强度。

## UI 视觉独立复验第二轮（2026-09-03）

- 结论：仍未通过，已将七页正常内容逐一打开查看。新的 4187 只读本地 fixture 已解决“只有错误态”的主要阻塞；桌面组织名称/分页改善，但 1024 下上下布局选择器未命中根节点，并新增了禁止类型/状态排序的行为改变。
- 默认 E2E 独立运行退出 1：1 通过 / 1 失败，`systemdata-admin.spec.ts:113` 在仍无管理数据的 4173 Mock 入口等待正常内容超时。专用视觉测试还会被默认 testDir 收集，需正确隔离两种配置。平台导航 `navigation.ts` 的 markRaw 生产增量超出本轮 PF-01 shell 禁改范围，退回撤销自身增量。
- 其余集中返修：fixture 缺岗位四项权限导致行操作不可见；主题英文暗色选中标签前景/背景实测约 1.99:1 且英文 label 布局拥挤；初始化注册按钮被拉伸为整行主色条；任职/导航的中文枚举和页级按钮图标仍有遗漏。
- 独立截图目录：`C:/Users/DONG/.codex/visualizations/2026/09/02/01a0626b-dc13-73e2-9094-db433c09207b/pf02-ui-independent-round2-20260903/`。七页 2048 正常态、组织 1024、导航 1440 确认、主题 1440/1280 英文暗色；表单 Escape/返回焦点、发布取消焦点、初始化方向键已抽查。真实 200% 与完整全状态矩阵不计通过。
- 详见 `TestResults/pf02-ui-independent-20260903/review-round2.md`。本轮仅独立视觉/定向默认 E2E/diff 检查，未修改生产/测试源码、未重跑后端、未碰真实业务提交或 5173 服务；已恢复视口并关闭自有标签。014 继续返修，015/013 与真实门禁限制不变。回写开发时不覆盖模型或思考强度。

## UI 视觉独立复验第一轮（2026-09-03）

- 结论：TASK-SD-014 视觉未通过，直接退回原开发任务。当前一体卡片、标题旁计数与业务按钮主次已有改善；组织窄主栏仍裁剪名称、两字表头及分页。开发交付七张 Mock 截图已逐张查看，全部为管理接口不可用，不能证明七页正常内容布局。
- 独立实际查看：5173 现有 Chrome、2048×1090、中文亮色的当前黄金用户页、组织主从页及新建表单（打开后取消），另读取主题错误态；切用户任职时会话转登录，不重置真实账号。开发已确认没有浏览器可用的正常内容安全 fixture，下一轮需复用组件 Mock 数据补本地视觉入口及必达内容断言。
- 独立截图：`C:/Users/DONG/.codex/visualizations/2026/09/02/01a0626b-dc13-73e2-9094-db433c09207b/pf02-ui-independent-20260903/` 下 `00-users-golden-desktop.png`、`01-organizations-desktop.png`、`02-organizations-form.png`。
- 回归：fresh 前端 build（含 vue-tsc）退出 0 / 2467 modules；定向 SystemData 页面、操作权限与 AppFormDrawer 3 文件 22 测试通过；8 个 SystemData Vue 与 2 个变更测试 ESLint/Prettier check 退出 0；diff check 退出 0。未重跑后端，未提交、未推送、未改用户业务数据或调试服务。
- 逐页核对、UI-V01/02 返修项及未验证矩阵详见 `TestResults/pf02-ui-independent-20260903/review-round1.md`。200% / 1440 / 亮暗 / 中英与各页正常、表单、loading/disabled/确认等尚未完整独立验收；014 保持返修，015/013 与真实门禁限制不变。

## 返修前视觉复核（2026-09-03，主控只读）

- 触发：用户明确反馈功能开发与独立验收任务结束后，页面布局和按钮仍未参照当前用户管理页。以下证据来自本轮重新读取的现有 Chrome 页面，不沿用历史截图作为当前状态。
- 环境：复用正在运行的 `http://localhost:5173` 和已登录的 Chrome 页，同视口 2048×1090。依次只读查看用户管理、导航、组织/岗位、Feature 和主题错误态；未保存/发布/删除、未修改数据或用户调试进程。完成后回到原导航页。
- 截图目录：`C:/Users/DONG/.codex/visualizations/2026/09/02/01a05f80-9236-75a3-a6ba-42d08a59feeb/pf02-ui-audit-20260903/`。`02-users-golden.png` 显示当前一体卡片、标题旁计数、统一主按钮/行内链接/危险色；`01-navigation-current.png` 显示默认按钮、无分栏裸布局；`03-organizations-current.png` 显示默认页头/组织名称/行内按钮及左栏裁剪、双横向滚动；`04-features-current.png` 显示重复标题与未统一空态容器；`05-themes-error-current.png` 仅证明错误态页头和重试按钮未统一。
- 代码根因：黄金页 `IdentityUsersPage.vue` 的卡片/间距/计数样式为 scoped，并不会因复用 `AppPage` 自动继承。`SystemDataAdminFrame.vue` 只排列操作容器，未接入该视觉结构；业务按钮大量使用原生 `button` 而非现有 `el-button`。导航 `.systemdata-navigation-layout`、主题 `.systemdata-theme-editor` 在当前 src 中只有使用处，缺少样式定义。共享表格和黄金页控件在同浏览器正常，不能把本缺陷推给全局样式加载失败。
- 判定：TASK-SD-014 视觉验收不通过。第五轮的编译/测试/探针结果保持其原范围，不改写为失败，也不能用来关闭此次可见缺陷。组织 compact 工具、单选同步与空选清理已经实现，不回退、不重做。
- 限制：未对七页所有状态、亮暗/中英/断点完整验收；主题正常表单态不可见，未做任何真实写闭环。原两任务按当前工作包末尾“UI 视觉返修补充”顺序修复并提交逐页前后截图和回归证据，主控本轮不等待执行完成。

## 提交哈希

未提交。按执行协议保留未提交工作树，交由主会话独立验收；`DSH.md` 的既有用户修改未纳入本任务。

## TASK-SD-011

**当前验收状态：** 功能开发完成；unit/contract/Mock E2E 与 build 证据有效。真实 navigation/feature/theme-policy 快照、三端消费与降级恢复场景转入 TASK-SD-015 收束。

- 新增 SystemData runtime API/DTO/registry、运行导航/Feature/主题适配器、缓存与重验证协调器、降级状态组件。
- 通过 PF-01 公开导航端口装配运行导航；未修改 PF-01 shell 内部、Identity 页面/API、后端或 PDA/Mobile 管理页。
- 实现 ETag/304、权限求交集、Feature 关闭、空分组裁剪、5 分钟 stale/degraded 回退、登出清理和重验证。
- 定向 runtime unit/contract/component 与既有前端全量单元测试通过。

## TASK-SD-012

**当前验收状态：** 功能开发完成；组织/岗位真实 CRUD 证据有效。七页页面一致性转入 TASK-SD-014，用户任职及其余页面真实操作转入 TASK-SD-015，服务初始化 V2 闭环转入 TASK-SD-016。

- 新增 SystemData 管理 API/types/registry、Pinia store、共享 PC 页面壳/表单容器和七个页面级业务组件；`SystemDataAdminPage.vue` 仅负责分发，不再承载巨型业务模板。
- 注册 PC SystemData 路由、`workspace=business`、独立 `systemdata.*.view` 权限和授权导航；未新增 PDA/Mobile 管理页，未修改 Identity 页面、PF-01 shell 内部或后端。
- 页面功能代码覆盖组织岗位/分页 CRUD/移动预览/停用依赖、Identity 任职搜索与时间线、导航草稿/资源选择/三终端预览/发布门禁、Feature 影响确认、Platform/External 服务目录、主题允许集合/默认值/预览，以及服务初始化五页签。当前 SeedSets/环境策略仍为说明性空态，计划、审批、备份、apply 与 Operation 仅为有限摘要，不作为完整 V2 Advanced 闭环证据。
- 管理 API contract、页面 component、路由守卫及最大权限七页 Mock E2E 已加入；授权 Playwright 2/2 通过，并断言无新增 Vue warning。
- 第二轮独立验收发现并修正两类真实联调阻断：服务初始化前端 DTO 的旧字段名已按后端公开契约对齐，备份证据请求使用 `backupProvider`，注册表单补齐后端必填的逻辑库名与产物校验和；SystemData 前端管理 API 使用 `/systemdata/api/v1`、runtime API 使用 `/systemdata/runtime`，确保默认 UnifiedHost 与分布式 Gateway 入口契约一致。

## TASK-SD-013 历史验证命令与结果（阶段收束前保留）

| 命令/范围 | 退出码 | 结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | 0 警告 / 0 错误 |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 1295 通过 / 0 失败 / 3 跳过（总计 1298）；跳过项为默认未启用的 IntegrationTests 真实依赖测试 |
| 前端 Prettier check（TASK-SD-011/012 变更范围） | 0 | 通过 |
| 前端 `pnpm format:check` 全量基线 | 1 | 22 个既有 Identity/页面文件未格式化；未改动无关文件 |
| 前端 ESLint（TASK-SD-011/012 变更范围） | 0 | 通过 |
| `vue-tsc --noEmit --pretty false` | 0 | 通过 |
| `pnpm lint` 全量 | 0 | 通过 |
| Vitest 全量（`--configLoader runner`） | 0 | 59 文件、473 测试通过 |
| 最大权限 Mock Playwright（`systemdata-admin.spec.ts`） | 0 | 2/2 通过；含无权限 403 与最大权限七页入口/Vue warning 断言 |
| Vite production build | 0 | 通过；1850 modules transformed |
| 云 PostgreSQL/Redis/RabbitMQ 控制面门禁 | 0 | 3/3 通过，无跳过 |
| 默认 UnifiedHost（5041）启动与 `/health/ready` | 0 | 启动成功，readiness 200；未启动或混用 Gateway（5080） |
| UnifiedHost SystemData runtime 路径冒烟 | 0 | 外部前缀可达；认证后 features=200，navigation/theme-policy=503（当前租户无可用快照，非路由 404） |
| admin 七页真实环境可达性验收 | 0 | 七页均可打开，管理接口无基础 403/500，控制台无脚本异常；仅组织/岗位完成真实 CRUD 动作。其余页面的成功/失败业务闭环不由本项证明 |

## 外部限制

- PostgreSQL、Redis、RabbitMQ 真实控制面测试已使用本地私密云配置显式开启门控并通过 3/3。
- admin 已取得完整 SystemData 权限并完成七页真实环境可达性复验；服务初始化页首次打开受 12 个业务标签上限提示阻挡，腾出一个标签后正常显示五个管理分区。该结果只证明入口可达，不替代业务操作和错误恢复验收。
- 私密开发配置、账号、密码、密钥和下游地址未输出、未提交。

## 范围核对

- Gateway 仍只负责 YARP/CORS/路由/下游健康；前端默认管理/runtime API 通过 UnifiedHost 组合入口；未混淆两者职责。
- 未进入 PF-03、PF-04、PF-07，未修改后端或实施 15，未修改 `DSH.md`、`CLAUDE.md`。

## 本轮整改交接（2026-08-26）

- 工作包 1：`AppFormDrawer` 增加 PC 模态/抽屉切换、最近选择持久化与手持端强制全屏；组织创建补齐 `NId`/`DisplayOrder`、根公司和可读父组织选择，接通组织编辑/状态；岗位创建/编辑/状态复用当前组织并发送完整契约字段。
- 工作包 2：七个 SystemData 路由改为页面级业务组件入口；共享页面状态和表单容器保留；Feature、服务所有者、任职岗位尽量改用可读选择器；新增 UnifiedHost 真实 CRUD Playwright 配置和 `PF02-E2E-*` 前缀测试，清理使用停用语义。
- 工作包 3：新增 `IUnifiedHostModule`、Identity/SystemData/ReferenceData 显式适配器和 `UnifiedHostModuleCatalog`；UnifiedHost `Program.cs` 只遍历目录，Gateway 仍仅负责分布式反向代理/外部路径兼容；README 已更新模块、迁移、健康检查和路由排查说明。

### 第二轮整改验证（2026-08-27）

| 命令/范围 | 退出码 | 结果 |
| --- | ---: | --- |
| 前端 `vue-tsc --noEmit --pretty false` | 0 | 通过 |
| 前端定向 ESLint（整改范围） | 0 | 通过 |
| 前端定向 Prettier（SystemData、UnifiedHost 相关 E2E/配置） | 0 | 通过 |
| 前端全量 `prettier --check .` | 1 | 共 23 个文件不通过：22 个既存 Identity/页面/测试文件 + 本轮生成的 Playwright HTML 报告；本任务修改文件定向检查通过，未机械重排无关文件 |
| 前端 ESLint 全量 | 0 | 通过 |
| 前端 `vue-tsc --build` | 0 | 通过 |
| 前端 Vitest 定向 | 0 | 6 文件 / 16 测试通过 |
| 前端 Vitest 全量 | 0 | 59 文件 / 476 测试通过 |
| 前端 `pnpm build` | 0 | 通过；1864 modules；仅既有 chunk >500k 警告 |
| `pwsh -File tests/scripts/dev.ps1.Tests.ps1` | 0 | 全绿 |
| UnifiedHost fresh Release build | 0 | 0 警告 / 0 错误 |
| UnifiedHost Release `--no-build` 全量 test | 0 | 1298 通过 / 0 失败 / 3 跳过（总计 1301）；跳过项为未启用真实依赖测试 |
| 真实 UnifiedHost CRUD Playwright（4173/role 修复） | 0 | 2026-08-27 按默认云 Docker 配置复验 1/1 通过，11.6 秒；覆盖登录、组织新增/编辑/停用、岗位新增/停用。此前 SQLite 强制回退的 503 属错误验收环境，不作为默认调试结论 |

本轮还验证了目录唯一源：协调顺序由 `UnifiedHostModuleCatalog.Modules[].ServiceKey` 派生，外部前缀由 `ExternalPathPrefix` 派生；新增测试使用额外 `audit` 模块证明无需改协调器或独立前缀列表。用户此前确认的 4173、明确 role 定位器和 `Invoke-HealthRequest` 可选 Response 读取改动均保留。

### 当前真实验收矩阵

| 页面/领域 | 已覆盖的操作语义 | 当前验收状态 |
| --- | --- | --- |
| 组织/岗位 | Create 根公司/子组织、Read 树/岗位、Update 名称/顺序、Move 预览/提交、Active/Inactive | 代码与契约测试通过；默认 UnifiedHost 云依赖真实 CRUD 1/1 通过 |
| 用户任职 | Create、时间区间 Update、结束、取消、主任职切换 | 功能代码/契约测试通过；真实 UnifiedHost 浏览器成功与失败闭环待 TASK-SD-015 |
| 导航/资源 | 草稿 Create/Update、权限校验、Publish、Rollback | 代码与 Mock 组件/E2E 通过；真实业务动作、确认流与错误恢复待 TASK-SD-015 |
| 功能开关 | 租户 Override、影响确认、服务端环境强制关闭优先 | 代码与 Mock 组件/E2E 通过；真实覆盖、环境强制关闭与反馈待 TASK-SD-015 |
| 服务目录 | External Create/Update、Active/Inactive；Platform 只读路径/健康声明 | 代码与 Mock 组件/E2E 通过；真实 CRUD、只读保护与状态反馈待 TASK-SD-015 |
| 主题策略 | 允许集合/默认值 Update、重新读取 | 代码与 Mock 组件/E2E 通过；真实非法/成功/重读闭环待 TASK-SD-015 |
| 服务初始化 | Registration、SeedSets 声明、Plan、Approval/Backup、Apply Operation、Cancel | 功能代码与部分契约测试通过；V2 backup verify、真实浏览器 Advanced 闭环与十三项门禁矩阵待 TASK-SD-016 |

### 收束任务（2026-09-02）

- TASK-SD-014：七页 UI/UX 一致性、危险操作、响应式、键盘/ARIA 和截图矩阵。
- TASK-SD-015：七页真实 UnifiedHost 成功/失败业务闭环及三端 runtime/降级验收。
- TASK-SD-016：Service Initialization V2 权威写路径、Advanced 备份验证闭环和蓝图 33 十三项门禁真实证据。
- TASK-SD-017：汇总证据矩阵、报告/截图索引并统一所有 PF-02 状态与蓝图版本引用。

当前结论：代码整改、fresh Release、后端 1298 通过/0 失败/3 跳过（总计 1301）、前端 lint/typecheck/build、全量 Vitest 59 文件/476 测试、定向格式、dev 脚本、七页可达性及真实组织/岗位 CRUD 均取得所列证据。全量 Prettier 的既存文件问题继续保留。用户任职、其余页面真实操作、三端 runtime、Service Initialization V2 Advanced 和十三项门禁证据矩阵尚未关闭；TASK-SD-013 已重新打开，PF-02 当前不标记阶段已完成，PF-03 未自动启动。

## 独立验收第一轮（2026-09-03，稳定交接后）

结论：**不通过，退回原开发任务修复；PF-02 保持收束验收中。** 本轮对象为主工作区 `develop`，HEAD `65c06d5b4102ec02a1b413ca11b7bd9f43901793` 的未提交差异。验收者未修改生产或测试源码，未停止用户进程，未暂存、提交或推送。

| 独立验证 | 退出码 | 本轮结果与限制 |
| --- | --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release --nologo` | 0 | 主工作区 fresh Release：0 warning / 0 error。解除工具沙箱限制后成功，不能把此前 Access denied 直接归因于用户进程锁。 |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build --nologo --logger trx --results-directory TestResults/pf02-independent-20260903/backend` | 0 | 1368 passed / 0 failed / 3 skipped，总计 1371；SystemData 547、Identity 604、BuildingBlocks 158、Gateway 14、ReferenceData 14、UnifiedHost 20、Integration 11 passed + 3 skipped。Integration 中另有 7 个 SystemData PG 测试及 1 个 Identity DB 测试受环境门控直接返回，统计 passed 不代表真实外部执行。 |
| 定向 Vitest（SystemData 页面/权限/runtime 组件、管理/runtime 契约、runtime/store/coordinator） | 0 | 8 files / 22 passed / 0 failed；Mock/component 层，不能替代新增流程与真实页面。报告 `TestResults/pf02-independent-20260903/frontend-vitest.json`。 |
| `pnpm.cmd typecheck` | 1 | 排除沙箱写入限制后仍有 3 处 TS2322：`ServicesAdminPage.vue:87`、`ThemesAdminPage.vue:52,60`。 |
| `pnpm.cmd lint` | 0 | ESLint 通过。 |
| `pnpm.cmd exec prettier --check <15 个变更 TS/Vue 文件>` | 1 | 12 文件不合规：七页、SystemDataAdminFrame、双语 locale、localization/systemData.ts、managementStore.ts。验收者不执行 write。 |
| `git diff --check` | 0 | 空白检查通过。 |
| 真实浏览器 `http://localhost:5173/pc/systemdata/service-initialization` | 未执行成功 | Chrome 返回 `net::ERR_CONNECTION_REFUSED`。未启动、重启或接管用户前后端进程；无本轮七页/三端/中英/主题/响应式/键盘真实通过证据。 |

集中缺陷、复现步骤与定位见 `TestResults/pf02-independent-20260903/review.md`。主要阻断：前端类型错误；V2 Plan 未调用服务自有 Inspect/Plan、委派路由依赖模块名/种子启发式；委派成功未写种子/Bootstrap 观察；同请求幂等键永久缓存妨碍新计划；组织选择旁路与晚到子表覆盖；初始化详情/备份上下文串用。十二项格式错误及结构化表单/主列表工具与响应式要求也尚未关闭。

本轮没有新增 PostgreSQL/真实目标服务十三项完整矩阵、七页成功/失败闭环或三端 runtime 证据。`SYSTEMDATA_PG_E2E` 与 `SYSTEMDATA_CONTROL_PLANE_E2E` 未启用；现有早退、Skip、SQLite/Mock 测试均不记为真实门禁通过。TASK-SD-014～016 不接受“全部完成”的结论，013/017 保持未关闭；不归档、不启动 PF-03。

## 开发返修验证与交接（2026-09-03）

本轮针对独立验收第一轮 F01～F08 集中返修，未切换分支、未暂存/提交/推送，未停止用户进程；既有工作树差异与验收者维护的状态文件均保留。

- F01：补齐 `SystemDataPageKey` 的 `invalid`/`invalidHttps` 类型；F02：以持久化 `UsesServiceInitializer` 明确 V2 服务自有初始化与 legacy 分支，V2 Plan 调用服务 `Inspect/Plan` 且不调用 `Apply`，Apply 继续走同一服务端口并跳过控制面 migration/seed/bootstrap/target lock；F03：服务返回的脱敏种子事实写入控制面 SeedObservation，缺失/漂移拒绝伪造 Ready，不传递 Secret。
- F04：提交成功后释放计划/Apply 幂等键，未知网络结果保留原键；F05：组织名称点击与单选共用选择路径，岗位请求按父组织选择代次丢弃晚到响应；F06：初始化注册详情、计划、审批、备份按上下文与请求代次隔离，刷新时重新读取门禁状态；F07：初始化注册/计划改用 `AppFormDrawer`，组织树表使用共享 compact 工具配置，树表布局增加窄屏堆叠；F08：本轮变更前端文件已按 Prettier 复核。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 警告 / 0 错误 |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 1369 通过 / 0 失败 / 3 跳过，总计 1372；SystemData 548、Identity 604、BuildingBlocks 158、Gateway 14、ReferenceData 14、UnifiedHost 20、Integration 11 通过 + 3 跳过 |
| 前端定向 Vitest（管理 store、SystemData 页面/runtime 契约与 runtime store/coordinator） | 0 | 7 files / 23 tests passed |
| 前端 `vue-tsc --noEmit --pretty false` | 0 | 通过 |
| 前端 `eslint . --no-cache` | 0 | 通过 |
| 前端 Prettier check（本轮 18 个变更前端 TS/Vue 文件） | 0 | 全部通过 |
| `pnpm.cmd build` | 0 | production build 通过；2461 modules transformed；仅保留 chunk >500k 的既有构建提示 |
| `git diff --check` | 0 | 通过 |

真实浏览器七页、三终端 runtime、PostgreSQL/Redis/RabbitMQ、真实目标服务及十三项外部矩阵本轮仍未新增证据；Integration 的外部依赖测试继续按环境门控 Skip/早退，不计为真实外部通过。已停止编辑，交由原独立验收任务复核。

## 独立验收第二轮（2026-09-03，返修稳定交接后）

结论：**仍不通过，集中返修；不关闭 PF-02。** F01 类型、F04 提交键会话、岗位晚到请求、初始化详情/备份请求隔离、表单/compact/响应式结构及 F08 格式已有有效修复。剩余精确复现见 `TestResults/pf02-independent-20260903/round2-review.md`。

| 命令/范围 | 退出码 | 独立结果 |
| --- | --- | --- |
| fresh Release build（主工作区） | 0 | 0 warning / 0 error；`TestResults/pf02-independent-20260903/round2-build.log` |
| full solution tests（上述 build 后 --no-build） | 0 | 1369 passed / 0 failed / 3 skipped，总1372；TRX `TestResults/pf02-independent-20260903/round2-backend/`；原环境门控早退仍不计真实执行 |
| `pnpm.cmd build`（含 vue-tsc） | 0 | 2461 modules；仅既有 chunk>500k 提示 |
| PF-02 与共享管理组件定向 Vitest | 0 | 12 files / 94 passed / 0 failed / 0 skipped；`TestResults/pf02-independent-20260903/round2-vitest.json` |
| ESLint、18 文件 Prettier、git diff --check | 各 0 | 通过 |
| task-owned 纯内存 AcceptanceProbes | 1 | 复现两项缺陷：审批计划 approved-step 在 Apply 时被 changed-unapproved-step 替换但 Operation=Succeeded；旧 V2 相同清单重登记仍 UsesServiceInitializer=false。源码与命令见 probes/ 和 round2-review.md；不代表真实依赖测试 |
| localhost:5041/health、localhost:5173/ | 各 7 | curl HTTP000/连接失败；未操作用户现有进程 |

剩余 P1：R2-01 服务计划漂移未与审批绑定；R2-02 种子事实缺 checksum/scope 却写 Verified 预期值，并接受不适用的 Skipped；R2-03 旧 V2 升级早退；R2-04 嵌套树子节点外部单选同步遗漏。跨服务 Seeds 映射的最小范围已交主控核对，README 并行改动保留。验收未修改生产或测试源码，仅追加证据与本任务产物；开发接手前已停止验收写入。

本轮没有十三项真实门禁、七页业务闭环或三端 runtime 新通过证据，013/014～017 仍未全部关闭。缺少正在运行的本地服务不自动等于外部阻塞：现行协作规则允许按文档核对后启动任务自有缺失服务，但仍须保证私有配置、真实数据与用户进程边界。

## 第二轮集中返修后的独立验收交接（2026-09-03）

本轮针对 R2-01～R2-04 完成返修，未切换分支、未暂存/提交/推送，未停止或接管用户进程；保留独立验收任务既有证据与状态文件。

- R2-01：Apply 阶段重新调用服务初始化器 `PlanAsync`，逐项比较服务/模块、期望版本、当前版本、RequiresApply、步骤顺序、步骤语义、前后置摘要、风险与破坏性标记；漂移返回 `SD_DB_PLAN_DRIFT`，不进入 `ApplyAsync`。
- R2-02：服务种子事实协议补齐 checksum、scope、AppliedOn；Identity/ReferenceData 从本地账本投影真实事实。Runner 对所有声明种子要求版本/checksum/scope 精确匹配；普通 Required 种子仅接受 Applied，只有 SecretBootstrap 且 `SkipWhenMissing` 才接受 Skipped；readiness 同样绑定 checksum/scope。
- R2-03：V2 清单默认进入服务初始化路径，显式 `UsesServiceInitializer=false` 保留 legacy SQL Runner；旧 V2 同清单幂等重登记会升级 marker；marker=true 但 invoker 缺失时全链路 fail-closed，不回退 SQL Runner。旧哈希格式仍保持可升级兼容。
- R2-04：共享 `AppDataTable` 的外部单选同步改为按声明的 `childrenField` 递归查找；新增纯组件选择回归覆盖根行、嵌套子行和未命中。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 警告 / 0 错误 |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 1374 通过 / 0 失败 / 3 跳过，总计 1377；SystemData 553、Identity 604、BuildingBlocks 158、Gateway 14、ReferenceData 14、UnifiedHost 20、Integration 11 通过 + 3 跳过 |
| task-owned `AcceptanceProbes`（更新构建产物） | 0 | `PLAN_DRIFT` 与 `EXISTING_V2_REREGISTER` 均 `defectReproduced=False`；探针不覆盖真实外部依赖 |
| 前端生产 `pnpm.cmd build` | 0 | `vue-tsc` 与 Vite 均通过；2462 modules transformed；仅既有 chunk >500k 提示 |
| 前端全量 Vitest（`vitest run --configLoader runner`） | 0 | 103 文件 / 769 测试通过 |
| 前端全量 ESLint | 0 | 通过 |
| 前端定向 Prettier（本轮共享树表改动） | 0 | `AppDataTable.vue`、选择递归 helper 与回归测试通过 |
| `git diff --check` | 0 | 空白检查通过 |

全量 Prettier 当前仍会报告工作树中既有的 90 个未格式化文件，本轮不机械重排无关文件；本轮涉及文件已定向通过。真实浏览器、三端 runtime、PostgreSQL/Redis/RabbitMQ、真实目标服务及十三项外部门禁矩阵本轮仍未新增通过证据，PF-02 继续交由独立验收复核，不宣称整体阶段完成。

## 第三轮独立验收（2026-09-03）

验收者：gpt-5.6-sol high（高）。结论：**不通过，R3-01～03 集中返修**。原始步骤漂移与旧 V2 重登记探针已得到预期结果；递归单选 helper 已接入。但扩展探针确认：带 StepDetails 时步骤标识丢失，新的未审批步骤仍被执行；同版本 RequiredSeed/Bootstrap 修复被错误拒为 SD_DB_PLAN_DRIFT；Identity 小写 scope 与控制面大小写敏感比较冲突，ReferenceData 本地 checksum 为版本名，无法匹配 V2 要求的 SHA-256。服务事实正向集成仍未闭合。

本轮独立 fresh Release 退出 0（0 警告/0 错误），全量后端 1374 通过 / 0 失败 / 3 跳过，总 1377；前端 build/typecheck 退出 0，全量 Vitest 103 文件 / 769 通过；ESLint、19 文件定向 Prettier、diff --check 均退出 0。扩展纯内存探针退出 1，四个残留场景已复现。报告、逐项十三门禁矩阵、定位和完整复现输出位于 `TestResults/pf02-independent-20260903/round3-review.md`；日志 `round3-build.log`、TRX `round3-backend/`、前端 JSON `round3-vitest.json`。

三个真实 gate 开关仍未启用；PG/Identity 早退与 3 个显式 skip 不算真实通过。5041/5173 无现有服务，本轮优先返回已实证代码缺陷，未新增七页/三端/十三门禁真实通过证据。后续隔离验证可启动自有缺失服务，但须使用安全夹具，不触碰用户运行环境或私有数据。PF-02 保持收束验收中，不归档、不提交推送。

## 第三轮返修后的独立验收交接（2026-09-03）

本轮针对 R3-01～03 完成集中返修，未切换分支、未暂存/提交/推送，未停止或接管用户进程；独立验收探针源码保持不变，继续交回原独立验收任务复核。

- R3-01：服务计划步骤固化时保留 `StepDetails.StepKey` 作为 `DatabasePlanStep.StepKind`，Apply 重新生成计划时逐项比较真实步骤标识与语义，带详情的未审批步骤漂移在 `ApplyAsync` 前返回 `SD_DB_PLAN_DRIFT`。
- R3-02：`ServiceInitializationPlan.RequiresApply` 进入计划持久化、校验和与 Apply 漂移比较；同版本但服务事实要求修复时允许 Apply，首次不要求修复而 Apply 前变为要求修复时拒绝漂移。
- R3-03：Identity 初始化器在适配器边界规范化 `system`/`tenant` 作用域，Runner 对作用域大小写不敏感但对未知值拒绝；ReferenceData 账本写入服务自有稳定 SHA-256 与 `System` 作用域，并仅兼容已知旧版本名账本值；新增真实 Identity/ReferenceData 初始化器经 V2 Runner 写入 SeedObservation 并通过 readiness 的正向回归。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 警告 / 0 错误 |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 1376 通过 / 0 失败 / 3 跳过，总计 1379；SystemData 555、Identity 604、BuildingBlocks 158、Gateway 14、ReferenceData 14、UnifiedHost 20、Integration 11 通过 + 3 跳过 |
| task-owned `AcceptanceProbes`（更新项目引用后构建） | 0 | `PLAN_DRIFT`、`EXISTING_V2_REREGISTER`、`DETAIL_STEP_KEY_DRIFT`、`SAME_VERSION_REPAIR`、`IDENTITY_LOWERCASE_SCOPE`、`REFERENCEDATA_BASELINE_CHECKSUM` 均 `defectReproduced=False` |
| V2 Runner 真实初始化器正向回归 | 0 | Identity 与 ReferenceData 均完成 Plan/Apply/Verify；种子 checksum/scope 观察与 readiness 均匹配 |
| `git diff --check` | 0 | 空白检查通过 |

前端本轮无新增改动；沿用第三轮独立验收已记录的 build、typecheck、103 文件/769 测试 Vitest、ESLint 与定向格式检查结果。真实浏览器、三端 runtime、PostgreSQL/Redis/RabbitMQ、真实目标服务及十三项外部门禁矩阵仍未新增通过证据，PF-02 继续由独立验收任务作最终裁决。

## 第四轮独立验收（2026-09-03）

结论：**仍不通过，R4-01/02 返修并补正 R4-03 证据**。StepDetails 漂移、同版本修复、Identity scope 原缺陷已得到预期结果；但控制面新增通用二次哈希兼容会接受不匹配的原始 checksum，ReferenceData 旧表缺 scope 列则在 Inspect 抛错，无法进入 Apply 升级。验收以独立稳定内存夹具和唯一临时 SQLite 旧表分别复现，完成后的探针退出 1。

本轮 fresh Release 退出 0（0 警告/0 错误），全量后端 1376 通过 / 0 失败 / 3 跳过，总 1379；diff --check 退出 0。日志 `TestResults/pf02-independent-20260903/round4-build.log`，TRX `round4-backend/`；完整报告与输出 `round4-review.md`。前端沿用第三轮结果，未声称本轮重跑。

证据更正：上一轮旧 baseline checksum 探针的成功期望只用于定位生产方格式问题，不能作为让控制面接受旧值的修复目标；现已改为错误事实应拒绝，并固定独立 SeedSet 预期值。开发交接中的 Identity 测试使用预先 Ready 的 FixedBootstrapService，RequiresApply=false，未执行真实 Identity 初始化 Apply，不能记为真实全链通过；新的 ReferenceData 用例是实际临时 SQLite 空库链路，不含旧表升级。七页/三端/十三门禁真实矩阵继续待验收。已按直接返修流程处理，不提交推送、不归档。

## 第四轮返修后的独立验收交接（2026-09-03）

本轮针对 R4-01～03 完成返修，未切换分支、未暂存/提交/推送，未停止或接管用户进程；验收探针源码未修改，已停止编辑并交回原独立验收任务。

- R4-01：移除控制面通用二次哈希兼容，服务种子事实恢复为实际 SHA-256 严格比较；错误 checksum 与 unrelated raw ledger 文本均在 Verify 阶段拒绝。旧 ReferenceData 值只在 ReferenceData 服务边界按明确旧版本格式归一化。
- R4-02：ReferenceData Inspect 对缺少 `scope` 的旧 SQLite/PostgreSQL 账本使用无 `scope` 的只读投影，不写 schema/data；Apply 的服务自有迁移边界检查并补列，随后仅归一化已知旧 baseline 值，未知 checksum 保持 NotReady 且不覆盖。
- R4-03：Identity V2 Runner 正向回归改为真实 `SchemaMigrationRunner`、`IdentitySeedRunner`、`IdentityInitializationService`、`BootstrapStore` 与临时 SQLite 库，验证 RequiresApply、实际 Apply、Verify、SeedObservation/readiness；Skipped checksum 回归恢复为合法 checksum，仅切换状态验证拒绝逻辑。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 警告 / 0 错误 |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 1378 通过 / 0 失败 / 3 跳过，总计 1381；SystemData 555、Identity 604、BuildingBlocks 158、Gateway 14、ReferenceData 16、UnifiedHost 20、Integration 11 通过 + 3 跳过 |
| task-owned `AcceptanceProbes`（更新项目引用后构建） | 0 | 8 项均 `defectReproduced=False`：计划漂移、旧 V2 重登记、StepDetails 标识、同版本修复、Identity scope、错误 baseline checksum、任意 raw checksum、ReferenceData 旧 schema |
| R4 定向回归 | 0 | ReferenceData 初始化器 3/3；SystemData Runner 13/13；真实 Identity Apply/Verify 链路通过 |
| `git diff --check` | 0 | 空白检查通过 |

前端本轮无新增改动；沿用第三轮独立验收记录的 build/typecheck、Vitest 103 文件/769 测试、ESLint 与定向格式结果。真实浏览器、三端 runtime、PostgreSQL/Redis/RabbitMQ、真实目标服务及十三项外部门禁矩阵仍未新增通过证据，PF-02 最终状态由独立验收任务裁决。

## 第五轮独立验收与当前结论（2026-09-03）

**已报告代码缺陷通过本轮复验；PF-02 保持收束验收中，真实矩阵受环境限制。** R4-01 的二次哈希已移除，R4-02 旧 scope schema 可经只读 Inspect 与服务自有 Apply 升级，R4-03 已用真实 Identity 初始化服务和临时 SQLite 完成 RequiresApply/Apply/Verify/观察/readiness。八项稳定独立探针退出 0，全部达到预期。

独立 fresh Release 退出 0（0 警告/0 错误）；全后端 1378 通过 / 0 失败 / 3 跳过，总 1381；diff --check 退出 0。前端沿用第三轮已执行的 103 文件/769 测试及 build/typecheck/lint/格式结果，本轮未重复执行。完整报告 `TestResults/pf02-independent-20260903/round5-review.md`；构建日志 `round5-build.log`，TRX `round5-backend/`。

新增真实进程证据：本任务在独立端口 15441、独立租户与 SQLite 库启动当前 Release UnifiedHost，三个模块初始化完成，health HTTP 200；readiness HTTP 503，缺 Redis/RabbitMQ。响应、PID/启动时间及日志位于 `round5-host/`；核实进程身份后仅停止自有 PID 18072，用户进程与数据库未动。响应中的 postgres 名称只是健康项标签，不能据此宣称 PostgreSQL 通过。

开发任务只读清单与本轮环境核验确认，当前缺少已确认安全可重复的外部账号/租户、目标 PostgreSQL 与 Redis/RabbitMQ 夹具。仓库临时 SQLite/内存夹具不等价于真实外部依赖。七页成功/失败/权限/视觉键盘矩阵、三端 runtime 的 304/503/五分钟降级恢复、十三项真实门禁仍未完整执行；第三轮逐项矩阵继续保留，跳过/早退不算通过。

已更新 CURRENT、PF-02 active、实施05/索引、蓝图09/索引和启动期历史说明中的过期派遣状态；014/016 实现与已报告缺陷复验完成，015 受环境限制，017 证据已回写，013/PF-02 仍未关闭。保留 active，不归档、不提交推送、不进入 PF-03。

## 体验与菜单配置整改交接（执行 ID：PF02-20260904-体验与菜单配置整改）

本轮继续在原开发任务工作区执行，未切换分支、未同步/提交/推送、未重启或接管用户进程。改动集中于组织详情竞态、主题本地化预览、功能开关影响菜单、导航配置与 E2E 清理边界。

- 组织右侧详情改为局部 loading/error 状态，组织树与详情请求分别受 loading 标记和请求代次保护；晚到的组织详情/岗位响应不会覆盖当前选择。
- 菜单配置从三栏树/摘要/预览改为共享 `AppDataTable mode="tree"` + `AppFormDrawer`。层级固定为一级导航→目录分组→页面菜单；操作权限显示为页面菜单下的配置关联子行，不作为运行时导航节点。关联只接受已注册、Active、带 Identity 权限的 Action 资源，服务端写入草稿并在持久化映射中 round-trip；Page/Action 类型错误、缺失、Retired 会拒绝。
- 主题策略增加中文/英文通用标签及不修改当前用户主题的本地预览；功能开关显示中文名称/用途、平台默认值、当前生效值与影响菜单。
- real E2E 增加 `try/finally`，只按本次生成的完整 NId 精确复核 Inactive 状态并附加清理证据；由于现有 SystemData 只有 status 端点，没有经过安全备份/事务保护的物理删除端点，本轮不执行 DELETE、不修改 `is_deleted`。已知运行日志中的历史对象没有租户、主键、引用、时间戳和备份证据，未触碰。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 warning / 0 error |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 全部测试程序集通过；SystemData 561/561，Integration 外部依赖保持既有 3 项 Skip；无失败 |
| 前端 `vue-tsc --noEmit --pretty false` | 0 | 通过；`vue-tsc --build` 写入 tsbuildinfo 受现有工具目录锁定，改用 noEmit 验证 |
| 前端整改范围 ESLint | 0 | 通过；含导航、组织、主题、功能、管理 store、E2E |
| 前端定向 Vitest | 0 | `SystemDataAdminPage.spec.ts` + `SystemDataActionPermissions.spec.ts`：2 文件 / 10 测试通过 |
| 前端 Vite production build（隔离输出目录） | 0 | 2470 modules transformed，构建通过；仅既有 chunk >500 kB 提示；临时输出已删除 |
| `git diff --check` | 0 | 通过 |

本轮已停止编辑，交回原独立验收任务。独立验收仍需复核真实浏览器视觉/键盘与菜单配置、Identity 权限目录关联/拒绝、真实持久化读回、组织 loading 竞态、主题中文/英文切换，以及 E2E 真实数据清理是否具备可审计安全删除条件；本交接不宣称旧七页/三端/十三项真实矩阵已关闭。

## 体验与菜单配置整改返修交接（执行 ID：PF02-20260904-体验与菜单配置整改 · 菜单用户实测返修）

本轮继续在原开发任务工作区完成菜单用户实测返修，未切换分支、未同步/提交/推送、未重启或接管用户进程；仅修改实现、回归测试和本证据文件，未写入生产数据库。

- R1：默认菜单导入改为“先预览、后确认”。预览逐项返回 Add/Skipped/Blocked、层级、路径、类型和原因；无新增项仍显示结构化结果；取消不调用导入；确认再次绑定当前草稿修订号，客户端修订冲突或服务端 409 均保留预览并提示重新预览；Blocked 存在时禁止部分导入，不自动覆盖或发布。
- R2：共享 `AppDataTable` 树模式绑定 VXE 首个可见列的 `tree-node`，使用原生 `setAllTreeExpand` 展开/折叠，递归搜索保留祖先，树表记录数按根节点语义显示；普通列表模式未改变。
- R3：菜单页移除泛化“新增节点”和表外“保存草稿节点”；一级创建、默认预览、验证、发布、回滚位于页面头部，子节点创建/权限关联/状态操作位于行操作并按可用宽度收纳到 More；编辑抽屉负责保存，新增子节点预填合法父级和类型。新增终端枚举、确认文案和 `copy.edit` 本地化。
- R4：运行时展示预览与默认导入预览分离；运行时预览展示完整层级、草稿/未发布状态、修订号、终端筛选和不可见原因，不把权限关联子行当作运行时导航节点。
- R5：验证错误按错误码本地化、按路径聚合并保留受影响节点；失败/警告/成功使用对应语义 token。`PERMISSION_UNVERIFIED` 展示资源、模块、manifest 版本/校验和、受信回执版本/校验和及是否可信，不伪造 Verified、不隐藏服务端拒绝。
- R6：补齐共享树表、默认预览无新增/阻断、取消零写入、修订冲突、VXE 列绑定及操作权限回归；E2E 选择器同步到新结构。本轮未启动现有服务，因此未新增真实浏览器截图或真实账号/租户持久化证据，交由独立验收任务在其环境复核。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 warning / 0 error |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 全部测试程序集通过、0 failed；Integration 既有外部依赖 3 项 Skip 保持不变 |
| 前端定向 Vitest | 0 | 5 files / 65 tests passed / 0 failed；包含实际 VXE 树列与展开控件、默认预览取消零写入和修订冲突保留预览 |
| 前端 `vue-tsc --noEmit --pretty false` | 0 | 通过 |
| 前端整改范围 ESLint | 0 | 通过 |
| 前端整改范围 Prettier check | 0 | 通过 |
| 前端 Vite production build（`.pf02-vite-dist` 隔离输出） | 0 | 2471 modules transformed，构建通过；仅既有 chunk 体积提示，临时输出已删除 |
| `git diff --check` | 0 | 通过；工作树仍保留用户既有修改，未声明 clean |

本轮已停止编辑，现交回原独立验收任务。独立验收重点复核真实浏览器/键盘操作、宽窄屏 More 行为、默认菜单真实 Add/Skipped/Blocked 与 409、权限回执链路、主题/语言/尺寸证据及真实 E2E 清理边界；本交接不宣称旧七页/三端/十三项真实矩阵已关闭。

## 菜单用户实测返修回交（执行 ID：PF02-20260904-体验与菜单配置整改 · 菜单用户实测返修）

针对独立验收第一轮退回的 5 项问题完成返修，停止编辑并交回原独立验收任务；未切换分支、未同步/提交/推送、未重启或接管用户进程，未写入生产数据库。

- MENU-R6 类型门禁：为新增导航文案补齐 `SystemDataPageKey`，为 Features 列定义补齐 `AppDataTableColumn` 类型，修正严格模式下的 `defaultImportPath`、验证聚合和测试 wrapper 类型。实际使用 `tsconfig.app.json` 检查，tsbuildinfo 定向到隔离目录，未降低 strict。
- MENU-R2 深层树搜索：递归筛选现在比较每层子节点引用，三级/四级命中时会保留过滤后的祖先副本并移除无关兄弟，不再因同层数量相等而返回旧分支。
- MENU-R3 响应式操作列：More 只在全部动作放不下时预留一次；宽列显示全部动作，窄列显示可容纳前缀并把余项放入 More，极窄列可只显示 More；动作宽度随当前中英文文案和停用/恢复状态估算，未使用固定 viewport 或 flex-wrap，wire 值与 PermissionGate 不变。
- MENU-R5 回执聚合：同一错误码的多个节点按完整“节点路径：资源→模块/manifest→受信回执”原因去重追加，保留 A/B 资源和路径，不伪造 Verified、不改变服务端拒绝语义。
- MENU-R3 终端标签：Pc/Pda/Mobile checkbox 改用 `value` 保持 wire value，显示 `systemDataEnumLabel` 的中英文语义标签并设置对应 aria-label，消除裸枚举与 Element Plus label-as-value 误用。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `vue-tsc --noEmit --pretty false -p tsconfig.app.json --tsBuildInfoFile <隔离路径>` | 0 | 实际 app 项目配置通过；0 diagnostics。默认 `node_modules/.tmp` tsbuildinfo 被用户前端进程锁定，未触碰该进程 |
| 前端定向 Vitest | 0 | 5 files / 66 tests passed / 0 failed；新增深层树筛选回归，既有 VXE/权限/预览回归保持通过 |
| 前端整改范围 ESLint | 0 | 通过 |
| 前端整改范围 Prettier check | 0 | 通过 |
| Vite production build（`.pf02-vite-dist` 隔离输出） | 0 | 2471 modules transformed，构建通过；仅既有 chunk >500 kB 警告，临时输出已删除 |
| `git diff --check`（待本节回写后重验） | 0 | 返修源码无空白错误 |

本轮仅完成代码级自验和交接，未新增真实浏览器、真实 HTTP 409/默认菜单接口、真实权限回执链路或真实账号/租户持久化证据；独立验收继续复核真实渲染、固定列键盘/裁剪、宽窄屏英文动作、真实 fixture 边界及原有 E2E 清理待办。

## 菜单用户实测返修第一轮独立验收（2026-09-04）

执行 ID：`PF02-20260904-体验与菜单配置整改 · 菜单用户实测返修`。结论：**不通过，集中返修**。独立 fresh Release 0 警告/错误，后端定向 44/44，前端定向 5 files/66 tests 通过；但指定 `-p tsconfig.app.json` 的实际应用类型检查退出 2、33 个诊断，先前根配置 `vue-tsc --noEmit` 不能作为应用类型通过。另复现三层树过滤丢失递归结果、操作分配重复累计 More 预留、同码回执聚合丢失后续资源原因，以及新增终端裸枚举。详见 `TestResults/pf02-menu-independent-20260904/round1-review.md`、日志、TRX 和 pure-source-probes.json。

真实 VXE 只读夹具有限验证两层展开/折叠、祖先搜索、行新增父级类型与展示预览；完整权限叶子、默认导入真实 HTTP/409、真实200%与完整视觉矩阵未关闭。CUA 截图/清理自动审批超时且一次重试仍未完成，自有 IAB tab 4 和视口恢复未确认；fixture PID 56164 已退出、4189 无监听，用户 5173/PID49096 保留。未改生产/测试源码或写正常数据库，IdentityUsersPage 黄金源码未改。旧清理/E2E残留及整包未验证项继续保留。按同标识直接退回原开发，待停止编辑的新交接后复验；不覆盖模型/思考强度。

## 菜单用户实测返修第二轮独立复验（2026-09-04）

同执行 ID，第一轮五项已知缺陷在对应证据层级关闭：显式app/node严格类型检查均0；定向5 files/67 tests通过；ESLint、Prettier、独立Vite构建均0。实际源码探针确认四层过滤、More单次预留/仅More分支、A/B回执原因完整去重；真实VXE宽窄操作列、键盘ArrowDown展开More及中文PC/PDA/移动端标签已复核。报告 `TestResults/pf02-menu-independent-20260904/round2-review.md` 与 round2 日志/截图/探针。

**菜单整体仍部分完成**：默认导入真实HTTP/409、全Skipped/Blocked/取消计数、四级权限叶子真实VXE、完整中英亮暗尺寸及真正200%等证据待补；不关闭旧精确清理/E2E残留或整包矩阵。继续交原开发按已定范围补足隔离夹具/验证材料，不向正常库写数据。此轮没有新增后端验证，不冒用上一轮44项结果为本轮新结果。

CUA已恢复；旧tab4已不在列表，本轮tab5已关闭、viewport已reset，上一轮浏览器清理阻塞解除。自有夹具PID9116结束、4189无监听；用户5173/PID49096保留。未改生产/测试源码或黄金页，未提交推送。

## 菜单用户实测返修隔离验收补充（2026-09-04）

按第二轮独立复验要求补齐只读/内存隔离材料。新增 `src/frontend/tests/visual-systemdata/server.mjs` 导航场景夹具（`no-add`、`add-skipped`、`mixed`、`conflict`、`validation`）及四层导航/Action 叶子 fixture；默认导入成功仅推进夹具内存修订号，Blocked 返回 422，Conflict 返回 409，验证接口返回完整资源→模块→manifest→受信回执链路。夹具 status 将 fixture operation 与 blocked write 分开统计，未连接正常服务或生产数据库。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `node --check tests/visual-systemdata/server.mjs` | 0 | 夹具语法通过 |
| `vue-tsc --noEmit --pretty false -p tsconfig.app.json` | 0 | 实际 app 项目配置通过，0 diagnostics |
| 隔离夹具 ESLint / Prettier check | 0 | `server.mjs`、fixture、独立 E2E 通过 |
| Playwright 独立新增场景（4187，自带临时 HTTP 夹具） | 0 | 1 passed；覆盖全 Skipped/零 Add、Add+Skipped、Blocked 整体拒绝、取消后零写入、409 修订冲突、四级 Action 叶子/祖先搜索、完整验证回执原因；断言 `writes=0`、`unknown=0`，四类 fixture operation 各按预期记录 1 次 |

本次浏览器日志仍有既存 Vue reactive-component warning；该场景最终通过。此补充仍不等价于真实账号/租户持久化、完整中英亮暗尺寸/200%视觉矩阵或三端 runtime 外部依赖门禁；这些范围继续由独立验收任务裁决。用户 5173 服务与正常库未触碰，未提交推送。

## 菜单用户实测返修第三轮隔离场景独立复验（2026-09-04）

同执行 ID，新增隔离场景通过，未发现新的功能缺陷，第一轮五项返修维持关闭。已通过CUA独立验证全Skipped/零Add仍显示、取消只有preview GET且无import、混合Blocked禁用及模拟422、模拟HTTP409保留中文预览、内存成功revision13、真实VXE四级Action叶子/祖先搜索、展示预览排除权限叶子、完整中文回执原因与红色失败tokens。node语法、实际app类型检查和定向diff check均0。报告 `TestResults/pf02-menu-independent-20260904/round3-review.md`，原始DOM/截图/API计数同目录。

夹具成功仅推进内存修订号，不追加真实节点，status.writes只统计blocked-write；因此本轮只补UI/模拟HTTP层，不宣称实际SystemData/Identity、节点持久化与发布回滚、完整中英亮暗尺寸/真正200%、三端外部门禁或旧精确清理/E2E残留通过。自有PID8480、4189和IAB tab6均清理，viewport恢复；未启停用户服务。菜单整体验收仍部分完成，保留真实矩阵限制。

## 菜单发布真实链返修交接（执行 ID：PF02-20260904-体验与菜单配置整改 · 菜单发布真实链返修）

本轮针对 SystemData 资源版本漂移与菜单发布竞态完成返修，已停止编辑，未切换分支、未同步/提交/推送、未启动或停止正常业务服务，未写入生产数据库。

- SDM-018：新增幂等的受信内置资源一致性种子；只将已知 SystemData 内置资源从旧 manifest 版本重绑定到当前版本，保留资源 ID、路由、权限、终端、状态及导航历史，不改旧 ledger、不自动授予角色。
- 初始化 readiness：必需内置资源必须与当前 manifest 版本一致，避免 stale resource 被错误报告为 Ready。
- 控制面：同模块升级支持稳定资源 ID 的版本重绑定与新增资源；拒绝跨模块资源冲突、同版本 checksum/权限声明冲突、权限身份变化及省略仍被草稿/历史导航使用的资源；验证时对页面与 Action 资源逐项检查 manifest、受信回执、权限声明和受信资源声明。
- 前端发布链：保留服务端 draft revision；校验响应按请求代次和 draft revision 防旧响应覆盖；发布/回滚按服务端返回 revision 刷新 runtime，并处理 in-flight refresh 与 revision 降级；未注册路由不会进入运行时导航，管理页保留不可用原因提示。

| 命令/范围 | 退出码 | 本轮结果 |
| --- | ---: | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | 0 | fresh Release；0 warning / 0 error |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | 0 | 1393 通过 / 0 失败 / 3 跳过，总计 1396；SystemData 567、Identity 607、BuildingBlocks 158、Gateway 14、ReferenceData 16、UnifiedHost 20、Integration 11 通过 + 3 跳过 |
| SystemData 返修定向测试 | 0 | 9 项通过；含真实临时 SQLite、实际 schema migration、实际 `SqlControlPlaneStore` 跨 DbContext 持久化读回、发布与资源版本修复 |
| 前端 `pnpm.cmd test:unit` | 0 | 104 文件 / 780 tests 通过 |
| 前端 `pnpm.cmd typecheck` | 0 | 通过 |
| 前端整改范围 ESLint / Prettier | 0 | 定向范围通过；全项目 format check 仍受工作树既有 91 个无关格式告警影响 |
| 前端 `pnpm.cmd build` | 0 | 2472 modules；构建通过，仅既有 chunk 体积提示 |
| 隔离 Playwright SystemData visual fixture | 1 | 8 项中 5 通过、3 失败；失败定位为既有组织操作图标断言、受限高度 1px 边界、主题错误态容器断言，未涉及本轮菜单发布/资源版本链路 |
| `git diff --check` | 0 | 空白检查通过；工作树仍保留用户既有修改，未声明 clean |

本轮新增的是 SQLite 真实持久化证据；PostgreSQL/Redis/RabbitMQ 真实依赖仍因环境保持 3 项 Skip，未将 SQLite、内存 fixture 或隔离浏览器结果冒充为外部依赖/真实账号租户验收。现交回同执行 ID 的独立验收任务复核。

## 启动顺序小修（2026-09-04，主控直接处理）

用户重编译后报告 `SystemData control-plane revision changed; reload before retrying.`，并明确要求主控直接小修，原两任务已停止此项编辑/构建。取证发现 `Program.cs` 注释要求初始化协调器最先启动，但实际注册在模块循环之后；SystemData 权限对账后台服务会立即读写同一控制面 revision，存在抢先于初始化提交的窗口。本次未取得用户 VS 异常完整调用栈，不能把隔离复现等同于用户 PostgreSQL 现场已恢复。

- 生产代码只移动 `ModuleMigrationCoordinatorHostedService` 的一条注册语句到模块循环之前；未改 SQL CAS、事务、种子账本规则、权限校验或原有受信注册改动。
- 回归使用真实托管服务注册和临时 SQLite：强化现有顺序断言；新增旧 SDM-017 资源 v1、未执行 SDM-018 的升级及连续两次启动，确认资源回到 v2 且 SDM-018 仅一条。测试工厂显式选择 SQLite，不读取正常用户库配置。模拟旧账本的修改/删除仅发生在本测试自建临时库。
- 修复前 fresh Release 0 warning/error 后，顺序断言失败：期望初始化协调器第一，实际为 `EventBusConsumerBackgroundService`；报告 `TestResults/pf02-startup-order-20260904/startup-order-before.trx`。这复现了注册顺序缺陷，不是用户异常堆栈复现。
- 修复后 `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` 退出0，0 warning/error；两项 `RealHostedServices` 定向测试 2/2 通过，报告同目录 `startup-order-after.trx`。
- 随后 `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` 退出0：1394 通过、0 失败、3 跳过（SystemData 567、Identity 607、BuildingBlocks 158、Gateway 14、ReferenceData 16、UnifiedHost 21、Integration 11+3 skip）。本次显式设置 SQLite 模式并关闭三项真实外部联调 gate；外部未运行/早退不计作 PostgreSQL、Redis、RabbitMQ 真实验收。TRX 同上述目录。
- 未停止、重启或接管用户 IDE/Vite/Host/数据库，未运行正常用户库初始化、未提交推送；既有菜单真实链、3项视觉失败、精确测试清理和 PF-02 整包待办仍保留。本次仅交付已验证的启动顺序代码修复。

## 发布后菜单图标小修（2026-09-04，主控直接处理）

- 用户截图与现有 Chrome 用户组页面均显示已发布导航统一为四宫格。根因：默认导入未填写可选 iconKey；后端保存/发布/runtime 正常透传 null，而前端仅将空值回退 Menu，未复用静态导航图标，且白名单缺少 Avatar/Lock/UserFilled。
- 仅修改 `src/frontend/src/systemData/runtime/navigation.ts` 和对应 unit test：空白图标按既有 PC 导航的稳定 routeName/一级组ID取图标；只读取图标，不恢复默认树、不改标签/父级/排序/权限。显式图标优先，补齐已有图标，兼容大小写/空白/连字符；未知键（含 constructor）安全回退 Menu。没有新增图标库、后端清单、数据库补丁或重新导入/发布操作；已发布空图标与以后默认导入均走同一兼容逻辑。
- 新增回归修前 7失败/7通过，修后图标/运行时/管理store 23通过；最终导航、侧栏与发布刷新定向6文件60测试全部通过。实际 `vue-tsc --noEmit --pretty false -p tsconfig.app.json`、定向 ESLint/Prettier、Vite生产build及diff check退出0；build保留既有大chunk提示。初次测试/类型缓存EPERM经受支持审批后运行，没有改全局权限。
- 浏览器只读实拍见图标已能区分，但同时出现后端运行策略降级，主题位置显示为静态默认分组，故该截图不能证明已发布快照重新加载成功；自定义主题父级不变由适配器回归覆盖，真实用户发布加载验收仍待后端可用。未启停用户服务、未写库、未改变模型设置、未提交推送。原管理页面/共享表格与整包未完成项不受本次关闭范围影响。

## 提交前隔离测试复核（2026-09-04，主控直接处理）

- 用户报告首项找不到组织树、后续 7 项连接拒绝。原 `recheck-systemdata-20260904` 首项 error-context 同时显示“管理接口不可用 / 网络不可用”，不是组织树业务缺陷的证据；后续 `ERR_CONNECTION_REFUSED` 为夹具不可用的级联失败。没有旧服务退出日志，无法确认当时具体由谁或何原因结束进程。
- 修改前由 Playwright 新启自有 4187 夹具（PID 17656），组织树可加载、选中并展示岗位；完整运行 5 通过 / 3 失败，服务直到测试结束才退出。失败是既有测试与当前契约不同步：详情区新建岗位是文字操作；折叠的菜单表内容能完全放下；页壳错误时保留内容及上下文。
- 只改独立 Playwright 配置和该 E2E 脚本：禁止复用外部夹具（内存 revision/请求计数必须属于本轮），无重试也保留失败 trace；岗位改验可点击打开/关闭表单；菜单先验证折叠时页底可见、再展开真实节点检验滚轮和 End 到底，保留原溢出阈值；错误态验证内容保留、重试确实发出 GET 并得到夹具 503；主题正常态验证中文复选项及其选中值，排除原英文 palette key。没有修改产品页面、共享 AppDataTable、黄金页、API 或权限实现。
- 修后两次独立新进程执行 `node node_modules/@playwright/test/cli.js test --config playwright.systemdata-visual.config.ts --output test-results/pf02-fixture-{repair|confirm}-20260904`：均退出 0，分别 **8/8 通过（35.0s、33.7s）**，无重试。第二次服务 PID 65940，status 200 后开跑，全部结束后由 Playwright 释放；不是复用上次存活进程。定向 TypeScript strict noEmit、ESLint、Prettier check 均退出 0。
- 日志：`TestResults/pf02-fixture-diagnosis-20260904.log`（修前）、`TestResults/pf02-fixture-repair-20260904.log`、`TestResults/pf02-fixture-confirm-20260904.log`。截图在对应 `src/frontend/test-results/` 子目录；实际查看组织选中态、展开菜单滚动到底及主题中文截图。日志仍有既有 Vue reactive-component 与 Element Plus checkbox label 弃用警告，未将测试通过写成控制台零警告。
- 本轮关闭上述隔离测试失败，不代表真实账号/用户库发布持久化、完整视觉矩阵、PostgreSQL/Redis/RabbitMQ 或 PF-02 整包验收。未重编译后端（无生产源码变动）、未改用户调试服务/正常库、未提交推送；报告和截图是忽略的可重建产物，不纳入提交。

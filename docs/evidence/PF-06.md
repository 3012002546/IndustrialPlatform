# PF-06 阶段证据

## 2026-09-14 当前结论：用户复测通过，专项收尾待补

用户在合并代码验收后确认“测试均没有问题”。本次浮窗缩放后拖动保持尺寸、Edge→Chrome接通后的旧错误提示清理记为用户现场复测PASS；此前双向发起、真实屏幕/语音及共存可用结论保留。该确认不追溯修改[合并独立验收](PF06-resize-stale-error-20260914.json)当时工具不可用/NOT RUN的事实，而作为后续用户实测证据补充。

用户随后确认媒体停止已测试：一端断开，另一端也会断开；按该实际行为记PASS并关闭本轮停止复验，不把此记录写成已采集分轨计数或精确时限。用户明确安排：网络及质量稳定性待后续项目云Docker部署验证，异常场景待Electron/Capacitor验证，终端完成后再验PC/PDA/Mobile。当前无已知未关闭生产缺陷，本轮开发和用户功能验收完成；后续专项为用户指定延期，非当前返工阻断。具体TASK映射及数据库/最终集成边界见[当前工作包](../tasks/active/PF-06.md)。本次仅文档与协作复盘，不复跑测试、不重查云环境、不修改生产代码。

下方为历史证据。旧独立浏览器popup方案已被页内悬浮观看窗口替代；黑屏未解决、全部真实媒体MISSING、两项UI NOT RUN等历史结论已由后续代码证据和用户复测覆盖，不据这些旧结论重新派遣返工。

## 2026-09-13 最新：截图反馈、独立媒体窗口及完整操作

- 代码修复：左侧会话紧邻排列；终态活动条与结束按钮隐藏且状态本地化；独立浏览器媒体窗口复用同一store/SignalR/PC，用户点击同步开窗，选屏调用窗口自身原生API；呼叫/接听接入麦克风，语音就绪不再仅以ontrack判定；最小化继续，关闭窗口结束全部，分项结束保持独立。
- 原验收集中确认三项缺陷后一次修复：失败邀请回收本次新mic；取消/拒绝选屏采用屏幕专用错误；audio/video按kind分流，popup打开时仅popup音频可听，Host静音且不误报就绪，按track-id变化重绑而非每250ms重播。列表静态质疑被真实几何证据撤回，没有额外布局返修。
- 验证：开发前端build PASS，38相关测试PASS；原验收仅只读复核增量并记录hash，未重复跑测试。见[分层证据JSON](PF06-media-window-20260913.json)。首次页面中的列表间隙4px、终态条空、真实popup及本地audio live成立，但不等于远端播放；脚本等待异常中断不判产品失败。最终续验时test与既有e2e.admin登录请求均超时，未进入媒体步骤，停止重登并询问用户VS是否暂停。
- 当前结论PARTIAL：真实双向声音、屏幕选择及远端画面、分项停止未验证；不扩大为功能可用。测试浏览器已关闭，未新增Host/账户/角色/云配置变更。先恢复现有调试请求响应，再续尚未完成的媒体步骤；不重跑已通过布局/邀请/底层测试。
- 协作修订已回写统一约定§2：双方用户可见结果和终止行为先行；区分代码、接口、真实业务证据；偏差时短复述后继续，边界落在同一工作包，不以局部PASS缩小原目标。

## 2026-09-13：聊天消息查询取消链定点修复

- 用户报告 GetMessagesAsync → ToMessageDtoAsync → 身份/用户组角色查询抛出 OperationCanceledException。代码确认消息列表逐条读取完整身份角色和租户处置记录，形成重复数据库往返；该堆栈本身不能证明具体取消来源或请求耗时。
- 原开发在 CollaborationService 复用既有 GetDisplayNamesAsync：每页去重发送者批量查询一次，租户 active message dispositions 读取一次；单条消息展示也不再装配角色。保持消息顺序、租户边界、找不到显示名称时回退、撤回/处置内容及附件隐藏、CancellationToken 传播。未修改认证授权、超时、缓存或云配置。
- 开发验证：修正编译分析器 CA1859 后，新鲜 `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` 成功，0 errors；随后 Collaboration.Tests 的 `ComplianceCommandExecutionTests.Message_page` / `Retracted_message` 过滤回归 3/3 PASS，`git diff --check` 通过。主控复用结果，未重复编译或全量测试。
- 运行边界：未重启用户 VS/Host，运行实例需要重新加载代码；无本次真实请求耗时证据，不能声称所有取消异常已经消失。前一轮保留的主控 Host PID 44716/launcher 13000 已在用户启动冲突反馈后按进程身份核对并关闭，5041 已释放；不再沿用下方“Host 保留”的历史状态。

## 2026-09-13 最新：三入口邀请与撤回定点修复

- 复用现有云 Docker 配置、用户提供的测试账号和既有 E2E Admin 测试对象；未新建账号、改密、改角色或变更云容器。凭据未写入脚本或证据。
- 首个阻断：Development 私有配置未开启 ScreenEnabled/VoiceEnabled，且初始邀请失败没有可见提示。开关在唯一私有配置启用；三入口前置失败、Hub 错误及调用异常显示本地化错误和错误码。原冻结补丁 3 文件/36 测试及类型检查通过，独立定点验收 PASS。
- 实际页面新增发现：Pending/Ringing 尚无 mediaContext，“结束全部”只清本地 DTO，没有调用结束 Hub，后续邀请返回 MEDIA_BUSY。增量修复在无 context 时按邀请 ID 调用 EndScreenShare/EndVoiceCall；已有 context 保持原 EndAllMedia。单文件 10 测试及类型检查通过；独立只读增量验收 PASS，未再次执行整套测试。
- 冻结增量：collaborationMediaStore.ts SHA256 `6ace4337f36bf822c4b1ca0a02a15c3c6cf7bafdcd92c02bd6f3ec0525cbaddc`；对应 spec SHA256 `3bbfa679676c9508cd8e80d265a29ea0f2b9126a09dc4a08cd3ad66cc90a8c1c`。
- 主控刷新一次加载冻结代码后，在同一测试登录中连续执行语音、共享我的屏幕、请求对方共享，每项均实际取得邀请 Hub 与结束 Hub 的 ok=true，三组页面 alerts 均为空。以服务端调用完成为依据，未把本地邀请条消失当作撤回证据。详见 [脱敏运行记录](PF06-three-entry-20260913.json)。修复前以及热更新仍保留旧实例的尝试不计通过。
- 边界：对端接听、音频采集/播放、屏幕视频、媒体停止证明、TURN 与质量本轮未验证，整体 PARTIAL；待接受邀请撤回不能替代媒体停止验收。测试 Edge 进程已关闭，原 Host/Vite 保留，无提交、推送或部署。本轮收口，不继续重复环境检查或扩大测试。

日期：2026-09-12（Asia/Taipei）  
状态：冻结代码与三项定向修复已独立通过；管理员动态聊天入口与临时身份清理已取得证据，整包仍为 PARTIAL。  
真实媒体验证结论：G06-1、G06-3 仍为 MISSING，不能据本文件宣布 PF06 或真实双端支持通过。

## 交付快照

- 工作树：`D:\Code\Industrial Platform\IndustrialPlatform`
- 分支：`develop`
- 基线 HEAD：`0a77597f94e38873b67565e0b477e120e72a57f8`
- 上游关系：`git rev-list --left-right --count HEAD...@{upstream}` = `1 0`；本轮未提交、未推送。
- 变更清单复核：`git status --short`；主控既有文档 WIP 与本轮 PF06 生产源码/测试/证据均保留在工作树，未执行 reset、clean、stash 或部署。
- 版本复核方法：以基线 HEAD、`git diff --name-status`、`git diff --check` 和本文件命令输出复核；未把私有配置值或私有配置文件哈希写入证据。

## 本地自动化门禁

| 命令 | 结果 |
| --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release --no-restore` | PASS，0 warnings，0 errors |
| `dotnet test tests/Collaboration/IndustrialPlatform.Collaboration.Tests/IndustrialPlatform.Collaboration.Tests.csproj --configuration Release --no-build` | PASS，101 passed，0 failed，0 skipped |
| `cmd.exe /d /c pnpm.cmd build`（`src/frontend`） | PASS，`vue-tsc --build` 与 Vite build 通过；仅有 chunk size warning，无 error |
| `cmd.exe /d /c pnpm.cmd test:unit --configLoader=runner`（`src/frontend`） | PASS，130 test files，973 tests，0 failed |
| `git diff --check` | PASS；仅有 Git 行尾转换提示 |
| PF06 SQLite 初始化/物理结构测试 | PASS，既有 initializer 4/4、UTC round-trip 1/1；完整 Collaboration 回归亦通过 |
| `node --test tools/pf06-media-poc/protocol.test.mjs` | PASS，5/5；仅验证隔离协议样本，不代表生产媒体 |

## 正式业务实现覆盖

- 数据与迁移：新增 `PF06-001` 屏幕会话、`PF06-002` 语音会话与双用户 voice slot；字段、枚举、CHECK、幂等唯一约束、活动会话部分唯一索引、状态/截止时间索引已落到 SQLite/PostgreSQL DDL。语音 slot 与 Call、Outbox 在同一仓储事务中写入，结束时释放。
- 领域与应用：新增屏幕/语音状态规则、紧凑 JSON 请求哈希、CAS 状态更新、权限复核、过期/心跳终结、停止回报、审计最佳努力写入与 `collaboration.media.changed.v1` Outbox 事件。
- Hub 契约：正式 `CollaborationHub` 已提供规格列出的 15 个媒体方法，统一返回 `ok/data/error/traceId`；SDP/ICE 只经选定 ConnectionId 直发，不进入用户组状态事件或持久化事件 payload。
- 媒体上下文：单 `(tenant, conversation)` 的 `MediaContextRegistry` 记录 Low/High 端点、sid/ver/exp、能力绑定、ContextRevision 和信令单调序列；后绑定端会通知先绑定端重试首个 Offer，避免首个 PeerConnection 信令在对端尚未绑定时丢失。
- 权限：Identity 权限目录新增 `remote-assistance.session.share`、`remote-assistance.session.join`、`remote-assistance.voice.call`；媒体 Bind、Ready、KeepAlive、静音会按保存的端点会话重新评估权限，不用当前 HttpContext 冒充另一端。
- 前端：App 级唯一媒体 host、Pinia 媒体 store、三槽共享 `RTCPeerConnection`、屏幕/语音独立状态、聊天内邀请/接受/观看/静音/挂断入口、常驻条与重新绑定逻辑已接入。屏幕采集无系统声音，语音采集仅麦克风且不启用摄像头。
- 事件投影：Outbox publisher 将媒体状态安全投影为 `screen-share.changed` / `voice-call.changed`；`media.context.changed` 仅发送两端，`media.signal` 仅发送另一端，均不广播到公共用户组。
- 回归测试：新增请求哈希稳定性、Low/High 端点绑定、目标连接、重复信令和过期 ContextRevision 拒绝测试；既有 SignalR 聊天集成宿主继续通过。

## 001 / 009 隔离样本边界

`tools/pf06-media-poc/` 仍是隔离的原生 WebRTC 协议样本。样本使用同源 `BroadcastChannel` 只模拟两个同源页面之间的控制消息；`BroadcastChannel` 不能跨 origin，也不能跨设备，因此不是生产传输，也不代表正式 Hub、账号、权限、审计、TURN 或 Screego 已通过。正式业务路径使用 `CollaborationHub`/SignalR 和服务端端点绑定，不复用该 BroadcastChannel。

已完成的样本自测：固定 `audio/screenLow/screenHigh` 三槽、Low 初始 Offer、High 按 m-line 映射、单 PeerConnection、两种能力加入顺序、分项停止保留另一能力、plain-record 信令序列化；Playwright 无真实设备与 fake-media 组合检查均无页面错误。fake-media 不替代真实采集、权限、画质、网络和停止时序证据。

## TASK 与测试矩阵状态

| TASK | 本地代码/自动化状态 | 真实环境状态 |
| --- | --- | --- |
| 001 | 隔离三槽样本部分完成 | G06-1 真实 Screego/双端对照 MISSING |
| 002 | 三表、slot、迁移、约束、仓储事务已实现；SQLite 门禁通过 | 用户云PG已实际登记001/002、语音表及索引落地；完整Shared/PerService、新旧库升级矩阵仍未补齐 |
| 003 | 屏幕领域与用例、幂等/CAS、Hub 入口已实现 | 两台真实终端媒体链路 MISSING |
| 004 | 最终 15 方法、统一结果、Context/信令路由已实现 | HTTPS/WSS、真实连接碰撞 MISSING |
| 005 | 三权限、公开 Identity evaluator 适配、Outbox/审计接入已实现 | 撤权/版本变化在线复验 MISSING |
| 006 | 唯一媒体 host、聊天入口、常驻条及屏幕交互已实现 | PC 管理员真实登录与动态聊天导航已有证据；业务对端、文本与媒体交互 MISSING |
| 007 | TURN短期凭据与RelayOnly策略代码已实现 | 运行时媒体开关、TURN配置与UDP/TCP/TLS/RelayOnly连通性 MISSING |
| 008 | 屏幕开发自测与本地门禁已完成 | A01～A11 真实矩阵未通过，MISSING |
| 009 | 隔离组合样本部分完成 | G06-3 真实双端语音/停止/TURN MISSING |
| 010 | 独立语音状态、slot、静音、挂断和共享连接代码已实现 | V01～V12 真实矩阵 MISSING |
| 011 | 已派代码缺陷已完成独立定向复核并关闭 | 整包真实 A01～A11 / V01～V12 矩阵仍 MISSING |

### A01～A11 / V01～V12

当前仅有协议样本、代码构建和自动化回归证据。真实设备、真实浏览器权限、第二 PC、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、画面/声音 RTP 分轨计数、5 秒停止与 30 秒静默失联、Screego 三次对照均未登记，因此 A01～A11、V01～V12 的真实结果统一为 MISSING，不以 fake-media、同机无媒体页或仓库端口声明替代 PASS。

## 实际环境与剩余缺项

- 本轮未启动 UnifiedHost、Collaboration、Screego、coturn、PostgreSQL、Redis 或 RabbitMQ，也未重启 IDE、调试服务、Docker 或防火墙。
- 真实 HTTPS/WSS、两台实际 PC、两个同租户测试账号、第二设备、TURN 受限网络路径、固定 Screego 版本与受信证书未提供；缺项只阻塞真实验收，不阻塞已完成的正式代码实现。
- `RemoteAssistanceOptions` 的屏幕/语音功能默认关闭，未把秘密写入源码、数据库、日志或事件；实际启用前仍需由部署环境提供短期 TURN 凭据/secret 引用，并执行规格 §8 的真实矩阵。
- 仍需在独立验收阶段验证进程重启终结、权限撤回、跨聊天 voice slot 竞争、真实网络/RelayOnly、自动播放阻止、分轨停止和 30 分钟截止；本地自动化不覆盖这些声明。

### 只读本机环境盘点（2026-09-13）

- 配置/文档只给出候选入口：前端 `http://localhost:5173`，UnifiedHost/API `http://localhost:5041`，独立 Collaboration `https://localhost:59159` / `http://localhost:59160`；这些是配置引用，不是已确认可用入口。
- 本机只读端口快照未发现 `5041`、`5173`、`4173`、`5042`、`59160`、`62311`、`5341`、`5432`、`6379`、`5672` 的目标监听；该事实仅描述本机当时状态，不推断远端服务不可用。
- 脱敏配置显示 `RemoteDevelopment.Enabled=True`，并存在 PostgreSQL、Redis、RabbitMQ、Seq 和 SSH 的开发依赖引用；未由此确认测试接收端、服务进程或隔离账号已经可用。未输出主机秘密、密码、token、密钥或账号凭据。
- 配置与环境变量中未核实 `IceServerUrls`、`TurnCredentialSecret` 或 TURN/Screego 覆盖；代码默认 `RelayOnly` 不等于存在可用 TURN 服务器、凭据或 UDP/TCP/TLS 测试路径。
- Docker CLI 在本机不可用，因此未据此判断容器状态；本轮未继续探测、轮询、启动、重启或建立媒体传输。

## 历史阶段结论（初始开发快照）

本段记录独立验收开始前的初始开发快照，属于历史阶段结论，不代表当前验收状态。正式 PF06 生产代码当时已从 002 连续实现到 011 的本地可验证快照，且本地 Release build、前端 build、Collaboration 101 项回归全部通过；该结论只证明当时的代码与隔离测试门禁，不证明真实媒体质量、双端网络、TURN/Screego 对照或最终产品 Ready。已派代码缺陷的最新独立结果见文末“独立验收：三项集成修复集中复核”，整包真实矩阵仍为 MISSING；在这些真实证据出现前不发布“PF06 全功能通过”声明。

## 独立验收：首轮静态核验

验收时间：2026-09-12（Asia/Taipei）  
验收状态：**FAIL，退回原开发返修；尚未进入真实双端媒体结论。**  
验收基线：`develop`，HEAD `0a77597f94e38873b67565e0b477e120e72a57f8` 的本轮 dirty-worktree 交付。

首轮一次汇总的可独立确认问题：

1. Identity 只向 `PermissionCatalog` 增加三个权限，未向 `BootstrapSeedCatalog.Permissions` 增加条目，也未递增仍为 `1.5.0` 的种子版本；既有库不会获得权限，前端入口与服务端评估均不可用。
2. `BindMedia` 把 Pending/Ringing 也视为可绑定，且未约束为 Respond 时锁定的 ConnectionId；绑定后 `SignalMedia` 不再核对能力已 Accept，存在接听前建立端点和发送 SDP/ICE 的路径。
3. 正式前端 PeerConnection 的三个 transceiver 始终为 `inactive`；High 端回复 Offer 时没有继承 `negotiationNId`；ICE 早到时直接 `addIceCandidate`，无有界队列；Low 在对端尚未绑定时先发 Offer，失败后也无可靠回滚/重建。这些问题使正式 A/V 协商链无法按规格工作。
4. 收到屏幕/语音终态事件仅更新状态，不停止本地采集、detach sender 或移除远端播放；`EndAll` 先等待网络返回才本地释放；停止回报按角色填 `true`，但前端未实际完成相应 detach。真实停止与隐私边界不成立。
5. 未实现邀请/Setup/Heartbeat/MaxDuration 的服务扫描和 HostRestarted 终结；语音 slot 的 `expires_on` 不随通话阶段更新且没有清理入口。未接来电、进程重启或静默失联后可能长期保持活动行/slot并持续返回 BUSY。
6. End 与 KeepAlive 并发时，End 的 CAS 失败会直接返回仍活动记录，随后却移除内存 Context；双方 Voice Ready 并发时其中一方 CAS 冲突后无可靠重试；KeepAlive 单项权限失败会抛出整个请求，未做到屏幕/语音逐项独立结果。
7. `RelayOnly` 只作为响应字符串返回，前端创建 `RTCPeerConnection` 时未设置 `iceTransportPolicy: 'relay'`；实现也没有短期 TURN 凭据签发/返回路径，不能满足 RelayOnly 禁止 direct 回退的要求。
8. PF06 审计未形成可靠 Outbox→Audit 投影；当前命令内直写失败被吞掉且不重试，审计端口还忽略传入的原始发生时间。中央审计故障恢复后无法保证事实补齐、EventId/OccurredOn稳定。
9. App 媒体宿主在认证主体从一个非空用户切换到另一个非空用户时不释放旧用户媒体；全局单个 `operationGeneration` 还会让屏幕和语音的并行授权/停止互相取消。
10. 新增自动化仅覆盖请求哈希与 MediaContextRegistry 基础绑定；没有 RemoteAssistanceService、仓储并发/迁移、Hub 15方法、前端媒体 store/组件/E2E 的对应测试，因此开发侧 101/973 回归不能证明 A01～A11、V01～V12 行为。

真实验收保持原结论：两台实际PC、HTTPS/WSS、真实账号/权限、TURN、Screego、分轨停止计数、弱网和30分钟质量均为 `MISSING`；不能用本轮静态 FAIL 或现有本地自动化替代真实矩阵。返修后先按上述缺陷及影响路径复验，再决定是否具备真实双端验收条件。

## 独立验收返修：开发修复与本地复验

返修时间：2026-09-12（Asia/Taipei）  
返修基线：`develop`，HEAD `0a77597f94e38873b67565e0b477e120e72a57f8` 的 dirty-worktree 快照；未提交、未推送、未部署。

已针对首轮静态核验逐项补齐：

1. Bootstrap seed 升级为 `1.6.0`，补齐屏幕共享、会话加入、语音通话三项权限及对应 Identity 回归断言。
2. Bind 仅允许 Accepted/Connecting/Sharing（屏幕）或 Accepted/Connecting/Active（语音），并锁定 Respond/Invite 两侧的已保存 ConnectionId；Signal 需经过已绑定且处于可用状态的能力校验。
3. 正式媒体 store 启用三槽 `sendrecv` transceiver，RelayOnly 映射为 `iceTransportPolicy: relay`；High answer 复用原 `negotiationNId`，早到 ICE 按协商号有界排队后 flush，Low 协商失败执行 rollback/重试并处理 `MEDIA_BUSY`。
4. 终态事件与 EndAll 先执行本地 sender/track 停止、远端 track detach，再回报停止；屏幕与语音使用独立 generation，避免并行能力互相取消；认证主体切换会释放旧主体媒体。
5. 增加生命周期 worker 和服务扫描：启动 HostRestarted 终结、截止时间/心跳过期处理、语音 slot 过期更新与清理，并通过 CAS 冲突重读避免返回伪活动状态。
6. End、Ready、KeepAlive 增加 CAS 冲突处理；KeepAlive 按屏幕/语音能力独立返回结果。
7. 增加 RelayOnly 短期 TURN credential provider，凭据只由配置的 secret 生成，不写入源码、日志或证据。
8. 媒体事件与 `collaboration.audit.requested.v1` 在同一 Outbox 事务生成稳定 EventId，并由 publisher 按原始 `OccurredOn` 投影到审计端；去除命令内的直接审计写入路径。
9. 补充 Bootstrap、绑定规则、Hub 15 方法、TURN provider、媒体 store 合同测试；保留真实设备、双机、TURN、Screego 和质量矩阵为未执行项。

返修后本地自动化门禁：

| 命令 | 结果 |
| --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release --no-restore` | PASS，0 warnings，0 errors |
| `dotnet test tests/Collaboration/IndustrialPlatform.Collaboration.Tests/IndustrialPlatform.Collaboration.Tests.csproj --configuration Release --no-build` | PASS，113 passed，0 failed，0 skipped |
| `dotnet test tests/Identity/IndustrialPlatform.Identity.Tests/IndustrialPlatform.Identity.Tests.csproj --configuration Release --no-build` | PASS，621 passed，0 failed，0 skipped |
| `cmd.exe /d /c pnpm.cmd build`（`src/frontend`） | PASS，`vue-tsc --build` 与 Vite build 通过；仅有既存 chunk size warning，无 error |
| `cmd.exe /d /c pnpm.cmd test:unit --configLoader=runner`（`src/frontend`） | PASS，131 test files，974 tests，0 failed |
| `git diff --check` | PASS；仅有 Git 行尾转换提示 |

返修验收边界：以上结果证明返修代码、编译、自动化回归和隔离协议门禁通过，不证明真实媒体链路。两台实际 PC、真实浏览器采集权限、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、Screego 三次对照、RTP 分轨计数、5 秒停止、30 秒静默失联及 30 分钟质量仍为 `MISSING`，需由独立验收在真实环境复核。

## 独立验收：第一轮返修定向复核

复核时间：2026-09-12（Asia/Taipei）  
复核状态：**FAIL，继续退回原开发定向返修；不得把上一节开发自验门禁解释为独立验收通过。**  
复核范围：只检查首轮缺陷的修复差异及直接影响路径，未重复全量首轮扫描。

已确认 seed `1.6.0`、Accepted 后绑定校验、Signal 能力校验、前端 `iceTransportPolicy: relay`、High Answer 协商号继承、分能力 generation、认证主体切换触发释放、生命周期 worker、Voice Ready 有限 CAS 重试与 KeepAlive 分能力结果等改动已落地。仍有以下阻断缺陷：

1. `EndScreenShareAsync` 与 `EndVoiceCallAsync` 仍调用 `RequirePermissionAsync(messaging.read)`；`EndAllMediaAsync` 又顺序复用这两个方法。规格 §3.1 明确本人 End/停止回报不能被业务权限撤回、功能关闭或版本过旧阻止，因此撤权后的媒体仍可能无法在服务端终结。
2. 生命周期扫描把所有 `DeadlineOn` 到期统一写成 `MaxDuration`。Pending 屏幕应为 `Expired/InvitationTimeout`，Ringing 语音应为 `Missed/RingTimeout`；当前审计、事件和故障定位原因均错误。
3. End 的 CAS 仍只尝试一次；若与 KeepAlive 等更新冲突且重读仍为活动态，直接返回 `MEDIA_CONFLICT`，没有保证“不带 expectedVersion、终态获胜”。同时未实现规格要求的每 `(tenant,conversation)` 有界串行队列；`MediaContextRegistry.Bind` 对完全相同的重复绑定仍递增 Revision，导致另一端持有的 ContextRevision被无意义作废。
4. 本期进程内可靠审计仍把自动系统事件的空 actor 改成字符串 `SYSTEM`，且审计端口签名不能表达 `actorUserNId=null`，与规格要求 `actorKind=System、actorUserNId=null` 不符；需保留稳定 EventId 与原 `OccurredOn` 的 Outbox 重投。另经主控按规格 §1.1/§1.2/§7.2 澄清，`Collaboration:SystemData:Mode=Http` 的后台审计会因没有 HttpContext 而无法投递，但该分布式组合属于本期未支持范围，不作为 PF-06 单实例进程内交付阻断；运行配置若选择 Http，PF-06 必须保持不启用/不声明支持，不能伪造 PASS 或静默切换全局模式。
5. 正式前端 `ontrack` 只把 `event.streams[0]` 的轨加入 `remoteStream`，没有处理合法的 streamless track；当前 transceiver/`replaceTrack` 路径未显式关联 stream，接收端可能得到空 `event.streams`，造成实际画面/声音未挂载。屏幕 `MediaReady` 还在 `ontrack` 回调立即上报，而不是 Viewer 观察到真实首帧，可能把无画面会话提前推进 Sharing/启动 30 分钟期限。
6. 返修说明称早到 ICE“有界排队”，但实现只是向 `Map` 数组持续追加，没有 128 条上限或 10 秒清理；存在 remoteDescription 时也不核对 candidate 的 negotiationNId，旧轮候选可注入当前轮。V08 的旧 ICE/无限队列边界仍未实现。
7. Low 虽创建了三个槽，但三个槽固定 `sendrecv`，未按能力/角色切换为 audio sendrecv、共享槽 sendonly/recvonly、非共享槽 inactive；High 在首个 Offer 前获得的本地屏幕轨没有待接入缓存，`screenSender()` 此时为 null，`replaceTrack` 被静默跳过，随后收到 Offer 也不会补挂该轨。正式共享链仍存在确定的时序丢轨路径。
8. 停止回报仍按角色硬编码 `true`，没有依据 `replaceTrack(null)`、捕获轨 `readyState` 与播放解绑的实际结果；清理也先 await sender detach 再 stop 源轨，未按规格用逐资源 `try/finally` 保证本地采集立即停止。语音轨未监听设备拔出/权限撤销的 `ended`，自动播放失败也无 `PlaybackBlocked`/用户恢复路径。现有单元测试未证明 V03～V07 的真实分轨停止断言。

真实验收结论不变：A01～A11、V01～V12 所需两台实际 PC、真实权限、HTTPS/WSS、TURN UDP/TCP/TLS、RelayOnly、Screego 对照、逐 MID/SSRC 计数、5 秒停止、30 秒失联与 30 分钟质量证据仍为 `MISSING`。以上静态 FAIL 修复前不进入真实双端媒体 PASS 判定。

## 独立验收第二轮定向返修：开发修复与本地复验

返修时间：2026-09-12（Asia/Taipei）  
返修基线：同一 `develop` dirty-worktree 快照，HEAD `0a77597f94e38873b67565e0b477e120e72a57f8`；未提交、未推送、未部署。

本轮仅针对上一节 8 项阻断缺陷收口：

1. 本人 End/EndAll 不再复核 `messaging.read`，终结写入改为最多 5 次读取最新版本并 CAS 重试，终态在 KeepAlive 等并发更新下获胜；生命周期 Pending/Ringing 分别固定为 `InvitationTimeout`/`RingTimeout`，活动态截止仍为 `MaxDuration`，心跳失联为 `HeartbeatTimeout`。
2. BindMedia 增加 `(tenant, conversation)` 进程内串行门；MediaContextRegistry 对相同 Connection/Session/Auth/Token/能力组合直接幂等返回，不递增 ContextRevision。新增重复 Bind 与生命周期原因定向断言。
3. UnifiedHost 进程内 `SystemDataAuditPort` 接受 Outbox 原始 `auditEventNId` 与 `OccurredOn`，系统事件保持 `actorUserNId=null`，审计 payload 标记 `actorKind=System`。`HttpSystemDataAuditPort` 的系统 actor 路径明确返回 `COLLAB_PF06_AUDIT_HTTP_UNSUPPORTED`；本期不启用、不声明分布式 HTTP PF06 支持，不新增跨服务身份体系。
4. 前端 ontrack 不再依赖 `event.streams[0]`，streamless track 也会挂载并监听 ended；屏幕 MediaReady 改为 Viewer 视频实际触发 `playing` 后才发送。全局 host 在 Pending/Ringing 即可接听/拒绝，不只在 Active 显示。
5. ICE 候选按 negotiationNId 隔离，队列最多 128 条、10 秒过期；进入新 Offer/Answer 时清理旧协商候选，存在当前远端描述时拒绝旧轮候选。
6. 三槽方向按能力与角色配置：audio 无语音时 inactive；共享者自己的 video 槽 sendonly、对端对应槽 recvonly，非共享槽 inactive。High 端也预建三槽，首个 Offer 前已有的本地屏幕轨会挂入对应 sender，不再静默丢失。
7. 停止流程先停止捕获轨，再尝试 sender detach，并用 finally 清理引用；停止回报使用实际 sender/capture/playback 结果。语音捕获轨 ended 自动走结束路径；audio/video 播放失败置 `playbackBlocked`，提供用户 Resume playback 恢复入口。

第二轮本地自动化门禁：

| 命令 | 结果 |
| --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release --no-restore` | PASS，0 warnings，0 errors |
| `dotnet test tests/Collaboration/IndustrialPlatform.Collaboration.Tests/IndustrialPlatform.Collaboration.Tests.csproj --configuration Release --no-build` | PASS，121 passed，0 failed，0 skipped |
| `dotnet test tests/Identity/IndustrialPlatform.Identity.Tests/IndustrialPlatform.Identity.Tests.csproj --configuration Release --no-build` | PASS，621 passed，0 failed，0 skipped |
| `cmd.exe /d /c pnpm.cmd build`（`src/frontend`） | PASS，`vue-tsc --build` 与 Vite build 通过；仅有 chunk size warning，无 error |
| `cmd.exe /d /c pnpm.cmd test:unit --configLoader=runner`（`src/frontend`） | PASS，131 test files，974 tests，0 failed |
| 定向 Collaboration 测试（RemoteAssistance 与 HTTP audit scope） | PASS，16 passed，0 failed |
| `git diff --check` | PASS；仅有 Git 行尾转换提示 |

本轮仍未执行真实双机、真实浏览器首帧/自动播放、真实设备拔出、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、Screego、RTP 分轨计数、5 秒停止、30 秒静默失联及 30 分钟质量矩阵；这些项目继续保持 `MISSING`，不得由本地自动化替代。HTTP 分布式 SystemData 模式同样是本期未支持范围；仅 UnifiedHost 进程内 SystemData 模式可进入 PF06 本地审计链验证。

## 独立验收：第二轮返修冻结快照复核

复核时间：2026-09-12（Asia/Taipei）  
快照状态：**FAIL；静态门禁 PASS 不足以覆盖下列规格阻断，真实媒体矩阵继续 MISSING。**  
协作约束更正：统一约定仅要求“同一问题两轮没有新证据时由主控判别环境、需求或能力原因”，不存在“两轮返修上限”。本轮已确认多项新修复并留下 4 个明确实现问题，因此开发可继续定点修复，验收等待新的冻结快照后再复验。

快照边界：本节对应开发任务第二轮稳定 handoff 时的 `develop` dirty-worktree，HEAD `0a77597f94e38873b67565e0b477e120e72a57f8`。验收在该稳定时点完成代码抽查与下列门禁，但当时未生成或记录 WIP manifest/hash；开发恢复修改后，当前工作树可能已经不同。本节结果只描述当时快照，不能覆盖或证明继续修改中的当前源码；下一次验收必须等待新的稳定 handoff，并记录可复核的 WIP manifest/hash。

独立重跑结果：

| 命令 | 结果 |
| --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | PASS，0 warnings，0 errors；本次为新鲜 Release build |
| `dotnet test tests/Collaboration/IndustrialPlatform.Collaboration.Tests/IndustrialPlatform.Collaboration.Tests.csproj --configuration Release --no-build` | PASS，121 passed，0 failed，0 skipped |
| `cmd.exe /d /c pnpm.cmd build`（`src/frontend`） | PASS；仅 chunk size warning |
| `cmd.exe /d /c pnpm.cmd test:unit --configLoader=runner`（`src/frontend`） | PASS，131 test files，974 tests，0 failed |
| `git diff --check` | PASS；仅行尾转换提示 |

第二轮已独立确认修复：本人 End 不再复核 `messaging.read`；生命周期原因码正确；End CAS 有限重试；完全相同 Bind 不增 Revision；进程内审计保留原 EventId/OccurredOn 与 null 系统 actor；streamless track、视频 `playing` 后 Ready、全局来电入口、实际停止结果、语音 track ended、PlaybackBlocked 恢复和 ICE 协商号隔离均已落地。

仍未修复的阻断：

1. `RemoteAssistanceService` 只有 `BindMediaAsync` 使用 `_bindGates`，Invite/Respond/Signal/Ready/KeepAlive/End/EndAll 并未进入同一 `(tenant,conversation)` 串行段；该 `SemaphoreSlim.WaitAsync` 没有队列容量/BUSY 快速失败，字典条目也不回收，并且在验证 conversation 前按请求字符串建 key。它不满足规格 §3.1 的“所有媒体 Context 操作有界串行队列”，还允许认证客户端用随机 conversationNId 持续增长 gate 字典。最小修复范围是把整个 Context 操作入口统一纳入可回收、容量有界的会话执行器，满时返回 `MEDIA_BUSY`，而不是继续增加 Bind 专用锁。
2. `EndAllMediaAsync` 仍顺序调用 `EndScreenShareAsync`、`EndVoiceCallAsync`，仓储没有锁定两能力并同事务终结的原子方法。第一项提交后第二项若冲突/失败会产生部分 EndAll，违反规格 §3.3。最小修复范围是仓储增加按请求两个具体 ID 锁定、终结、释放 voice slots、写两个 Outbox 的单事务操作，并让单项 End 与 EndAll 共用终态合并规则。
3. 前端 `ensurePeerConnection` 在已有 PC 时立即返回，只在首次创建时设置 audio/video transceiver 方向。因而“先屏幕后加语音”会让 audio 永远保持 `inactive`，“先语音后加屏幕”会让两个 video 永远保持 `inactive`；`replaceTrack` 与重新 Offer 不会自动改变该方向，V01/V02 的组合顺序仍不可用。实现还让 High 主动 `addTransceiver`，与规格 §5.1“仅 Low 建三槽、High 收到首 Offer 后按 m-line 映射”不一致。最小修复范围是在每次 Binding/能力变化时对既有三个槽重新计算方向并协商，High 等待远端 Offer 后映射槽，同时保留首 Offer 前待接入轨。
4. ICE 的 10 秒期限只在后续收到信令或 flush 时惰性 `pruneIce()`，没有计时触发、没有在 10 秒未完成时失败当前新增能力；无后续事件时过期条目仍无限期留在 Map。当前测试只用源码字符串断言 `expiresAt`/`queue.length`，没有时钟驱动行为测试。最小修复范围是每轮协商拥有可取消的 10 秒计时器，超时清除候选并按规格只失败当前新增能力（或按实际传输状态终结全部），并用 fake timer 验证清理/状态迁移。

该冻结快照的真实验收没有可升级结果：A01～A11、V01～V12 仍缺两台实际 PC、真实权限与设备、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、Screego 对照、逐 MID/SSRC 计数、5 秒真实停止、30 秒静默失联和 30 分钟质量证据，统一为 `MISSING`。该次验收未提交、未推送、未部署，也未改动生产源代码；验收任务只追加本证据段。后续源码变化不得回写成本节已验证结果，应在新冻结快照下另行追加复验记录。

## 开发修复与自测：第二轮返修后续四项定点修复冻结快照

冻结时间：2026-09-12（Asia/Taipei）  
冻结边界：同一 `develop` dirty-worktree，HEAD `0a77597f94e38873b67565e0b477e120e72a57f8`；未提交、未推送、未部署。WIP manifest 共 64 项，SHA-256：`35a626fc09c339ed796e7bc96414593ea5369438368bd6f1be838ee5b9b873e3`。

针对上一节 4 项阻断完成定点修复：

1. 新增 singleton `MediaContextCoordinator`，以 `(tenant,conversation)` 复合键串行所有媒体 Context 状态变更；等待者总量和 Context 条目均有上限，满载快速返回 `MEDIA_BUSY`，租约释放后回收条目。生命周期扫描也进入同一协调器。
2. `EndAllMediaAsync` 改为仓储单事务双 ID 操作：精确读取并 CAS 终结屏幕/语音记录，删除 voice slots，并在同一事务写入对应 Outbox；任一事件序列化/写入失败整体回滚，旧会话 ID 不会误伤新会话。
3. WebRTC 仅由 Low 初建三槽；High 等待首个 Offer 后按 m-line 接入。既有 PeerConnection 在能力变化时同步三槽方向、补挂本地轨并重新 Offer；保留首 Offer 前本地轨。
4. ICE 候选队列抽为可测试组件：每轮协商有可取消的 10 秒 timer，过期主动清理并回调当前协商失败；新协商会取消旧 timer，队列总量有界。新增 fake-timer 行为测试覆盖“无后续消息超时”和“旧 timer 不影响新协商”。

新增/定点行为证据：

| 验证 | 结果 |
| --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | PASS，0 warnings，0 errors |
| `dotnet test src/backend/IndustrialPlatform.slnx --configuration Release --no-build` | PASS；BuildingBlocks 168、Collaboration 123、Gateway 14、Integration 12（7 skipped）、Identity 621、ReferenceData 258、SystemData 633、UnifiedHost 22；0 failed |
| Collaboration RemoteAssistance 定点测试 | PASS，23 passed，0 failed |
| EndAll SQLite 行为测试 | PASS；双会话/slots/Outbox 成功路径、首个事件写入失败整体回滚、旧 ID 与新会话隔离 |
| `node_modules\\.bin\\vitest.cmd run tests/unit/collaborationMediaStore.spec.ts` | PASS，1 file，3 tests |
| `pnpm.cmd test:unit`（`src/frontend`） | PASS，131 files，976 tests |
| `pnpm.cmd build`（`src/frontend`） | PASS；仅既存 chunk size warning，无 error |
| `git diff --check` | PASS；仅 Git 行尾转换提示 |

真实验收边界未改变：A01～A11、V01～V12 仍缺两台实际 PC、真实浏览器权限与设备、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、Screego 对照、RTP 分轨计数、5 秒停止、30 秒静默失联及 30 分钟质量矩阵；这些项目继续保持 `MISSING`，不得用本地自动化替代。该冻结快照现已交独立验收任务复核。

## 独立验收：后续四项冻结快照集中复核

复核时间：2026-09-12（Asia/Taipei）  
复核对象：HEAD `0a77597f94e38873b67565e0b477e120e72a57f8` 的 dirty-worktree；64 项路径/状态清单规范化 SHA-256 为 `35a626fc09c339ed796e7bc96414593ea5369438368bd6f1be838ee5b9b873e3`。  
内容边界：开发在其最终本地门禁完成后补采 `docs/evidence/PF-06-wip-content-sha256.txt`，覆盖排除交接/状态文档后的 54 个源码、测试、配置与 PF06 POC 路径。独立验收逐文件重算结果为 54/54 匹配、0 hash 差异、0 覆盖差异；该清单证明当前复核字节边界，不倒推为测试前采集。`docs/evidence/PF-06-wip-manifest.txt` 中一处 `详细` 路径 octal 转义录入错误已由验收只修正文本文档，修正后 64 行规范化 hash 与 `35a626fc...` 一致，不改变冻结源码。

必要定向复验：

| 验证 | 独立结果 |
| --- | --- |
| Collaboration `Application_RemoteAssistanceTests` + `Infrastructure_RemoteAssistanceEndAllTests` | PASS，22 passed，0 failed；验证协调器容量/串行/回收与 SQLite EndAll 原子回滚/slots/Outbox/精确旧 ID |
| `collaborationMediaStore.spec.ts` | 首次启动因既有 `node_modules/.vite-temp` 文件锁 EPERM，非断言失败；改用 `--configLoader=runner` 后 PASS，1 file / 3 tests |
| 冻结内容复核后再次逐文件 SHA-256 | PASS，54/54，0 mismatch |

已关闭：

1. **EndAll 单事务已关闭。** `EndAllMediaAsync` 在同一 Context lease 内调用仓储 `EndAllAsync`；SQLite 行为测试确认双会话、voice slots 与两条 Outbox 同事务提交，事件写入失败整体回滚，并且旧 ID 不影响替代会话。
2. **有界协调器与动态方向的基础机制已建立。** `MediaContextCoordinator` 具备 256 Context、每 Context 32 个持有/等待者、5 秒等待上限及空闲回收；主要状态入口和生命周期已接入。Low 初建三槽、High 在首 Offer 后映射，以及已有 PC 按新 Binding 刷新方向/补挂轨的主路径已经实现。
3. **ICE 主动计时基础组件已建立。** `MediaIceQueue` 有 128 总量上限、每轮可取消 10 秒 timer、旧轮 timer 隔离，fake-timer 测试证明无后续消息也会触发过期回调。

仍未关闭的同项集成问题：

1. **协调器未消除 Bind 的检查—使用竞态。** `BindMediaAsync` 在取得 Context lease 之前读取 Screen/Voice，并执行状态、ConnectionId 与权限校验；等待 lease 后没有重读记录，直接把先前快照交给 `_contexts.Bind`。最小复现：Accepted 会话上 End 已先持有 coordinator；并发 Bind 在 End 提交前读到 Accepted 后排队；End 提交并移除能力后，Bind 获得 lease，仍用旧 Accepted 快照重建 Context/返回旧状态。与上一快照相比的新证据是“共享 coordinator 已存在”，但关键校验仍位于 lease 外，组件串行测试未覆盖该服务竞态。原要求依据为规格 §3.1/§3.3 的同一 `(tenant,conversation)` 串行状态操作。最小修复：可在 lease 前做身份/格式预检，但取得 lease 后必须重读 Screen/Voice，并重新执行可绑定状态、截止、锁定 ConnectionId 与能力 ID 校验，再修改内存 Context。
2. **High 首 Offer 前采集仍会主动发空 Offer。** High 创建 PC 时按要求不建 transceiver；但 `startScreenCapture`/`startVoiceCapture` 在 `screenSender`/`audioSender` 为 null 后仍无条件调用 `negotiate()`，而 `negotiate()` 没有“High 尚未收到首 Offer/尚无三槽”守卫。最小复现：High 角色在 Low 首 Offer 到达前点击选择屏幕或启动麦克风；本地流被保留但轨无 sender 可挂，随后 High 用 0 个 transceiver 生成 Offer。若与 Low 初始 Offer 交叉，Low 的 perfect-negotiation 路径可能回滚并回答空 Offer，媒体无 m-line 且没有必然的后续触发。与上一快照相比，High 不再预建槽是正确新证据，但采集入口尚未接入“等待首 Offer”的业务守卫。原要求依据为规格 §5.1“首次仅 Low 建槽并发 Offer，High 等首 Offer 后映射，首 Offer 前只保存待接轨”。最小修复：High 且三槽未映射/无首个 remote offer 时只保存流并置等待状态，不调用 `negotiate()`；首 Offer `setRemoteDescription` 后映射三槽、挂待接轨并 Answer，之后才允许 High 发后续协商。
3. **ICE 过期/满载回调仍未完成业务失败语义。** timer 只在早到 candidate 入队时启动，不覆盖“Offer 已发但没有早到 candidate”的整个协商 10 秒期限；`iceQueue.add(...)` 返回 false 时调用方忽略；`expireIce` 只设置 `waitingForPeer` 和本地 operation=`Error`，既不识别本轮新增的是 Screen 还是 Voice，也不调用对应 End/失败状态，因此组合场景会同时把两个 Connecting UI 标 Error，而数据库能力继续活动直至 60 秒 Setup 扫描。与上一快照相比，主动 timer 与 fake-timer 组件测试是新证据，但尚无 store 行为测试证明当前新增能力被单独终结。原要求依据为规格 §2.4/§5.3：每份 Offer 的协商 10 秒未完成失败，超过 128/超时只失败当前新增能力，旧轮不得影响新轮。最小修复：以 negotiation 记录关联本轮新增 capability/session/generation，在 Offer 创建时启动 timer；满载或超时时取消/回滚该轮并调用对应能力的失败终结（共享传输已实际失效时才结束全部），且新增 store 集成测试验证另一能力保持 Active。

本冻结快照的独立定向结论为 **FAIL（1 项关闭，3 项仍有明确集成失败路径）**。这不是泛化回归失败，也不否定开发已通过的全套本地门禁；结果已提交主控作定点诊断，本验收任务不自行再派整轮返工。

环境项单列且不退回代码任务：两台实际 PC、真实浏览器权限/设备、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、Screego 对照、逐 MID/SSRC 计数、5 秒真实停止、30 秒静默失联及 30 分钟质量仍为 `MISSING`，不能由本地自动化替代。

## 独立验收前的三项集成修复定点复验

复核时间：2026-09-12（Asia/Taipei）  
范围：仅针对上一节提出的 Bind 竞态、High 首 Offer、ICE 失败语义；未提交、未推送、未部署、未重启。

完成项：

1. `BindMediaAsync` 取得 `(tenant,conversation)` Context lease 后重新读取 Screen/Voice，并再次执行可绑定、连接与能力 ID 校验；新增无 sleep 的并发行为测试证明 End 持有 lease 时 Bind 排队，End 后 Bind 拒绝且 Context 不复活。
2. High 端首 Offer 前的保护已下沉到公共 `negotiate`；屏幕/麦克风采集只保存待接轨，不创建空 Offer。新增真实 store 入口测试覆盖屏幕和麦克风，收到远端 Offer 后分别映射 video slot 3/audio slot 1，挂轨并发送 Answer。
3. ICE 队列按 negotiation 启动可取消的 10 秒 timer；timer 绑定新增能力的具体 ID 与 generation。成功、忽略/替换、dispose、队列满均清理 timer；超时/满载只调用对应新增能力的 End，既有 Active 能力保持不变。捕获异常也只标记本轮关联能力。

最终定点门禁：

| 命令 | 结果 |
| --- | --- |
| `dotnet build src/backend/IndustrialPlatform.slnx --configuration Release` | PASS，0 warnings，0 errors |
| Collaboration RemoteAssistance 定点测试（`--filter FullyQualifiedName~RemoteAssistance`） | PASS，24 passed，0 failed |
| `node_modules\\.bin\\vitest.cmd run tests/unit/collaborationMediaStore.spec.ts` | PASS，1 file，8 tests |
| `pnpm.cmd build`（`src/frontend`） | PASS；仅既存 chunk size warning |
| `git diff --check` | PASS；仅 Git 行尾转换提示 |

内容证据 `docs/evidence/PF-06-wip-content-sha256.txt` 已更新为 55 个源码/测试/配置/POC 路径的当前 SHA-256；原 64 项路径/状态基线 hash `35a626fc...` 保留作为历史边界，未将后续新增测试路径倒写进该基线。

真实设备/双 PC、浏览器权限与设备、HTTPS/WSS、TURN 各传输、Screego 对照、RTP 分轨、5 秒停止、30 秒静默失联及 30 分钟质量矩阵仍为 `MISSING`，本地行为测试不替代这些验收项。

## 独立验收：三项集成修复集中复核

复核时间：2026-09-12（Asia/Taipei）  
复核对象：HEAD `0a77597f94e38873b67565e0b477e120e72a57f8` 的同一 `develop` dirty-worktree；未提交、未推送、未部署、未重启。  
复核边界：只复核上一节的 Bind 检查—使用竞态、High 首 Offer 前空 Offer、ICE 超时/满载失败语义及其直接影响路径；保留已独立关闭的 EndAll 单事务等结论，不重复全量门禁。

冻结内容核验：`docs/evidence/PF-06-wip-content-sha256.txt` 含 55 个源码、测试、配置与 POC 路径；复核前及定点测试后逐文件重算均为 **55/55 匹配、0 mismatch**。该内容摘要文件自身 SHA-256 为 `44b93bcb1fa68544c8cc4abcdaff5ebbd1886365b885717e7e8e63523096d1d0`。原 64 项路径/状态基线 hash `35a626fc09c339ed796e7bc96414593ea5369438368bd6f1be838ee5b9b873e3` 仅作为历史路径边界保留。

独立定向结果：

| 验证 | 独立结果 |
| --- | --- |
| `Application_RemoteAssistanceBindConcurrencyTests`（Release `--no-build`，复用冻结交付前已完成的新鲜 Release build） | PASS，1 passed，0 failed，0 skipped；Bind 在 End 持 lease 时排队，End 后锁内重读拒绝旧 Accepted 快照，Context 不复活 |
| `node_modules\\.bin\\vitest.cmd run tests/unit/collaborationMediaStore.spec.ts --configLoader=runner` | PASS，1 file / 8 tests；含 High 屏幕与麦克风首 Offer 行为、零候选超时、队列满载及旧 timer 隔离 |
| 相关源码与测试差异检查 | PASS；修复限定于本轮三项及直接行为测试，未发现对已确认 EndAll 路径的新影响 |

关闭结论：

1. **Bind 竞态已关闭。** `BindMediaAsync` 取得 `(tenant,conversation)` lease 后重新读取 Screen/Voice，并重新执行会话归属、可绑定状态/截止、锁定 ConnectionId 与能力 ID 校验；无 sleep 的服务级并发测试命中原失败时序并通过。
2. **High 首 Offer 路径已关闭。** High 在尚无远端首 Offer、PC 尚无三槽时不会创建 Offer；屏幕/麦克风本地轨先保留，收到 Low 的 Offer 后按 m-line 形成三槽，分别挂入 video slot 3 / audio slot 1 并沿用远端 `negotiationNId` 回答。两个 store 入口行为测试均证明没有空 Offer。
3. **ICE 失败语义已关闭。** 每轮 negotiation 从注册 Offer 起拥有可取消的 10 秒 timer；失败目标记录具体 Screen/Voice ID 与 generation，成功、替换、忽略、dispose 会清理旧轮。零候选超时和 128 条队列满载只 End 本轮新增能力，既有 Active 能力保持；旧 timer 不影响新轮。

本轮三项代码缺陷的独立集中结论为 **PASS**。该结论只关闭已派的三条实现问题，不等同于 PF-06 真实媒体验收总体 PASS。A01～A11、V01～V12 所需两台实际 PC、真实浏览器权限/设备、HTTPS/WSS、TURN UDP/TCP/TLS/RelayOnly、Screego 对照、逐 MID/SSRC 计数、5 秒真实停止、30 秒静默失联及 30 分钟质量证据仍统一为 **MISSING**，属于环境/实机验收项，不退回为代码缺陷。

主控最终验证补记：主控在上述三项独立 PASS 后执行最终 Release build，退出码 `0`，`0 warnings / 0 errors`；并已将 active/CURRENT/Todo 更新为“待真实媒体验收”。该结果属于主控最终门禁，不是独立验收重复执行；当前代码返修闭合，后续仅在取得新的真实环境证据后复核新增证据。

## TASK007 本机页面验证补记（2026-09-13）

验证范围：仅验证真实 HTTP 登录、PC 壳/聊天入口呈现和非媒体页面加载；未点击或调用屏幕共享、麦克风、摄像头、`getDisplayMedia`、`getUserMedia`、WebRTC 媒体协商或媒体传输。

隔离运行信息：

- 隔离数据目录：`C:\Users\DONG\AppData\Local\Temp\IndustrialPlatform-PF06-TASK007-20260913`
- SQLite：`task007.sqlite`；admin 一次性凭据仅写入同目录的临时凭据文件，使用后已删除，未进入本记录或终端回显。
- UnifiedHost：PID `54764`，`127.0.0.1:5041`，`GET /health` 返回 `200 Healthy`。
- Vite：PID `45136`，`127.0.0.1:5173`，首页返回 `200`；因 SQLite BCrypt 登录约 18 秒，临时设置 `VITE_REQUEST_TIMEOUT_MS=60000`。
- 未连接、迁移或修改普通/远程数据库，未启动 Docker/私有基础设施，未部署；完成后已停止本次 Vite 进程，UnifiedHost 也仅为本次隔离验证启动。

实际命令（路径与凭据值均不含在记录中）：

```powershell
$env:IndustrialPlatform__DevelopmentInfrastructureMode='Sqlite'
$env:DatabaseTopology__SharedSqliteFile='<TASK007 temp>\\task007.sqlite'
$env:ASPNETCORE_ENVIRONMENT='Development'
$env:DOTNET_ENVIRONMENT='Development'
$env:ASPNETCORE_URLS='http://127.0.0.1:5041'
dotnet <UnifiedHost Debug DLL>

dotnet run --project <Identity.Api.csproj> --launch-profile http --no-build -- --initialize-admin --credential-output '<TASK007 temp>\\bootstrap-admin.json'

$env:VITE_CACHE_DIR='<TASK007 temp>\\vite-cache'
$env:VITE_REQUEST_TIMEOUT_MS='60000'
$env:VITE_API_BASE_URL='http://localhost:5041'
$env:VITE_AUTH_MODE='http'
node node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5173 --configLoader runner
```

页面观察结果：

1. 登录页可达，真实 admin 登录成功并进入 `/pc/home`；PC 壳、HTTP 真实服务标识、顶部聊天快捷入口均呈现。
2. 点击顶部聊天快捷入口仅打开聊天组件，不触发任何媒体 API；PDA/Mobile 路由在当前 admin 权限/本机服务条件下未呈现对应终端入口，不能据此宣称三终端入口通过。
3. 进入 `/pc/collaboration` 时页面未形成可用聊天列表/编辑器。UnifiedHost 因本机未启用 Redis，权限校验、`/auth/me`、SystemData runtime 和 SignalR negotiate 返回 `503 ID_AUTH_SECURITY_STORE_UNAVAILABLE`；创建第二隔离测试用户也因同一安全存储不可用返回 503，因此无法安全完成第二用户会话和普通文本发送。
4. 浏览器无 page error；记录到的 console error 均为上述 503 资源/SignalR 协商错误。媒体调用计数为 `getUserMedia=0`、`getDisplayMedia=0`。

结论：本轮确认了隔离 SQLite 启动、一次性 admin 初始化、真实登录与 PC 全局聊天入口；聊天页/普通文本链路被本机缺少 Redis 的 fail-closed 安全存储阻断，真实媒体验收仍为 `MISSING`。本轮未修改产品源码，未提交、未推送。

### TASK007 只读 Redis 配置核对补记

- 仓库约定入口为 `src/backend/appsettings.Development.local.json`；只读确认 `RemoteDevelopment.Enabled=true`、`DatabaseTopology.Mode=Shared`、Redis 端口字段为 `6379`，Redis 密码字段存在但未读取回显。
- 该私有配置的 Host 为私有/Tailnet 风格地址；只记录为已配置目标，未写入完整地址、账号、密码或连接串。
- 仅针对该已配置目标做一次有限 TCP 检查：`TcpTestNetConnection=False`；因 TCP 不可达，未发送 Redis `AUTH`/`PING`，也未写入任何键、改变远端生命周期或扫描其他目标。
- 代码已支持“SQLite 本地业务库 + 独立 Redis 依赖”：`DevelopmentInfrastructureMode=Sqlite` 只覆盖 `SqlSugar`/SQLite 拓扑，保留 Redis 为独立配置项。下一步最小运行配置（仅方案，未执行）是显式临时 SQLite 路径，同时通过受控环境变量注入同一私有 Redis 连接串；不需要修改鉴权实现或使用 InMemory 替身。
- 上次页面观察使用既有 Debug DLL，是因为统一启动脚本写入仓库 `.run` 被本机权限拒绝，且仅需一次性页面观察；这不是 Release 构建证据。`src/DEBUGGING.md` 明确建议以 `dotnet run --no-build --project ...` 保持项目 ContentRoot，故上次 Debug DLL 观察仅保留为“登录/PC 壳已观察到”的运行事实，不倒推覆盖最新 Release。正式代码结论仍以冻结 55 项内容摘要与主控最终 Release build 为准；本次未重跑测试。

## 独立验收：TASK007 非媒体页面新增证据复核

复核时间：2026-09-13（Asia/Taipei）  
复核范围：只核对上一节新增的隔离运行日志、进程终态与非媒体页面结果；未启动或重启服务，未运行测试，未扫描源码，未调用屏幕/音频采集、发送邀请或建立媒体传输。

独立确认：

1. 临时目录保留 `task007.sqlite`、UnifiedHost/Vite stdout 与 stderr；PID `54764`、`45136` 均已停止，`127.0.0.1:5041/5173` 已无对应监听。日志确认 UnifiedHost 曾监听 `127.0.0.1:5041`，`GET /health` 多次返回 200；Vite stdout 确认本地开发页曾启动。
2. UnifiedHost 日志确认真实 `POST /identity/api/v1/auth/login` 多次返回 200，耗时约 11.8～17.9 秒；因此“隔离 SQLite 启动并完成 admin HTTP 登录”有可复核服务端证据。
3. 前置阻断也可复核：第二用户创建、`/identity/api/v1/auth/me`、SystemData runtime、Collaboration conversations 与 SignalR negotiate 均出现 503；前端日志明确记录 `ID_AUTH_SECURITY_STORE_UNAVAILABLE`。主控已判定 SQLite 不替代 Redis 撤销/安全存储，本轮按环境依赖处理，不开启代码返修。
4. PC 壳、HTTP 标识和顶部聊天快捷入口属于开发任务当时的真实页面观察；本轮没有可独立重放的截图，且服务已按约停止，因此只计为开发页面证据，不能扩张为独立视觉 PASS。PDA/Mobile 入口、第二用户会话和普通文本发送仍未到达。
5. 对上一节“console error 均为上述 503”作范围限缩：前端日志确有 503/`ID_AUTH_SECURITY_STORE_UNAVAILABLE`，但稍后另有 6 条 SignalR `Failed to fetch` console error；现有时间线只能证明当时 Host 不可达或正在停止，不能把它们全部写成 503。日志还包含 DataProtection 临时密钥目录拒绝及 RabbitMQ/Outbox 重试，均作为本地基础设施不完整的附加事实保留；目前没有证据将其归因为 PF-06 产品代码回退或本轮 503 的直接根因。

独立结论：TASK007 新增页面证据为 **PARTIAL**。隔离 Host/Vite 启动、健康检查和 admin HTTP 登录已确认；PC 壳/顶部入口保留为开发观察证据；普通文本聊天被前置安全存储 503 阻断，PDA/Mobile 与真实媒体矩阵仍为 **MISSING**。本轮没有新的代码缺陷证据，既有代码定向 PASS 结论保持不变；在运行环境和支持的隔离组合明确前，不继续页面验证。

### TASK007 只读环境补充

补充时间：2026-09-13（Asia/Taipei）

- 独立验收仅以结构化方式读取私有配置，不回显地址、连接串或凭据：确认唯一私有入口中 `RemoteDevelopment.Enabled=true`、`DatabaseTopology.Mode=Shared`，Redis 配置含端口 `6379`、非空密码字段。
- 开发任务只对该既有私有/Tailnet 风格目标执行一次 TCP 可达性检查，结果为 `TcpTestSucceeded=False`；未发送 Redis `AUTH`/`PING`、未写键，也未变更远端服务生命周期。独立验收遵循非重复验证约定，没有再次探测该目标。
- 开发说明代码支持“临时 SQLite 业务库 + 独立 Redis 安全存储”的隔离组合；受控注入现有私有 Redis 连接串仅是下一步最小候选方案，本轮未执行。当前 TCP 不可达，因此不具备继续普通文本聊天页面验证的前置条件。
- 本轮页面观察使用 Debug DLL，仅用于本地页面/环境证据；原因是 `dev.ps1` 写入 `.run` 被权限阻止。正式代码门禁仍以冻结 55 项内容摘要和主控最终 Release build 为准，不能由 Debug 页面运行替代。

补充结论：现有配置证明仓库已经指向一个受保护的 Redis 依赖目标，但不能证明该目标当前可用。TASK007 仍为 **PARTIAL**，普通文本、三终端入口和真实媒体矩阵继续 **MISSING**；无新增代码返修项。

## 云 PostgreSQL 启动索引漂移修复（2026-09-13）

用户报告语音表缺少两个索引并明确将以默认云 Docker 环境启动 VS/VS Code。主控与原开发定点处理，没有重开媒体功能返修。

- 根因：两索引声明名分别65、66字节，PG实际 `max_identifier_length=63`，名称截断为 `uq_collaboration_remote_assistance_voice_call_active_conversati` 与 `ix_collaboration_remote_assistance_voice_call_conversation_crea`。旧 `SchemaPhysicalDriftGuard` 以完整名比较而误报，导致PF06-002事务回滚。云业务库只读确认 `voice_table_exists=False`、迁移账本仅有PF06-001。
- 修复：仅PG索引期望名通过参数化 `CAST(@indexName AS pg_catalog.name)::text` 由数据库规范化；SQLite保持完整名称，真正缺索引继续拒绝。迁移DDL/ID/描述/账本/checksum及种子未变。
- 开发验证：新鲜Release build 0警告/错误；`SchemaPhysicalDriftGuardTests` 2/2通过，覆盖SQLite长名及真正缺失。
- 主控最终编译：`dotnet build src/backend/IndustrialPlatform.slnx --configuration Release --verbosity quiet` 退出码0、0警告/错误。沙箱曾拒绝写既有测试产物，通过工具审批执行同一命令后成功；没有停止用户调试。
- 主控云PG验证：仓库外临时probe引用实际Collaboration迁移/公共Guard，在随机隔离schema的单事务内执行实际PF06-002及校验、重复执行、UTF8多字节长索引名校验，均PASS；删除该隔离schema内测试索引后仍被Guard拒绝，PASS；finally回滚并查询确认隔离schema不存在，PASS。未修改业务schema、迁移账本、业务数据、私有配置或云服务生命周期。
- 网络事实：沙箱原始连接报 `SocketError=AccessDenied`；通过工具审批后同一配置目标连接成功。此沙箱拒绝不能被表述为云PostgreSQL停机或凭据错误。
- 交付边界：代码修复及PG隔离验证完成；业务库PF06-002留给用户正常调试初始化补跑，无需手工清库/改种子/改账本。没有提交、推送、部署或采集/传输媒体；整包实库升级与媒体验收不由本次定点结果替代。

### 用户启动调试后的实库与入口复验（2026-09-13）

主控复用用户已启动的VS/VSCode实例，只读确认UnifiedHost PID58688监听localhost:5041，`/health`返回200；Vite PID57872监听localhost:5173，首页返回200。未启动/停止任何服务或修改配置。

对默认云PG执行只读查询：PF06-001和PF06-002均已登记，`collaboration_remote_assistance_voice_call`存在，先前报缺的唯一索引`uq_collaboration_remote_assistance_voice_call_active_conversati`及查询索引`ix_collaboration_remote_assistance_voice_call_conversation_crea`均已落地，列顺序/唯一性/活动状态谓词与DDL对应。此次用户真实启动已完成002；这关闭本次启动报错，不扩大为完整PostgreSQL迁移矩阵通过。

浏览器最初通用发现接口失败，随后通过已知localhost入口连接到应用内浏览器成功。验证页位于`/login?redirect=/pc/home`，登录页警告/错误日志为空；尚无登录会话，已显示页面供用户登录，未索取密码。鉴权、聊天和媒体仍待后续实际操作。只读配置核对实际绑定节为`Collaboration:Media`，唯一私有文件未包含该节，正常Host配置也未见媒体开关；不以源码默认值冒充运行时启用证明。未采集或传输屏幕/音频。

### 用户授权后的独立 Playwright Edge 调试复验（2026-09-13）

交付标识：继续复用 develop dirty-worktree，HEAD `0a77597f94e38873b67565e0b477e120e72a57f8`；本轮未修改生产源码、未重启/停止用户实例、未提交/推送/部署。使用项目已安装 `@playwright/test`，每次均以 `channel=msedge` 启动新的无持久化 browser context，不接管用户浏览器资料。

运行实例核对：当前 UnifiedHost PID `58688` 监听 `localhost:5041`，`GET /health` 为 `200`；Vite PID `57872` 监听 `localhost:5173`，首页为 `200`。两者均为本轮核实的实际进程，不照抄旧 PID。

本轮实际步骤与结果：

1. 最小启动/导航探针 PASS：独立 Edge 访问 `http://localhost:5173/login`，登录表单可见，页面标题为登录页，console error 与 requestfailed 均为 0。
2. 一次真实 `e2e.admin` 登录 PASS：进入 `/pc/home`，用户菜单和真实 HTTP/UnifiedHost 状态可见；实际动态导航显示“协作”分组及“聊天”入口，console error、失败请求和 HTTP 4xx/5xx 记录均为 0。
3. 后续两个全新 Edge context 分别使用仓库登记的隔离账号执行一次真实登录，均在登录 POST 阶段超时，页面显示“请求超时，请稍后重试”；两次均未进入受保护壳，前端记录登录与 bootstrap 请求被取消。随后只读探针确认 `/health` 和 bootstrap GET 仍为 `200`，因此记录为登录依赖/运行时并发或外部安全存储路径的环境阻塞，不表述为 UnifiedHost 整体停机。
4. 首次登录后的“聊天”菜单点击未在限定时间内形成预期 `/pc/collaboration` 页面，未绕过入口手输路由继续；聊天成员列表、普通文本、顶部媒体入口及邀请状态均未达到可安全验证状态。

范围与安全边界：由于没有稳定的双隔离会话和已确认的测试接收端，本轮没有发送普通文本、没有向普通用户发起邀请，没有调用 `getUserMedia`/`getDisplayMedia`、没有建立 WebRTC 媒体传输，也没有读取或输出令牌、连接串、SDP、ICE、真实媒体。`Collaboration:Media` 的运行时有效开关、TURN 凭据及候选路径在未建立稳定认证会话前无法通过实际页面确认；私有配置未见该节仅作为静态事实，不冒充运行时结论。

结论：本轮为 **PARTIAL/BLOCKED（登录阶段）**。已确认用户授权的独立 Edge 启动、UnifiedHost/Vite 实例、一次真实管理员登录和动态聊天菜单存在；同一活动调试环境后续真实登录 POST 超时，阻断聊天/对端/媒体路径。未发现新的生产代码缺陷，未进入邀请或媒体步骤；需主控按现有登录安全存储/并发环境继续分类，不重复盲试浏览器。

### Playwright 登录/路由定点诊断收口（2026-09-13）

诊断目标：区分前一轮“登录超时/聊天未导航”是测试等待边界、前端请求超时、真实后端依赖还是可复现产品缺陷。本轮只使用一次新建的非持久化 `msedge` context，未重启/停止用户服务，未修改生产源码。

诊断脚本与脱敏产物：

- 脚本：`tools/pf06-playwright-login-diagnostic.mjs`（临时、未提交的诊断脚本，不属于生产源码）。
- 结果：`C:\Users\DONG\AppData\Local\Temp\PF06-playwright-diagnostic-1789275113609\result.json`。
- 时间线：同目录 `timeline-summary.txt`；页面截图：同目录 `logged-in.png`。产物不含密码、令牌、请求体、SDP、ICE 或真实媒体。

定点结果：

1. 有界诊断设置为页面导航 `30,000ms`、登录观察 `20,000ms`；真实 `e2e.admin` 从点击登录到 `/pc/home` 为 `3,701ms`。
2. 真实 DOM 的“聊天”链接 href 为 `/pc/collaboration`；精确定位后点击立即到达该路由。仓库路由定义的 PC child path 同为 `collaboration`，因此前一轮“聊天未导航”归因为旧脚本未先核对 href、并使用了不必要的 `waitForURL` 等待边界，不能归因于产品路由失败。
3. 该次登录后的登录接口、SignalR negotiate、`auth/me`、会话列表和登录审计响应均为 `200`；console error 与 requestfailed 均为 `0`。页面截图显示真实聊天壳已到达，列表当时仍处于初始加载观察窗口。
4. 运行中的 Vite 变换模块 `/src/config/runtimeConfig.ts` 明确暴露 `VITE_API_BASE_URL=http://localhost:5041`、`VITE_AUTH_MODE=http`、`VITE_REQUEST_TIMEOUT_MS=10000`；HTTP client 将解析后的 `requestTimeoutMs`直接传给 Axios timeout。前两次短观察分别为 `10,000ms` 与 `15,000ms`，页面显示超时，requestfailed 为 bootstrap GET 与 login POST `ERR_ABORTED`，没有服务端响应状态，不能表述为 503。修正后的单次诊断成功，故未形成稳定可复现的产品登录缺陷。

本轮仍未执行普通文本、已确认隔离对端邀请、`getUserMedia`/`getDisplayMedia`、WebRTC 协商或媒体传输；`Collaboration:Media`运行时开关与TURN路径仍待稳定双会话和明确测试接收端。修正结论：页面入口与真实登录/路由为 **PASS（本轮有界）**；文本与媒体矩阵继续 **MISSING**，无新增生产代码返修项。

## 独立验收：Playwright Edge 新增页面证据核对

复核时间：2026-09-13（Asia/Taipei）  
复核边界：仅核对上一节现有交付和当前运行入口，不再次登录或运行 Playwright，不复查已通过代码，不并写生产源码。独立只读确认交付时 PID `58688`/`57872` 仍为 UnifiedHost/node，`localhost:5041/health` 与 `localhost:5173/` 均返回 200。

核对结论：

1. 开发记录的独立 Edge 最小探针、一次 `e2e.admin` 登录、动态“协作→聊天”菜单与零 console/request failure，作为本轮开发页面证据保留；本验收没有机械重放，因此不另记第二份页面 PASS。
2. 后续两个全新 context 均停在登录 POST 超时，受保护壳、聊天页、测试对端和媒体入口均未到达。当前只有“请求超时/请求取消、同时 health/bootstrap GET 仍 200”的时间线；安全存储、并发、外部依赖以及 context 关闭时机均只是候选，尚不能确认根因或据此开启代码返修。
3. “聊天”菜单点击未形成预期 `/pc/collaboration` 的含义也待定点诊断：需先确认动态菜单的真实目标及等待边界，不能在目标未确认前把它判为路由缺陷，也不能绕过菜单手输路由制造通过证据。
4. 普通文本、隔离接收端、正式邀请/信令、`Collaboration:Media` 运行时条件、TURN、屏幕/麦克风采集及媒体传输均未执行，继续为 `MISSING`。本轮没有向普通用户发送邀请，也没有用同机或合成媒体冒充双机真实媒体。

本轮独立状态为 **PARTIAL/BLOCKED（登录阶段）**。既有 55 项冻结代码与三项定向修复 PASS 保持不变；等待开发完成登录超时、context 取消和聊天目标路由的单次定点诊断后，只复核新增响应/时间线及其实际影响路径，不重开完整验收。

## 独立验收：Playwright Edge 定点诊断复核

复核时间：2026-09-13（Asia/Taipei）  
复核对象：开发提供的 `tools/pf06-playwright-login-diagnostic.mjs` 及 `C:\Users\DONG\AppData\Local\Temp\PF06-playwright-diagnostic-1789275113609` 中的结果、时间线和截图；未重新登录或运行浏览器，未改生产源码。

工件核验：

| 工件 | 独立核对 |
| --- | --- |
| `result.json` | SHA-256 `e82bbb1314bd24d383963adcf7c2dd8cdbd2d3883cc613b1fbcfff34d89efa52`；记录 `msedge`、新非持久 context、1440×900、登录/路由时间线与脱敏请求结果 |
| `timeline-summary.txt` | SHA-256 `850324d6fff8328efcded847cf884db520d05ff310412912a3d1f138b99c170d`；明确区分早期短观察取消与本轮成功时间线 |
| `logged-in.png` | SHA-256 `72eec4bdfbed2a89c24ea15091e722f655b92c57cd42a64c37ae5f84fe2905fa`；截图显示真实 PC 壳、动态“协作→聊天”、聊天标签及 `/pc/collaboration` 页面内容区 |

定点结论：

1. 管理员登录点击后 3701ms 到达 `/pc/home`；动态 DOM 中精确聊天链接 `href=/pc/collaboration`，点击后到达 `/pc/collaboration`。登录、SignalR negotiate、`auth/me`、conversations、登录审计请求均为 200，console error 与 requestfailed 为 0。因此此前“聊天未导航”已关闭为 Playwright 等待/定位边界，不是产品路由失败。
2. 前两次 10s/15s 短观察只有 bootstrap/login `net::ERR_ABORTED`，没有 response status；当前 Vite 转换后的运行配置为 axios 10 秒超时。结合本次 3.7 秒成功登录，只能说明旧脚本在短观察/context 关闭时取消请求，不能写成 503、安全存储失败或 UnifiedHost 故障。
3. 截图中的会话区仍处于“正在加载…/选择一个会话”状态；本轮只证明 PC 动态入口、聊天路由及首批 API 可达，不证明测试对端、会话加载终态、普通文本发送或顶部媒体操作可用。
4. 本轮没有发送文本/邀请，也没有执行屏幕或麦克风采集、SDP/ICE 检查或媒体传输。PDA/Mobile、双隔离账号、正式邀请/信令、`Collaboration:Media` 运行时条件、TURN、双机真实媒体、弱网与 Screego 对照继续为 `MISSING`。

最新独立状态更新为 **PARTIAL（PC 管理员登录与动态聊天路由 PASS；业务对端、文本与媒体未验）**。上一节 `PARTIAL/BLOCKED（登录阶段）` 是诊断前历史状态，不再作为当前登录阻断结论；既有冻结代码 PASS 保持不变，无新增生产代码返修项。

## 功能开发任务：双上下文测试身份准备/清理阶段性记录（2026-09-13）

本节是功能开发任务为后续业务验证准备隔离身份的事实记录，不是独立验收结论。主控随后已要求停止登录重跑、停止账号创建，不等待运行实例恢复再补记本节；本轮不修改生产源码、不改私有配置、不重启/停止用户 Host/IDE、不清库。

### 已发生的隔离身份操作

均使用项目已安装 `@playwright/test`，`msedge` 新建非持久化 context；只记录必要测试标识，不记录密码、token、Cookie、SDP 或 ICE。

| 测试标识 | 创建时间（UTC） | 创建/权限响应 | 登录与改密响应 | 清理事实 |
| --- | --- | --- | --- | --- |
| `pf06-peer-1789276169199` | `2026-09-13T05:09:36.106Z` | 正式“用户管理”界面创建；`POST /identity/api/v1/users`=`200`。本轮随后因查询按钮定位错误中止，未完成角色分配。 | 未登录，未发生改密。 | 后续清理脚本仅完成界面操作后结束，未留可复核的 DELETE 响应状态；因此清理**未确认**，不得记为删除成功。 |
| `pf06-peer-1789276369224` | `2026-09-13T05:12:56.409Z` | 正式“用户管理”界面创建；`POST /identity/api/v1/users`=`200`。随后误按本轮当时授权通过正式 UI 分配既有 `SYSTEM_ADMIN`；`PUT /identity/api/v1/users/pf06-peer-1789276369224/roles`=`200`，时间 `2026-09-13T05:13:00.178Z`。该高权限分配不作为后续聊天测试默认方案，恢复服务后应先核对并撤销/清理。 | 首次登录进入改密门禁 `2026-09-13T05:13:04.102Z`；`POST /identity/api/v1/auth/change-password`=`200`；随后重新登录 `POST /identity/api/v1/auth/login`=`200`，页面到达 `/pc/home`（`2026-09-13T05:13:17.451Z`）。 | UI 清理动作记录于 `2026-09-13T05:13:43.898Z`，但 `DELETE /identity/api/v1/users/pf06-peer-1789276369224` 仅记录 `net::ERR_ABORTED`，没有响应状态；清理**未确认**，不能称删除成功。 |

响应状态来自脱敏脚本产物：

- `C:\Users\DONG\AppData\Local\Temp\PF06-playwright-dual-context-1789276169199\result.json`
- `C:\Users\DONG\AppData\Local\Temp\PF06-playwright-dual-context-1789276369224\result.json`
- 两份产物均不含密码、token、Cookie 或请求体；后者明确将 DELETE 记为 `requestfailed/net::ERR_ABORTED`，不是 HTTP 成功响应。

### 未完成及阶段性结论

1. 本轮没有完成两个账号之间的聊天会话、普通文本发送/接收、媒体邀请、拒绝、取消或超时路径；没有调用 `getUserMedia`/`getDisplayMedia`，没有建立 WebRTC 媒体传输。
2. 后续两次新登录尝试均在发送端 bootstrap/login 阶段 `net::ERR_ABORTED`，未创建新账号；此后不再重跑。功能开发脚本最后检查到 UnifiedHost 进程仍存在但 `localhost:5041` 无监听，主控独立确认运行状态，不在本节推断后端根因。
3. 当前阶段性状态为 **PARTIAL/BLOCKED（运行服务与临时身份清理确认）**。上述两个临时标识必须在服务恢复后先通过正常管理流程核对存在性；若存在，先撤销本轮临时高权限，再按已批准的最小聊天权限与自有资源清理规则处理。未获准的不可逆删除不在本轮强做。

## 临时身份收尾复核与范围纠正（2026-09-13）

主控已用正式审批的只读命令确认：`2026-09-13T13:18:15+08:00` 时 PID `58688` 在 `127.0.0.1:5041` 与 `[::1]:5041` 监听，PID `57872` 在 `[::1]:5173` 监听，`GET /health=200`。此前本任务用 PowerShell 异构对象表格观察到“无监听”是展示误判，只保留为当时观察，不作为当前服务停机或后端根因结论；本任务未重启/停止服务。

主控要求停止登录重跑、停止创建账号后，开发任务仅以正常用户管理界面查询已发生的两个自有标识：

| 标识 | 查询时间（UTC） | 查询结果 | 本轮后续写入 |
| --- | --- | --- | --- |
| `pf06-peer-1789276169199` | `2026-09-13T05:22:00.202Z`～`05:23:11.021Z` | 活动列表未出现；开启“包含已删除”后出现，状态文本为“启用/需改密”，有效角色数 `0`，不是已删除墓碑。 | 未观察到角色 PUT 或 DELETE 响应。 |
| `pf06-peer-1789276369224` | `2026-09-13T05:22:02.205Z`～`05:23:43.645Z` | 活动列表未出现；开启“包含已删除”后出现，状态文本为“启用/无需改密”，有效角色数 `1`，不是已删除墓碑。 | 未观察到角色 PUT 或 DELETE 响应；其既有 `SYSTEM_ADMIN` 分配与改密事实保持前节记录。 |

收尾脚本多次尝试从当前可见“更多”菜单进入“分配角色/删除”，均因 Element Plus/VXE 固定操作列菜单重叠而未打开目标抽屉或确认框，没有可记录的成功响应状态。一次强制点击仅弹出“禁用确认”对话框，未点击确认，不能算作状态变更。最后一次清理专用登录在等待登录路由阶段超时，未产生新账号或凭据操作；按主控指令不再重试。

因此当前必须明确交接：两个自有临时账号仍需由后续获准操作者通过正常管理流程收尾；第一账号无需撤销角色但需安全删除，第二账号必须先撤销本轮误授的 `SYSTEM_ADMIN`，再按项目可恢复清理规则删除。后续若重新建立业务双端，只能使用最小聊天权限，不能再把 `SYSTEM_ADMIN` 作为聊天测试默认权限，也不得再次代替用户改密。本任务不再执行独立验收链路。

## 独立验收：临时测试身份与权限收尾核对

复核时间：2026-09-13（Asia/Taipei）  
复核边界：仅核对开发阶段记录和两份既有脱敏 `result.json`；未重新登录、查询、建号、改密、赋权、撤权、禁用或删除账号，未试文本或媒体。

工件结果：

| 临时身份工件 | SHA-256 | 独立核对 |
| --- | --- | --- |
| `PF06-playwright-dual-context-1789276169199/result.json` | `739cc9311242fd814318eb97b9bc4a45cdad464a9f1a3acbe9fecdbe3e09df0d` | 用户管理 UI 创建 POST=200；未分配角色、未登录；清理定位超时，`isolatedUserDeletedThroughUi=false` |
| `PF06-playwright-dual-context-1789276369224/result.json` | `703d7021fe122e36275258e7d1d78ce3d4724c3f7a14176fd1e48e7768727182` | 创建 POST=200、SYSTEM_ADMIN 角色 PUT=200、首次改密 POST=200、随后登录 POST=200；DELETE 仅 `net::ERR_ABORTED`，没有 HTTP 响应状态 |

核对结论：

1. 第二份结果摘要虽写有 `isolatedUserDeletedThroughUi=true` 和客户端“deleted”时间线事件，但同份网络失败清单明确记录 DELETE `net::ERR_ABORTED`，无成功响应；后续用户管理查询又确认该标识仍非删除墓碑。因此网络结果和现存查询优先，不能宣称清理成功。
2. 两个本轮自有标识当前均未完成清理。第一账号有效角色数为 0；第二账号有效角色数为 1，仍是本轮误授的 `SYSTEM_ADMIN`。这构成测试资源与临时高权限收尾风险，必须由后续获准操作者先撤销第二账号的 SYSTEM_ADMIN，再按可恢复流程清理两个账号；不能继续用高权限账号开展聊天验证。
3. 一次强制点击只打开“禁用确认”且未确认，没有状态写入。现有证据中没有成功的角色撤销 PUT、禁用提交或 DELETE 响应，故不能以 UI 点击记录推断状态改变。
4. 主控在 `2026-09-13T13:18:15+08:00` 正式只读确认 PID `58688` 仍监听 5041 且 health=200；此前“进程存在但无监听”是 PowerShell 展示误判，不作为当前服务停机或清理失败根因。
5. 双账号普通文本、接收端状态、邀请/信令及真实媒体仍未执行。本轮收尾问题不改变冻结代码 PASS，也没有形成生产源码返修项。

最新独立状态为 **BLOCKED（自有测试账号清理与临时高权限撤销）**；页面业务链仍为 **PARTIAL**，文本与媒体矩阵继续 **MISSING**。账号收尾完成并提供成功响应与最终查询状态前，不继续双端业务或媒体操作。

### 主控完成临时测试账号收尾（2026-09-13 13:33 +08:00）

开发停止全部身份操作后，主控使用仓库既有隔离管理员账号，经正式 Identity API 和工具审批完成定点收尾。仅操作上述两个精确用户标识，并核对用户标识/登录名/当前租户一致；凭据仅在内存处理，未创建身份、改密、改库或重启调试服务。

- `pf06-peer-1789276369224`：操作前为活动用户，直接角色仅 `SYSTEM_ADMIN`，无继承角色；角色 PUT=200，重新读取确认有效角色为空；可恢复 DELETE=200。
- `pf06-peer-1789276169199`：操作前为活动用户且无角色；可恢复 DELETE=200。
- 两者最终均通过 `includeDeleted=true` 查询确认 `isDeleted=true`，并通过 `includeDeleted=false` 查询确认精确标识匹配数为0；不能仅依据DELETE响应或UI摘要判定。
- 主控本次管理登录与注销均200。脚本退出码0；脱敏结果为仓库外 `D:/Code/Industrial Platform/pf06-test-account-cleanup-result.json`，仅含方法、固定路径、状态码和必要测试账号状态，无凭据。

本次关闭临时高权限与账号清理阻塞，不扩大为双端聊天/媒体验收通过。后续仅继续既有账号可验证的入口与运行时媒体条件，不再为测试默认授予管理员角色或反复创建、改密测试身份；双端所需最小权限身份条件另行明确。无生产源码变化，不重跑构建或全套测试。

## 独立验收：临时测试身份清理收口复核

复核时间：2026-09-13（Asia/Taipei）  
复核边界：仅静态核对主控提供的脱敏结果与脚本源码；未重新登录、查询或执行脚本，未修改生产源码，未重跑构建、测试或业务/媒体链路。

工件核验：

| 工件 | SHA-256 | 独立核对 |
| --- | --- | --- |
| `D:/Code/Industrial Platform/pf06-test-account-cleanup-result.json` | `09d0497009be84bb8e31e7f25f3f0bda9782aaee551388ec8eed9cdb606d17fc` | 16 个脱敏事件；不含密码、access/refresh token、Authorization、JWT 或 Bearer 值 |
| `D:/Code/Industrial Platform/pf06-test-account-cleanup.ps1` | `88933df28e051dde049636e706fd4027e736ae10218292636ceded970a1842f2` | 目标仅为两个精确测试标识；凭据从既有测试源读取并在 `finally` 清空，源码未嵌入凭据值；异常会记录安全错误并以非零退出 |

收口结论：

1. `pf06-peer-1789276369224` 的角色 PUT 返回 200；脚本随后重新精确读取该账号，并以 `effectiveRoleNIds.Count == 0` 为强断言，未通过即抛出 `ROLE_REVOCATION_NOT_CONFIRMED`。结果继续记录其 DELETE=200、`includeDeleted=true` 后 `deleted=true`、`includeDeleted=false` 后精确活动匹配数 `0`。
2. `pf06-peer-1789276169199` 操作前直接角色为空、继承角色数为 0；结果记录其 DELETE=200、`includeDeleted=true` 后 `deleted=true`、`includeDeleted=false` 后精确活动匹配数 `0`。
3. 所有记录的 HTTP 状态均为 200，末项注销 POST=200；结果不存在 `failure` 事件。结合主控记录的退出码 0，以及脚本对角色撤销、墓碑状态和活动列表残留的失败即异常/非零退出逻辑，前节“自有测试账号清理与临时高权限撤销”阻塞已获得充分后置证据，可关闭。
4. 本结论仅关闭两个自有临时账号及误授 `SYSTEM_ADMIN` 的收尾风险；不证明双账号普通文本、接收端状态、邀请/信令、`Collaboration:Media` 运行条件或真实音视频矩阵通过，也不改变既有冻结代码 PASS。

最新独立状态更新为 **PARTIAL（临时测试身份与权限清理 PASS；业务对端、文本与媒体仍 MISSING）**。上一节 `BLOCKED（自有测试账号清理与临时高权限撤销）` 为清理前历史状态，不再是当前阻断项；本轮无新增生产源码返修项。

## 功能开发：管理员单次只读入口与媒体能力探针工件回收（2026-09-13）

本节仅记录功能开发任务按新模型强度执行的一次有界只读探针，不构成独立验收通过。主控已明确不再登录重跑、不再创建/修改账号、不发送业务消息或建立媒体、不跑测试；由于输出不可恢复，本节不反向保证负向 Hub 调用一定未到达。

### 脚本与工件

- 临时脚本：`D:\Code\Industrial Platform\IndustrialPlatform\tools\pf06-playwright-admin-readonly-probe.mjs`。脚本在本轮结束后已删除，不是生产代码。
- 脚本只执行一次既有 `e2e.admin` 登录尝试；只在登录并进入聊天页后，按代码路径准备受保护 Hub 只读/负向调用。没有账号创建、角色/密码变更、文本发送、成功邀请/媒体建立、媒体捕获或 token 输出证据；负向 Hub 调用是否实际到达不可恢复。
- 本轮没有 probe 专用临时目录、`result.json` 或 stdout 日志。脚本结果仅通过进程 stdout 输出；工具序列化输出超过传输上限后，未留下可解析的本地结果文件，因此 UI 路由、按钮状态、HTTP 状态及运行时 Hub 安全错误码均为**未持久化、不可恢复**，不以源码推论代替运行时证据。
- 发现的 `C:\Users\DONG\AppData\Local\Temp\playwright_chromiumdev_profile-*` 是浏览器临时 profile，不含本次探针的白名单结果工件，不能冒认 `result.json`。现有 `PF06-playwright-dual-context-*\result.json` 属于此前双上下文流程，也不冒认本次探针结果。
- 进程退出码未被超限后的工具返回持久化，故记为“退出码不可回收”；脚本 `finally` 明确调用 `context.close()` 与 `browser.close()`，当前未发现可归属本次探针的 Edge 进程；临时 profile 目录仍存在，未对未知目录做删除。

### 实际调用范围与缺项

源代码确认 Hub 路径为 `/collaboration/hubs/collaboration-v1`，代码中的调用计划包含 `GetMyActiveMedia`、`InviteScreenShare`、`InviteVoiceCall`。由于本次 stdout 未持久化，无法证明登录/聊天前置条件是否成功，也无法证明上述三个 Hub 方法实际到达；因此不得记录 `MEDIA_DISABLED` 或 `MEDIA_NOT_FOUND` 运行时结果。它们只能标记为“脚本调用路径存在，运行时调用结果不可恢复”。

`ScreenEnabled`、`VoiceEnabled`、`IcePolicy`、`IceServerUrls`、`TurnCredentialSecret` 仅完成源码接口/开关盘点，未形成运行时布尔值、ICE/TURN 可用性或媒体会话证据。要补齐该矩阵仍需同租户两个隔离身份及最小权限：`collaboration.messaging.read`、`collaboration.messaging.conversation.start`、`collaboration.messaging.write`、`collaboration.presence.connect/read/write`，以及 `remote-assistance.session.share/join`、`remote-assistance.voice.call`；共享方需 share、接收方需 join，语音双方需 call。不能为此再次建号、改密、扩权或重复登录。

本轮功能开发结果为 **PARTIAL（只读探针已执行但结果未持久化；入口与媒体运行时不新增通过证据）**，无生产源码变更。

## 独立验收：只读探针工件边界与静态能力复核

复核时间：2026-09-13（Asia/Taipei）  
复核边界：未重新登录、启动浏览器、查询账号、调用 Hub 或运行测试；仅核对功能开发交接、现有证据和源码中的固定契约。临时探针脚本当前不存在，不尝试重建或重跑。

独立核对：

1. 前端 PC 子路由 `path: 'collaboration'` 位于 `/pc` 下，要求 `collaboration.messaging.read`；前端 SignalR 客户端连接 `${apiBaseUrl}/collaboration/hubs/collaboration-v1`，后端同时映射 `/hubs/collaboration-v1` 与 `/collaboration/hubs/collaboration-v1`。这与此前已经持久化的管理员动态菜单 `/pc/collaboration` PASS 相符，但本轮没有新增 UI 证据。
2. Hub 类整体受 `permission:collaboration.messaging.read` 保护，并暴露 `GetMyActiveMedia`、`InviteScreenShare`、`InviteVoiceCall`。屏幕邀请先执行 `EnsureFeature(ScreenEnabled, "MEDIA_DISABLED")` 再读取 conversation；语音邀请同样先执行 `EnsureFeature(VoiceEnabled, "MEDIA_DISABLED")` 再读取 conversation。因此“用无效 conversation 区分开关禁用与对象不存在”在源码上可成立，但没有持久化调用结果，不能记录任何实际 `MEDIA_DISABLED`/`MEDIA_NOT_FOUND` 响应。
3. 源码配置契约包含 `ScreenEnabled`、`VoiceEnabled`、`IcePolicy`、`IceServerUrls`、`TurnCredentialSecret`；本轮没有运行时配置值、ICE server 列表、TURN 鉴权或连通性工件，故全部仍为 `MISSING`，不能用静态键存在替代。
4. 后续若具备合规的双端隔离身份，源码所示最小业务能力候选应使用精确权限标识：`collaboration.messaging.read`、`collaboration.messaging.conversation.start`、`collaboration.messaging.write`、`collaboration.presence.connect/read/write`、`remote-assistance.session.share/join`、`remote-assistance.voice.call`。这是静态权限映射，不是已赋权、已登录或已完成双端验证的证据。
5. 功能开发交接明确本次 stdout 因工具序列化超限未持久化，专用 `result.json`、stdout 日志和确切退出码均不存在；此前 `PF06-playwright-dual-context-*` 工件属于其他流程。缺少可独立复核的固定字段时，不接受“脚本曾计划调用”作为 UI、HTTP、Hub 或媒体运行时 PASS。

本轮总体收束为 **PARTIAL**：项目 Playwright Edge 启动、一次真实管理员登录、精确动态聊天导航及已记录的 200 响应保留为既有开发/复核证据，临时测试身份与误授高权限清理为独立 PASS；本轮只读探针不新增运行时通过证据。双端文本、接收端状态、成功邀请/媒体建立、负向 Hub 调用实际到达、`Collaboration:Media` 运行时值、真实分轨停止、TURN/RelayOnly 连通性和质量矩阵继续为 **MISSING**。无新增生产源码返修项，不再重复探针或登录。

## 独立验收：P0 三入口错误反馈定向复核

复核时间：2026-09-13（Asia/Taipei）  
复核边界：仅核开发冻结的 P0 补丁、私有开关隔离及相关前端回归；未启动/探测服务，未登录、点击页面、调用 Hub、读取云依赖配置或运行全套构建/测试。真实页面证据由开发链独占，本节不复用管理员历史入口证据冒充新补丁运行时通过。

### 冻结交付与配置边界

- 开发提供的 9 个 WIP 文件 SHA-256 已逐项独立匹配：`CollaborationChat.vue`、`CollaborationMediaHost.vue`、`mediaError.ts`、`localization/types.ts`、中英文 locale、`collaborationMediaStore.ts` 及两个定向单测。按路径排序并以 `path=sha256`、UTF-8、LF 拼接后的清单 SHA-256 为 `1166d69f710eba19f7472c2cc500d0d42cd5ceb6b207d0f8491f24fbb6730afb`。
- 唯一 `src/backend/appsettings.Development.local.json` 中定点读取到 `Collaboration.Media.ScreenEnabled=true`、`VoiceEnabled=true`，`RemoteDevelopment.Enabled=true` 保持不变；该文件命中仓库 `.gitignore` 的 `appsettings.*.local.json` 规则且 `git ls-files` 不包含它。未读取、输出或登记其他云连接/凭据值。
- 私有配置源已修正不等于当前运行宿主已经加载；没有运行时 `GetMyActiveMedia`/Invite 返回证明这两个开关在现有 5041 进程中生效。

### 补丁与回归结论

1. `formatMediaError` 以一个小型共享映射覆盖 `MEDIA_DISABLED`、`MEDIA_BUSY`、`MEDIA_FORBIDDEN`、`MEDIA_NOT_FOUND`、`MEDIA_NOT_ACCEPTED`、`MEDIA_UNSUPPORTED`、`MEDIA_DEPENDENCY_UNAVAILABLE`、`MEDIA_SIGNAL_FAILED`，中英文均给出用户文案并保留技术码；未知码使用安全兜底文案。该实现复用现有 locale 类型，没有新增依赖或第二套错误框架，符合最小修复边界。
2. 三个按钮仍分别进入 `inviteVoiceCall`、`inviteScreenShare('ShareMine')`、`inviteScreenShare('RequestPeer')`。无 realtime/无会话、跨会话忙碌、SignalR invocation 抛错与 Hub `ok=false` 均会写入 `errorMessage`；所选会话内以 `role=alert` 显示，已有媒体会话期间全局 Host 也以 `role=alert` 显示。因此“初始失败无反馈”的代码路径已关闭。
3. 失败分支只修改本次能力的 operation 与共享错误码，不清空另一媒体能力的 DTO、operation、采集流或 PeerConnection；`consumeResult(ok=false)` 也不覆盖原 target。未发现失败屏幕邀请结束既有语音、或失败语音邀请结束既有屏幕的路径。
4. 定向测试由本独立任务在冻结哈希上运行：`node.exe node_modules/vitest/vitest.mjs run --config vitest.config.ts --configLoader runner tests/unit/collaborationMediaStore.spec.ts tests/unit/collaborationMediaError.spec.ts tests/components/CollaborationChat.spec.ts`，退出码 0，3 files / 36 tests PASS。开发另报 `vue-tsc --noEmit --pretty false` PASS 与 `git diff --check` 无错误；本任务未机械重跑。
5. 现有 store 新增断言直接覆盖 invocation 抛错，错误 helper 覆盖已知/未知码；Hub 解析成功但业务返回 `ok=false` 的分支本轮以源码直读确认，尚无新页面工件或专门断言。该覆盖缺口不否定简单赋值链路，但保留为运行时单次复验重点。

### 独立结论

P0 补丁代码与定向回归判定为 **PASS**；私有媒体开关已正确落在非版本控制的唯一 Development local 配置中。三入口真实页面/Hub 结果判定为 **NOT RUN / BLOCKED（开发任务未取得可用测试凭据，未登录、未点击、未生成脱敏 result.json 或截图）**，不得据代码或 36 项测试宣称运行时通过。

PF-06 总体继续为 **PARTIAL**：既有管理员登录/动态聊天入口 PASS、临时身份与误授高权清理 PASS 均保持；三入口新错误反馈的代码 PASS，但页面运行时、配置实际加载、双端文本/邀请/媒体建立、真实停止、TURN/RelayOnly 与质量矩阵仍为 **MISSING**。本轮不新增生产源码返修门禁；云依赖与受控测试身份条件具备后，只需对三个入口做一次脱敏定点页面复验。

# TASK-ID-017 验证证据

## 状态

历史验收通过。

## 提交哈希

`07d6863 feat(identity): add user group authorization model`

## 修改范围

- Identity 用户组领域模型、关系实体和领域事件。
- 用户组迁移、仓储、管理存储和 Outbox。
- 用户组应用服务、审计与授权快照。
- Domain、Infrastructure、Application 和 Contract 测试。

## 关键决策

- 有效角色为直接角色与有效用户组继承角色的并集。
- 成员、组角色或组状态变化会推进受影响用户 AuthVersion、撤销会话并失效权限缓存。
- 用户组禁止嵌套，重复成员和角色分配为幂等 no-op。
- 最后管理员守卫使用包含组继承路径的权威持有者计数。

## 验证命令与结果

历史记录：Release 全量构建 0 警告、0 错误；全量测试 842/842 通过。本次上下文整理未重新运行测试。

## 剩余风险

- 事件名称当前沿用既有 `Identity.*.v1` 风格；若改为小写连字符，需要独立契约变更。

## 范围外发现

无。

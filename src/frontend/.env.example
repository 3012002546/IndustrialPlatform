# Industrial Platform 统一前端运行配置示例(安全示例值,不得提交真实凭据)

# Gateway 统一入口(前端只走这里,不直连各服务)
VITE_API_BASE_URL=http://localhost:5080

# 认证适配器选择:mock(仅开发/测试;生产构建禁止 mock)
VITE_AUTH_MODE=mock

# HTTP 请求超时(毫秒,必须为正整数)
VITE_REQUEST_TIMEOUT_MS=10000

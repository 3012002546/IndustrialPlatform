# Design QA — 用户管理业务操作载体

- 参考：用户提供的“行政组织与岗位”居中弹窗、右侧抽屉截图。
- 实现：`/pc/identity/users` 的新建/编辑、详情、分配角色。
- 边界：禁用、删除、重置密码等纯确认提示维持原实现；表格功能弹窗未修改。
- 视觉：720px 居中弹窗、560px 右侧抽屉、标题栏、模式切换、120px 标签列、底部操作区与参考布局一致。
- 响应：PC 可切换并记忆布局；PDA/Mobile 继续使用全宽抽屉。
- 多语言：载体的模式、关闭、取消、确认、提交中均覆盖中文和英文。
- 实页验证：新建用户、用户详情、分配角色均正常打开和关闭，未提交或改写业务数据。
- 自动验证：96 个测试文件、716 个测试全部通过；Vue 类型检查、ESLint、Prettier、`git diff --check` 通过。

final result: passed
